# M4 — post-merge review (Fable) — item 70
Reviewed at merge 7ec8125. Census: 7 files, +711/−40, zero
schema/server/deps. VERDICT: PASS. Item 70 REVIEWED — GREEN.
The park-law high-water mark: m3's Q1 check parked EVEN THOUGH its
literal selectors would still pass vacuously — "a vacuously-green
check that asserts a retired truth is the thing the immutability
law exists to prevent." m2/m3's parked blocks gain their first
tenants (originals verbatim, re-asserted against the new opposite
truths) and the exit code now covers parked checks, byte-identical
when unset. m4.mjs proves the origin staircase through EVERY value
1..7 across a 4000-word sweep, roots at the SEGMENT level (distinct
origin roots, not shoots), and makes PAGE IS PRIMARY executable:
the paper's rect byte-identical with the instrument mounted and
absent. The instrument is inert (pointer-events:none — "never a
control"); the flare is evental orange (ember token, carries no
text, aria-hidden, reverts — counts nothing, remembers nothing);
both styles share one lane; FX1 S5's dead meter stays dead; legacy
<1100 byte-identical. DeskInstrument renders siblings-not-ternary
so toggling never remounts the growth layer. READ SCOPE: the three
harness files + DeskInstrument at full patch; the residual
CSS/wiring (~125 lines) verified through its asserted consequences
at three widths.
