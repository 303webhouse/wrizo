# FX12 — the Quiet House · P0 fix brief · 2026-07-24

**Place at:** `docs/wrizo-alpha/fx12-quiet-house-brief.md`.
**Owner: chat 1.** Branch `fx12-quiet-house`, own worktree; guard-rail;
ledger on `main`. **ZERO SCHEMA, ZERO SERVER FILES.** Zero-schema
merge pre-authorization; Fable reviews post-merge; deploys batch with
the P0 wave on Nick's word.
**Authority:** SV1, ratified ("P0 Go", 2026-07-24), plus sitting
findings V3 and V5 — one surface, one hygiene pass.

## S1 — the nudges sleep, whole

The WAITING FOR YOU section unrenders everywhere; the nudge-generation
engine sleeps entire — no computation, no injection, on any surface.
Data untouched; nothing deleted. The return is a later Tutor-panel
ticket under content law (no guilt-language, no counts, no repeats,
deduplicated) — record that gate on the ledger item so the sleep can't
quietly become the grave. A14's letters-frame survives as ratified
law; this retires its current implementation, not its principle — say
so in the park annotations.

## S2 — the beats sentence dies (V3)

The Structure lens's "Not linked to a beat." line is removed — the
beats system is dormant (CD4) and the lens may not speak a dead
language. "Loose — belongs nowhere yet" survives (true, home-derived).
The Thread arc will give Structure its true linked-language later;
until then, silence over falsehood.

## S3 — Fragments deduplicate (V5)

The Fragments lens lists each entry once — dedupe by entry id at the
source, not the render. The lens's own header rule ("recency and
shared tags only") is unchanged.

## Harness

Falsified checks (any asserting the nudges section's presence/content,
the beats line, or duplicate-tolerant fragments) get full A4 park
cycles, verbatim, supersession = SV1/this brief, live successors:
nudges-absent structural sweep (the cd4.mjs no-"Done" pattern —
zero WAITING FOR YOU anywhere), beats-line-absent, fragments-unique
(seed two same-project entries, assert single listing each). Both
`HARNESS_PARKED` settings; grep-first.

## Definition of done

The house is quiet. No surface volunteers guilt, counts, or the same
sentence twice; the Tutor speaks only living language; and the
writer's attention belongs to the page again.

— Fable, from SV1 + V3 + V5, for chat 1
