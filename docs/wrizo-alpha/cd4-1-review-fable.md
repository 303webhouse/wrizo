# CD4.1 — the Last Two Words · Fable's post-merge review · 2026-07-24

**Ticket:** CD4.1 (item 61), directed by the CD4 review. Built and
merged by chat 1, merge `f1be3dd`, records `7303b0f`; deploy held for
the batched word.
**Method:** the entire merge read — five files, 93+/7−, zero schema,
zero server files, zero new deps. The park-quoted original compared
against its deleted live line in the same diff: byte-identical.

## Verdict: GREEN. The last two completion verbs a writer could see are doors now. Close pends Nick's sitting alone (his eye on the two "Close" words).

## VERIFIED

**The two strings, exactly as directed.** The card-edit popup's close
button reads "Close" with class and behavior unchanged; Spread's toggle
reads Select/Close — enter the mode, leave the mode. Both carry the
ruling in-comment.

**The park discipline, all three modes applied correctly again.** The
fx4 original ("S5: Done closes the popup…") parked VERBATIM in the
house pok-record form with a probe that opens a fresh popup — carrying
the FX7 coordinate fix the build self-caught — and re-verifies the
current label directly; the live successor reads the label *before*
clicking and still proves close/un-blur/commit. `j5.mjs`'s
`click('Done')` was an ACTION, not an assertion — fixture maintenance,
updated in place, correctly distinguished. And the j4/ab4/ab1 records
that merely *mention* the old label were left frozen and unannotated —
they close by class, their probes pass, and they accurately record past
state: the load-bearing-claim vs passing-mention line, drawn the same
way CD4 drew it. Knowing when not to park, and when not even to
annotate, is the law matured.

**The standing guard.** `cd4.mjs`'s sweep asserts zero controls reading
exactly "Done" across every surface the word ever inhabited — the popup
open, Spread in select mode, a system board, prose, script — via a
selector that covers buttons, role=button, role=tab, and anchors. The
DoD ("no surface in Wrizo says Done") now has a regression tripwire.

## ADVISORIES — non-blocking

1. **Lexicon debt, inherited not created:** "Close" and "Select" are
   literals, as "Done" was before them — these pre-lexicon surfaces
   (the popup foot, the Spread toggle, the neighboring "Undo") migrate
   to `deskLexicon` together at next touch. Parity today; debt named.
2. The sweep's surface list extends opportunistically as future
   harnesses mount new surfaces — the guard grows with the app.
3. If any of the three frozen records is ever found to make a
   present-tense structural claim about Done, it gains the
   cross-reference annotation at that touch — none appears to today.

## Close condition

Nick's sitting — the two "Close" words under his eye; one string each
if either reads wrong. Then item 61 closes, item 59's last gate with
it, and the word "Done" exists nowhere a writer can see. The app now
agrees with its own thesis in every label it wears.

— Fable
