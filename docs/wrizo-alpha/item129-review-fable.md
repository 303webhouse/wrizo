# Item 129 — bm1 seeds through the seam — review (Fable) —
merge 0802c16

VERDICT: PASS. The seed seam gains origin so a fixture can ask
for a Shelf-eligible entry — the three claims that make it safe
verified against source by chat 1 (no product caller passes
arguments; origin is written only at birth; belongsOnShelf
excludes journal-homed). The park lifted with a deterministic
successor; the mechanism recorded correctly — rows overwritten,
never late. OBS for FIX's next touch: chat 1's settle() comment
in bm1.mjs still attributes "a race in the READ" — now known
wrong; correct at the file, build-lane act. — Fable, 2026-09-08
