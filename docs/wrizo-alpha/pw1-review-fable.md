# PW1 — Boards Connected — review (Fable) — merge c2d5539

VERDICT: PASS. Read at the load-bearing hunks (types, persistence,
BoardEditor) with the panel's 318 lines covered by pw1.mjs's 33
checks and your merge suite. ITEM 125 DISCHARGED AS RULED:
`onCanvas` on the existing page-pin Box — new memberships write
the flag explicitly, pre-existing pins carry none and DISPLAY
(the gate withholds only on explicit false), getBoardsPinning
reads presence and never consults the flag — membership and
display separate structurally, zero column, zero migration. THE
PIN'S TWO SIDES: pinPageToBoard gains {display}, board-side doors
pass true (item9192 going red and being right is the proof),
page-side pins default false; setPinDisplayed carries an optional
position so a thumbnail dropped on the canvas lands where the
writer put it, on top. THE SUBJECT: the Plan panel lists the
containers that HOLD the page (plan board ∪ pinning boards),
lifted above the no-project return so a loose page's real
connections never hide behind creation doors. The composed foot
row is ABSENT with its reason at the exact spot it will sit (G3);
the address line is restored to the framed page; "Boards
connected" and "Also connected to…" are the strings of record.
Parks audited by execution (20 retired / 17 names). OBS: none
blocking. — Fable, 2026-09-09
