# PB1 — post-merge review (Fable) — item 71

Reviewed at merge e8ae17d (branch tip af47582, suite 51/51 both
settings). Census: 19 files, +1016/−84, all apps/desktop; zero
schema, zero server, zero deps — pre-authorized class, correctly
applied. VERDICT: PASS. Item 71 GREEN (merged-but-undeployed;
rides the P2b SHA and must be named in its manifest).

THE SEVEN RE-POINTS (ab3, b1, cd2, fx6, fx14, hb1, hb2): lawful,
all seven. Subjects preserved; vehicles re-pointed with the change
disclosed at each site; fx14 and hb2 — the two whose assertion text
changed — quote their originals verbatim. fx14's accident-pass is
closed with a STRONGER regex (born-address form excludes query
strings); both now assert two stages, unborn then born. pb1.mjs
named live successor at every site.

pb1.mjs (397, new): pins the regression's SHAPE — door writes
nothing; first word writes everything with the word in it; durable
across reload; burst integrity across the birth boundary read from
disk, not DOM; whitespace is not a word; descriptor reload-safe
(ruling 1); board born on first box; PLAN→ fixed order, one board,
no orphan (ruling 2); screenplay births at zero words (the
amendment, quoted at its call site); affordance absences asserted;
no-sweeper proven on a seeded legacy empty, zero tombstones. The
footer quotes and owns its own falsified claim; waitForRowText
documents its own first bad run.

THE DEVIATION, RATIFIED: birth corrects the address with
history.replaceState, not navigate() — no hashchange, no route
change, no remount, no focus loss. Found by the burst check (a
58-char burst had persisted as "The "), ordered write-then-address
so a crash between them loses nothing, replace-not-push so Back
never returns to a walked-through door. Better than the reviewed
design; the remount class dies. This is what verify-don't-reason
is for.

THE SLOT (persistence.ts): outside `cache`, therefore never
serialized, synced, or enumerated — absence by construction, the
flush/flushNow/upsert mechanism named in the comment. A real row
always wins the fall-through; no ordering hazard. Both false
honor-discard comments REPLACED, originals quoted; createJournalPage
now carries door directions for future authors.

ONE OBSERVATION (OBS-1, non-blocking): unborn Screenplay birth is
asserted at the STORE, not the SURFACE — no check that ScriptEditor
mounts without a reload (the unborn tree deliberately never
remounts; the born-world dispatch lives in PageEditor()). Ten-second
check at the next hardware sitting: new page → Screenplay → is the
room Courier? If it does not flip live: polish defect, not data —
the row is correct and a reload lands it.

— Fable, 2026-07-30
