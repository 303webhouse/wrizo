# CD4 — the Two Retirements · Fable's post-merge review · 2026-07-24

**Ticket:** CD4 (item 59). Built and merged by chat 1 (reassigned owner,
recorded), merge `4777d19`, records `75ee997`; deploy held for the
batched word.
**Method:** house depth — the entire merge read line-by-line: all nine
files (three components, six harnesses, including every line of the new
253-line `cd4.mjs`), 409+/32−, zero schema, zero server files, zero new
deps, `deskLexicon` untouched (the `plan` term survives for the door and
for th1's direct check). Every park-quoted original compared against its
deleted live text in the same diff — byte-identical, all of them.

## Verdict: GREEN. Both retirements landed at their sources, the ruling implemented exactly as amended, and the park sweep is the most intricate yet executed — lawful in every mode it needed. Close pends CD4.1 (directed below) + Nick's sitting.

## VERIFIED

**S1 — Done dies; the door is every Board's exit.** Both render paths'
Done buttons removed and commented; the mode strip now mounts for EVERY
board with the tabs correctly staying non-system and the
`role=tablist`/`aria-label` made conditional — a lone door is no
tablist, an accessibility nicety the ruling didn't even ask for. The
ruling's core claim holds in the code exactly as derived: **no new
navigation logic exists** — `travelToPage`'s pre-existing unpaired
branch is the system board's exit, `backTo` already `'/'`, the cold
load landing at Arrival, itself a page. The harness proves the door on
all three system boards plus a user board, label byte-exact `"Page →"`,
at the floor, the reference, the wide, and legacy widths — and walks
all three door paths under genuine trusted pointer: paired → the paired
page, unpaired → the FX10 named return, system cold-load → `'/'`.

**S2 — the elder Plan retires.** Both PageEditor paths and ScriptEditor
lose the legacy flight tab; the bar reads `['Pages','Plan →']` prose
and `['Pages']` script, proven at three widths plus legacy — with the
legacy path's pre-existing "Copy page text" difference documented and
asserted rather than hidden. The structural checks are the sharp kind:
the toggle holds exactly ONE button, and zero bare-"Plan" words exist
in the bar. Dormant-not-dead is honored to the letter: route,
component, `beat_id`, `story_plan_id` all untouched.

**The park sweep — three lawful modes, correctly distinguished.**
(1) Full A4 cycles with probes: `b2.mjs`'s two Shelf-Done checks and
`cd1.mjs`'s bar generations — where `cd1.mjs` now carries a
**four-generation chain** (original → CD3 → BM1 → CD4) with every
generation's text parked verbatim in its own fresh entry and every
probe following reality under the codicil, in the same commit as the
removals. The file has become the immutability law's own showcase.
(2) A comment-form park in `th1.mjs`: the original quoted verbatim in
a non-executing comment, the successor verifying the lexicon term
directly — the check's real intent preserved while its DOM subject
retired. Lawful as a third form where a file has no parked section;
the pok()-record form remains the house preference where one exists.
Honestly disclosed as caught by the full-suite re-run, not the grep
sweep — the sweep's limit named instead of papered over.
(3) Cross-reference disclosures in `hb1.mjs`/`bm1.mjs`: frozen texts
that *mention* Done in still-true checks annotated with pointers, no
assertion falsified, no park manufactured. Knowing when NOT to park is
part of the law too.

## RULINGS on the two disclosed residuals

**(a) StructureBoard's secondary access — ENDORSED as correctly
scoped.** CD4's mandate was the bar's duplicate word; ProjectHome's
"View board", BeatWizard's redirect, and QuickSprint's toggle are the
legacy system's remaining doors, and their sentencing belongs to the
Thread committee (retire with W1, or earlier — its call). Recorded as
an explicit Thread-committee agenda item; the arc seed inherits it.

**(b) The two transient "Done" labels — RULED: they die. CD4.1 — the
Last Two Words — is directed to chat 1:** Nick's word was categorical
and the DoD says no surface says Done. The card-edit popup's close
button becomes **"Close"**; Spread's select-mode exit toggle becomes
**"Close"** (the pair reads Select/Close — enter the mode, leave the
mode; a door word, never a completion word). Micro-scope: two strings,
the fx4 `board-popup-done` check and any Spread label assertions get
their park cycles in the same commit, `cd4.mjs` gains a
no-"Done"-anywhere structural sweep as the standing guard. Zero
schema; rides the batched deploy. If either word reads wrong under
Nick's hand, it's one string at the sitting.

## Close condition

CD4.1 merged; Nick's sitting — his hand through all three system-board
doors, and his eye on the bar that finally holds one Plan. Then item 59
closes, and the app contains no completion verb anywhere a writer can
see.

— Fable
