# HB1 review addendum — fold-delta spot-check + merge verification · Fable · 2026-07-17

Companion to `hb1-review-fable.md` (left byte-stable; its blob `c6ddbd5` is now a
verified reference). This is the F-3 record CC correctly flagged as missing from
disk at merge time — the spot-check happened in-session before Nick's merge word;
this paper is late, not skipped. Chat-only = lost; hence this file.

## Fold-delta verdict: GREEN (pre-merge, now of record)

All four hb1.1 commits verified on origin by SHA (`f568519`, `2a606d2`, `b732d32`,
`142c660`). The review doc landed byte-identical (blob fingerprints matched,
`c6ddbd5`, 5,949 bytes). **F-1** — the ceremony's focus trap is real: focus to the
first offered territory on mount, Tab contained both wrap directions, all three
behaviors pinned in hb1.mjs (28 → 31). One transient edge (both buttons disable
during the chosen-flash, freeing Tab ~500ms over an otherwise-inert page) judged
harmless, on record here. **F-2** — ledger item 27 carries the verdict, all five
rulings, the mercy valve closed as *decided*, the stash instruction honored.

## F-3 findings

1. **The parked HomeFlow is prior art of the rite itself** ("HOME port v6"): a
   fully built earlier threshold — 50-word gate on the forward-only surface,
   progress-coupled ambient, bloom reward, account-creation keep. The park is
   exemplary (honest header, verbatim-relocation of sign-in documented). Two
   things v6 knew that HB1's rite doesn't, handed to the sitting as open design
   questions, hb1.2-sized if wanted: **the echo** (v6's reward showed the writer
   their own words back — "you wrote this just now…" — where HB1's ceremony
   announces only the theme) and **the mercy** (v6 idle-nudged a stuck newcomer;
   HB1's gate is silent by the review's own "speaks once" ruling — a real
   tension, Nick's to resolve with the device in hand).
2. **s1.mjs's DORMANT park is A4 discipline done right** — original assertion
   preserved in comment, narrower surviving truth re-asserted behind
   `HARNESS_PARKED=1`, and an honest flag that no successor proves the old
   unprompted-glance truth. That flag is a live open call for the sitting:
   **the glance** — does Arrival stay austere (mark, bar, doors), or offer a
   quiet unprompted trace of where you left off?
3. **F5 correction, against the brief's own claim:** the paste seam is likely
   already closed. The Voice Wall (s1.mjs DoD7) blocks foreign paste and admits
   own-ink back on the script surface, and the forward-only surface has carried
   a paste block since the v6 era. One Ctrl+V at the gate during the sitting
   confirms; the ledger's F5 note then takes a one-line correction at the next
   docs touch.
4. **Disclosed partial:** Desk.tsx's parked-shim prose accepted at census level
   (unrouted per App.tsx; sibling park by the same hand exemplary), not
   eyeballed. To be read at the next touch of that file.

## Merge verification (HB1)

Nick's merge word is on the record in `7e7d7f4`'s own message. Fast-forward
`df88ff5 → 7e7d7f4` verified; the cap commit is docs-only (+24 ledger/backlog),
so main received exactly the branch state the green suite ran on, plus paper.
The pre-ff reconciliation's one conflict (item 27 double-claim) was docs-only,
resolved by chronology — HB1 keeps 27, FX2 becomes 28.

## Post-merge landscape (for orientation, not re-ruled here)

FX2 (born from Nick's CD1 device sitting: the sliver grip, Draft's typewriter
threshold) merged into main after HB1: `ee69bbc` (merge, one import-block
conflict in PageEditor.tsx kept-both) → `25644ea` (fx2.mjs fixtures adapted to
HB1's world) → `2b866c80` (docs-only cap, verified +44). Main's code state ==
`25644ea`, where the full 15-script suite ran green both HARNESS_PARKED
settings — hb1.mjs 31/31 among them, which is HB1's own health on blended main.
**FX2's post-merge review belongs to the origin chat's Fable by lineage** and is
still owed per the ledger; not annexed here. The HB1 device sitting therefore
happens on a main carrying both tickets — Nick can feel his two FX2 findings in
the same sitting, with verdicts routed to their own tracks.

## Standing

Deploy waits on the sitting and Nick's word. The `cd1.1 erratum WIP` stash stays
held until CD1's origin-chat close conditions clear. Sitting list = the review
doc's felt checks + the echo, the mercy, the glance, the Ctrl+V, and one poke at
any in-app "Done" that still targets `/`.

— Fable, for the record · 2026-07-17
