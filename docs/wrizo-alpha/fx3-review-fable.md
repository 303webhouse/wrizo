# FX3 — the Proportions · Fable's post-merge review · 2026-07-17

**Reviewed:** merge `f87295e` (census: 16 files, exactly the named
surfaces — fx3.mjs new at 405 lines, park-sweep touches across
ab1/ab2/fx1/fx2/hb1, code confined to Sliver, useTypewriterFade,
ModeStage, index.css, PageEditor, ScriptEditor, GoalGlow, deskLexicon,
writingSettings, and the new writingGoalUnit store — localStorage-
shaped, zero schema surface) plus the ledger record `5340398` read
whole. Depth per the FX2 precedent: census + record, with the
independent review's algebraic confirmation of the S2 measure
invariant standing as the deep verification on the riskiest claim.

## Verdict

**GREEN.** The redeploy gate is open on Nick's word. Real bugs were
found and fixed in-flight (scroll-offset unit mismatch, sliver-anchor
scaling, spurious scrollbar); S1's height-fill holds with zero drift
at both reference widths.

## Rulings of record

1. **S4's JournalEntry divergence is tolerable as a TRIAL only.**
   BoardEditor has no modes to align — fine forever. JournalEntry's
   divergence leans on CD1's precedent, which covered its right
   cluster's CONTENTS, not the strip's alignment. Condition: **if
   Nick ratifies right-alignment, it unifies across every host,
   JournalEntry included.**
2. **The veil race routes to the HB session.** The hb1.mjs settle
   wait was the correct harness fix, but the app-side truth remains:
   the first-run veil sometimes races first paint. A new writer
   glimpsing the unveiled app for a flash would undercut the
   threshold's whole effect. Named carry for the HB chat's Fable —
   the veil should mount before first paint or render synchronously.
3. The reviewer-worktree failure was tooling, not task; the
   one-checkout law held; CC's takeover and the hb1 root-cause
   (real first-mount sensitivity, 19/19 clean after) were exemplary.

## Close conditions for ledger item 29

1. This review on disk (this file). 2. Redeploy on Nick's word.
3. Nick's sitting — FX3's proportions, the typewriter start, and the
right-align trial verdict can ride the CD2 sitting or come sooner.

— Fable, 2026-07-17
