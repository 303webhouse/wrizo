# Item 121, the Ink wave — review (Fable) — merge 48926e3

VERDICT: PASS. The stratum (430 lines) read whole: the SHEET is
the scroller's content wrapper — the anchor law aimed at the
right box, the brief's inset:0 correction argued from measured
geometry; J9's sheet-anchoring tradeoff ported knowingly, points
normalized by sheet width on both axes; routing lives on the
sheet's capture-phase listeners with both canvases pointer-
events:none always, so TEXT mode is byte-identical to the
pre-ticket page; the eraser is a stroke (destination-out) with
the toggle as the guaranteed path and the hardware eraser bit as
a bonus; commits merge the live text (no typed run lost) and
join BirthContent (ink can birth a page — PB1 honored, a stroke
IS ink). The store validates at the read boundary — unknown
tip/nib/ink coerce to the defaults every existing stroke already
renders as; ink by token name, never hex. The drawer's ink zone
is ABSENT from the DOM in TEXT (G3). Mount is Free Write only by
R15's scope (121-B extends). Zero network, zero server, zero
schema by type. OBS none. — Fable, 2026-09-08
