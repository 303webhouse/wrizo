# FX14 — One Page · Fable's post-merge review · 2026-07-25

**Ticket:** FX14 (item 65), P0. Built and merged by chat 1 (after its
own caught-and-unwound out-of-order local merge — origin never
touched), merge `a348027`, records `a2ec42a`; deploy held for the P0
word.
**Method, disclosed precisely:** 25 files, 774+/851−, zero schema,
zero server files. Read WHOLE: all six product files (the one-line
`routeForEntry` law, App.tsx's `JournalIdRedirect`, the five creation
doors) and all 152 lines of `fx14.mjs`. The 19-harness sweep sampled
at each split category's HEAVIEST instance, read whole: `ab2.mjs`
(the ten-check whole-section park + two multi-generational probe
retirements), `w2.mjs` (three whole-section parks + the
second-generation FX4-chain retirement), `j5.mjs` (the heaviest
re-point). Remaining sweep files verified at diff-scan depth, backed
by the parked-records audit running clean post-merge and the 42/42
both-settings suite. `hb1.mjs` taken at record depth, disclosed.
Park-quoted originals spot-verified byte-identical against deleted
lines in the same diff throughout.

## Verdict: GREEN. There is one place where writing happens, every door leads to it, and the largest park sweep in the house's history executed three lawful modes without one record harmed. Close pends Nick's sitting; the P0 deploy word is unblocked.

## VERIFIED

**S2 — the law is one line, and J6 paid for it.** `routeForEntry`
returns `/page/:id` unconditionally — the exact one-line change the J6
seam was built to make cheap, its annotation quoting SV6 and honoring
the census pointer. `/journal/:id` is a permanent `replace` redirect
via the bridge-shaped `JournalIdRedirect` (the system-board gates' own
pattern); the JournalEntry surface is unrouted AND unimported —
tree-shaken from the bundle — with the file deliberately left in place
as J7's inheritance. Old links, resume paths, and muscle memory all
land right, proven for all three entry shapes the old function
branched between.

**S1 — every creation door, found and turned.** The grep sweep's five
sites (Cascade's two doors, DrawersTree, Catch, Spread's fallback) all
land on THE Page, each annotated; origin semantics preserved
structurally — the Journal door still stamps `origin:'journal'`,
untyped, proven in `fx14.mjs` down to the persisted entry. Spread's
fallback branch unified in the same breath.

**S3 — the Journal board untouched**, as briefed.

**The sweep — three lawful modes, and the third is hereby RATIFIED as
law: the vehicle/subject distinction.** (1) PARKED as falsified where
the retired surface was the check's SUBJECT — whole sections with
originals quoted byte-for-byte INCLUDING their nested prior
`(was …)` records, successors mapped precisely (universal claims to
live twins on THE Page; JournalEntry-specific behavior to J7's
census). (2) FIXTURE RE-POINTED where the journal page was merely the
VEHICLE — the subject untouched, the seed reproducing the identical
persisted state (same shape, same stroke form, STEPPED timestamps
preserving lens determinism), seeded from the Desk per the flush-race
law, hydration reloads added where the old path was cache-live: ~50
checks of real coverage preserved instead of discarded. (3) ROUTE
UPDATED where the route itself was the assertion (hb1's resume). The
multi-generational chain-keeping reached a new high-water mark: w2's
retired probe annotates that its own record's named successor is
itself now parked — the chain stays legible even when it folds back
on itself. Probes retired to the documented-supersession form with
dead navigation REMOVED, so armed runs cannot hang on an unrouted
surface.

## ADVISORIES — non-blocking

1. **The comment-form parks (~30 originals) are invisible to DF1's
   audit tracer** — carried as DF1.1 advisory 2 in its own review;
   until taught, the audit's "clean" statement covers pok-form records
   only.
2. **The parks' successor pointers ARE J7's work order:** roughly
   fifteen JournalEntry behavioral claims (window-scroll way-back, the
   pager, the entry-view Add-to door, ink layer, metadata absence,
   undo generosity, and kin) now name J7's behavior-parity census as
   successor. J7's brief inherits that enumerated list — settings or
   death per SV6, the census's own undo finding honored.
3. The suite's one non-green (tu2 1/96) is ruled in DF1's review —
   the rescission — and is provably not an FX14 regression.
4. `hb1.mjs` at record depth, disclosed above; its route update is
   re-proven live by `fx14.mjs`'s redirect checks regardless.

## Close condition

Nick's sitting — New Page under his own hand landing on THE Page,
an old journal link redirecting right — and the wipe erases the
Untitled detritus the old doors bred. **With this review, all three
P0 tickets and the rider are merged and reviewed: the deploy word is
unblocked.**

— Fable
