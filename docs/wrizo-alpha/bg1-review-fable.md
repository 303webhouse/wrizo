# BG1 — post-merge review (Fable) — item 67
Reviewed at merge 5d5ae5e. Census: 6 files, +855/−17, zero
schema/server/deps. VERDICT: PASS. Item 67 REVIEWED — GREEN.
One component, three placements, and anti-gamification BY
CONSTRUCTION — "the component takes labels and handlers, and has
nowhere to put a badge"; the language-clean check asserts it
structurally against the rendered text. Sprout's rail is proven the
hard way: fetch, XHR, and WebSocket all wrapped, ZERO outbound
requests on a draw. The vanish rule deliberately lives with the two
callers that can see emptiness. Esc dismisses the page's row only,
with the reasoning (the board's row IS the empty canvas; dismissing
it would recreate the dead end). System boards absent-whole; the
Shelf's declarative line untouched; New Lane reachable from an
empty storyboard for the first time ("a lane is not furniture").
fx6.mjs's park + in-place re-point apply the vehicle/subject
distinction exactly. The one merge conflict (useFirstLineInvite)
was resolved to FX15's version with the reasoning disclosed and
fx15.mjs re-run on the combined tree. READ SCOPE: bg1.mjs, the fx6
park, and BeginningsRow at full patch; residual wiring verified
through asserted consequences. LINEAGE: SV19/SV20 (size and
legibility) were new sitting verdicts, shipped as BG2, reviewed.
