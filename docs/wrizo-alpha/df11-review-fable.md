# DF1.1 — post-merge review (Fable) — item 66
Reviewed at merge e2b6945 (DoD 30fc2ca, six sweeps). Census: 7
files, +660/−50; package.json delta is SCRIPTS ONLY — the zero-deps
claim verified. VERDICT: PASS. Item 66 REVIEWED — GREEN.
The instrument holds under review. run-suite.mjs encodes its three
laws with their origin stories; the mid-run foreign-browser abort
("a sweep cannot be half-clean"), the CONTAMINATED stamp, verdict-
matching by SHAPE (the fx7 case — the runner's own reds-on-known-
benign disease, caught by the DoD's first sweep and named as such),
and NOVERDICT-never-passes put the house laws in executable form.
clear-before-launch is the complete root cause of "CDP page target
never appeared" (PID-recycled stale profiles; fails pre-app-load so
it voids but never fakes) — and deliberately NOT a TEMP sweep, the
provenance rule again. j5's three species each carry mechanism +
fix + the falsified first claim owned on the record ("≥4 always
holds" was false; the narrower local truth is better). tu2's
recorder proves the SCHEDULE on the browser's clock — original
check names preserved verbatim, two strictly stronger checks added.
selftest-quiescence extracts the predicate FROM j5.mjs (drift
structurally impossible), proves both directions plus recovery, and
sits outside the harness glob so the DoD composition is untouched
by its own existence. The audit's coverage statement is now honest
arithmetic with its counting rule disclosed — an owned
approximation over false precision — and DF1's four hand-ruled
edges are handled AND kept written down so the reasoning survives
the tool learning to ignore them. NOTE (designed scope, not
defect): the self-test runs by discipline, not automation — it is
deliberately outside the suite; if the predicate ever moves it
fails loudly and demands re-pointing.
The 47-file DoD arithmetic verified across the wave: 47 → +fx16/
fx18/bg2 = 50 (P2a) → +pb1 = 51 → +fx17 = 52. Every ledger claim
consistent.
