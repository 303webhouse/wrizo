# The fix wave (E4 + E3 + ab2 re-point + 118a-ii) — review
(Fable) — merge a20b51f

VERDICT: PASS. Four product changes, read at the bytes:

E4: `|| unborn` removed at the one line that gated the right hand
on pages — with the existence proof cited in place (three other
mount sites unconditional, unborn boards included) and the
identity claim VERIFIED not read (the minted per-surface id
stamped on the very row the old defect wrote; MISSING_ENTRY
cannot reach the line — the dispatcher returns one level up).
The coupled half: send() now REFUSES OUT LOUD on an unborn
surface and the composer is NOT cleared — "the writer's sentence
survives the refusal and sends itself the moment there is a page
to attach it to." PB1 held as standing law rather than overturned
for a convenience; the measured premature-birth row is quoted in
the comment. gateActive stays with both recorded reasons.

E3: the `{open && …}` wrapper removed — the body now exists for
its own fade; the mirror was exact everywhere but that line, so
the repair is a mount change with zero timing copied, the
frame-sampled evidence (content:false at opacity 1.00) in place.

118(a-ii): the resting card renders through the POPUP'S OWN
decoration engine with a null caret — no new renderer — markers
collapse via font-size:0, NEVER display/visibility, so
textContent still carries the stored text byte-for-byte
(draftDecoration's stripping hazard honored); escHtml bounds the
innerHTML; the one-directional auto-size is ASSERTED in
item118.mjs rather than left as reasoning.

The PB1 park is the offer's centerpiece done right: the red
recorded beside the green as the finding, the two-claim
distinction kept (absence superseded by ruling; wrote-nothing
surviving with MORE weight), the header's self-contradicting
verdict string corrected in the same commit.

— Fable, 2026-09-02
