REAPER: 0 harness browser(s), 0 owner(s) —  | floor=300s | targets=0
REAPER: nothing to reap.
PASS  M1: the CSS Custom Highlight API is present in the app's own browser — `CSS.highlights` registry and a constructible `Highlight`. This is the ONLY mechanism that paints a range without putting anything in the DOM, which is what §6b requires of the mark  [{"highlights":true,"ctor":true,"registry":"HighlightRegistry","ua":"Edg/153.0.0.0"}]
PASS  M1 (the premise, CORRECTED BY MEASUREMENT): the editor's text is reachable as TEXT NODES a Range can span. The first version of this fixture looked for one `.fo-word` per word and found ONE — the settled text is a single `.fo-run`, and `.fo-word` is only the word being typed. The feature must locate a word by OFFSET, not by span class, and so does this measurement  [{"textNodes":2,"text":"The rain fell on the quiet street and nothing moved at all","runs":1,"liveWords":1}]
PASS  M2 (premise): the target word has a real on-screen rect to sample  [{"left":461,"top":292,"width":35,"height":19}]
PASS  M2: a Range over the word registers as a Highlight (`CSS.highlights.set`) with no DOM mutation in the editor — no element inserted, no character added  [{"ok":true,"registered":true}]
PASS  M2 ⛔ THE DECIDING CHECK — the word's pixels actually CHANGE when the highlight is registered. A highlight that registers and paints nothing would pass every API check and fail the writer, so the tint is measured in pixels and not inferred  [{"before":{"r":210,"g":204,"b":192,"px":665},"after":{"r":163,"g":148,"b":182,"px":665},"meanChannelDelta":113}]
PASS  M2 (the control): a DIFFERENT word on the same page is unchanged across the same two frames — so the delta above is the highlight and not a repaint of the whole surface  [{"ctrlRect":{"left":500,"top":292,"width":39,"height":19},"ctrlDelta":0}]
PASS  M3 ⛔ §6b — THE MARK PUTS NO CHARACTER IN THE TEXT. The stored manuscript contains exactly the writer's sentence: no marker glyph, no sentinel. This is the rule the drawing's `✎` broke, and the reason the Custom Highlight API is the mechanism at all  [{"stored":{"text":"The rain fell on the quiet street and nothing moved at all","hasPageLinks":false},"hasPencil":false}]
PASS  M3: and the highlight adds NO element to the editor's subtree — the tint is painted by the browser, not by wrapping the words in anything  [{"nodeCount":2}]
PASS  M4: PAGE IS PRIMARY — painting the mark does not change the editor's bounding rect, and does not reflow the word it marks. Self-check line 1 answered NO, measured rather than reasoned  [{"editor":{"left":331,"top":288,"width":537,"height":29},"word":{"left":461,"top":292,"width":35,"height":19},"wordBefore":{"left":461,"top":292,"width":35,"height":19}}]
PASS  M5: TWO highlights may cover the same words at once and both stay registered — EXP1-Q5 is YES (Nick's ruling: a writer may link a phrase inside an already-linked sentence, and the rail lists everything covering a spot), so the paint layer has to allow overlap rather than replace  [{"ok":true,"registered":true,"size":2,"both":true}]
PASS  M6 (the finding, not a pass/fail of the product): after an ordinary edit the registry still HOLDS the highlight — recorded so the build knows whether re-registration is needed for the registry or only for the ranges  [{"stillRegistered":true}]
PASS  M6: and whether the tint is still PAINTED after the editor rebuilds its spans — measured. delta-from-unpainted=0 (>12 means still painted, <=12 means the range detached and the mark must be recomputed on every render)  [{"deltaFromUnpainted":0,"afterTypeRect":{"left":461,"top":292,"width":35,"height":19}}]

EXP1 PAINT MEASUREMENT: PASS (12 checks)
