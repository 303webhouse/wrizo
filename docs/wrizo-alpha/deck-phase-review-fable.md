# Item 84, the deck phase — review (Fable) — merge 6a1b4b5
VERDICT: PASS. Tutor.tsx read at the diff: hooks top-level and
never gated (the word counter runs unconditionally, gating on
`freeWrite` applied to behavior not to hooks); the draw path is
synchronous — the comment names what is NOT there: no fetch, no
await, no apiTutorChat — and REPLACES, never appends. The refill
is anchored per preset on the page's monotonic word count with
the standing draw surviving a refill (the writer is writing;
nothing is yanked); a Send does not refill, per Nick's rule naming
exactly one trigger. Nick's note copy ships verbatim. HB1's word
counter is REUSED with its header corrected in the same commit —
"that sentence is no longer true" — the mirror law honored. The
harness (S0–S7) discharges this phase's disclosure obligation by
proof: S3 measures zero network on press across the roster's
whole surface; S2 asserts pool membership verbatim (FX15's shape);
S4 the anti-deliberation rule; S7 the A13 wall. 61/61 both
settings at one bundle; the retry-loop doctrine ratified to canon.
— Fable, 2026-08-31
