# Fable session handoff — the Board Modes / Listener day · 2026-07-22

You are Fable. This document hands off one of the densest sessions in
the project's history: the Tutor gained ears and a seat, the Board
gained its own modes and a pairing schema, the export lifeline was
built, four laws were ratified, and Nick leaves for vacation
**2026-08-04** intending to draft his own book in this app. Your first
duty is the disk, not this document — but this document tells you where
today's truth landed.

## Boot order

1. `docs/wrizo-alpha/fable-session-handoff-v2.md` — house laws (still
   authoritative for everything not amended below).
2. `docs/open-threads.md` — the ledger. Items 41–55+ are today's.
3. This document.
4. The reviews and passes of 2026-07-21/22, all in `docs/wrizo-alpha/`:
   `tu2-review-fable.md`, `fx7-review-fable.md`, `fx8-review-fable.md`,
   `m2-review-fable.md`, the Board Modes second pass, and the briefs
   for E1 / FX10 (revised) / J6 / FX9 / BM1 / M2.

## What deployed (Nick's word, end of session)

**Everything merged went out in one deploy:** J6 (routing substrate +
parity census), FX9 (folded lists), E1 (Get My Words Out — the export
lifeline), FX10 (the Tutor drawer + vanishing fixes), CD3 (the strip's
order + Done deprecation), and BM1 (the Board's Own Modes — schema:
`plan_board_id`, merged on Nick's explicit word after Fable's GREEN
line-by-line schema review, 23/23/23 hand-verified). TU2, FX7, FX8, M2
were already live.

## YOUR FIRST DUTY — four owed post-merge reviews

**J6, FX9, E1, and FX10 were merged under zero-schema pre-authorization
and deployed on the B3 precedent WITHOUT Fable's post-merge review.**
This was disclosed to Nick and he chose speed knowingly. The reviews
are owed, not waived. Run them at the house depth (`list_commits` →
stats census → full_patch on risk centers). Risk centers to hit:
- **E1:** the export completeness claim — no writer word may be missing
  from an "Everything" export; the offline proof; what S1's diagnosis
  found on the Publish buttons.
- **FX10:** the drawer geometry vs. TU2's parked checks (A4 chains);
  the one-vanishing-engine invariant; the trusted-pointer hover proof.
- **J6:** `routeForEntry` behavioral identity (S2 changed ZERO
  behavior by design — any routing diff is a defect); the parity
  census document's existence and honesty (it gates J7).
- **FX9:** the no-counts law on folded headers; id-keyed persistence.
Also verify BM1's final Done-retirement commit landed with its
fallback-exit proof (see rulings below), and check the b2/hb1
Done-check successors' A4 chains.

## The laws ratified today (all on the ledger; enforce them)

1. **Parked entries are immutable — with the record/probe precision.**
   The frozen record (name, quote, tested condition AS HISTORICALLY
   ASSERTED) is untouchable: superseded again, re-pointed, annotated —
   never rewritten, no incidental exemption (a count bump IS a
   mutation; B1's pre-law bump was ruled a violation, remediated). The
   LIVE RE-VERIFICATION PROBE attached to a parked entry may update in
   place, disclosed in its commit — it is an instrument, not history.
   The distinction is structural (the record is the quoted non-executing
   text; the probe is what runs); nothing may be relabeled to dodge it.
2. **A stalled or placeholder report is a report that does not exist.**
   No close condition is satisfied by an unwritten report; no agent
   records a verification whose output it did not read. Merging past a
   stalled review requires naming the gap in the ledger + compensating
   verification, disclosed.
3. **Workflow sub-agents never drive a full harness suite.** They
   background it, their turn ends, a placeholder is captured — three
   occurrences proved it structural. Long-suite verification belongs to
   the MAIN LOOP, synchronous, verdict as the final message, or in
   small synchronous batches.
4. **The S0-push rule.** Records commits are authored in the ticket's
   own worktree and land on `main` by fast-forward push; the primary
   checkout is merge-only, serialized; `git status` clean before any
   merge or verification sweep. One worktree per session, INCLUDING
   quick cosmetic passes — the eleven-file uncommitted collision was
   today's proof.
5. **A ticket number is claimed by its ledger item**, never by a
   review's forward reference (the FX8→FX9 rename).

## Rulings of record (Nick's words and Fable's, today)

- **"Done" is dead everywhere** — a completion verb in an app whose
  thesis is that writing is never done. The Board's Done was interim
  (its only exit) until BM1's PAGE → door; its retirement commit was
  conditioned on the unpaired cold-load fallback proving out under a
  genuine trusted-pointer click. VERIFY it landed.
- **Board Modes:** OPEN / STORYBOARD / OUTLINE + the PAGE → door
  (arrow-dressed, never selected — doors are doors, modes are modes);
  the page's mirror PLAN → door; the telos line "The plan serves the
  page."; **decks are data, modes are projections** (constitutional —
  one deck library, N renderers, per-mode forks are STOP conditions);
  ordering single-sourced on shared box data; pairing lazy-born, never
  nagging, no badges ever. Commonplace was scrapped (linking absorbed
  into OPEN via the existing 'connection' box). S1 chose (b): the
  StoryPlan fold DEFERRED — Nick's T4 authorization stands unexercised;
  "the StoryPlan fold" is its own future ticket.
- **The two-Plans collision:** the old beats control and the new
  PLAN → door shared the page bar. Nick was asked for one word
  (recommendation "Beats") — CONFIRM what he chose and that the
  lexicon string shipped.
- **The Tutor:** DeepSeek V4 Flash first-party is the ruled seat
  (Nick's deliberate eyes-open choice, key funded); rules 1–36 + the
  five-layer memory architecture are TENTATIVELY ratified as a living
  document — hard gates that stay hard regardless: the TU5 schema go
  at merge, and the disclosure wording (mechanically true always).
  Rule 37 (the delta is context, not an assignment) is live in the
  system prompt.
- **The rhizome (M2):** Nick's verdicts — too dark, too confined; he
  wants it roaming the whole ground, filled by essay length. That is
  **M3 — the Rhizome Roams** (unbriefed). "Start Here" ruled brass
  (item 40 closed). The framed desk has NO progress row (FX1 S5) —
  whether it should is a parked question Nick hasn't answered.
- **The 10px paper rise** (CD3's border cleanup) — accepted as
  intended, pinned as a bounded delta, awaiting his eye.
- **StructureWizard:** superseded-not-killed — lives untouched until
  **W1 — the Deck Wizard** (guided elimination → a populated Board with
  pre-filled cards and a start-here prompt) replaces it. Post-vacation.

## The queue (as Fable left it)

**Before Aug 4, in order:** (1) your four owed reviews + BM1
Done-commit verification; (2) **Nick's device sitting** on the mega
deploy — E1's export ("Everything," wifi off) and FX10's drawer are
items one and two; the sitting agenda is
`device-sitting-agenda` (delivered as an artifact — commit it if it
never landed on disk); (3) **the offline test — STILL UNRUN**, the
last survival check; (4) **TU5 — the Tutor's memory** (confirmed as
THE pre-vacation Tutor ticket over TU4; needs its brief; schema;
disclosure v3 required the moment bible facts travel); (5) **M3** and
**FX11 — the Board's Hands** (FX8's `isDragging` cleanup leak +
Nick's resize-then-can't-move glitch), both small; (6) **feature
freeze Aug 1** — fixes only after.

**Post-vacation, decided and waiting:** BM2 (side-by-side — a real
layout ticket: two live surfaces, FX2 clearance, the Tutor's room);
the StoryPlan fold; J7 (JournalEntry retirement, briefed FROM the J6
parity census); TU4 (mechanics lens + ~40 lesson cards — gated on T4:
Nick never answered inline-vs-margin); TU3 (spend ledger); TU6
(BYO accounts — encrypted key custody, per-provider disclosure); W1;
the deflake pass + parked-entry history audit (item 48 + rider);
board zoom (ambient, vector-crisp, all modes); the Board-rail
question (the Board is the only surface with no navigation chrome —
deliberate or gap, ticket-sized); menus/toolbars rethink under the
new architecture (Nick deferred as non-loadbearing).

## Standing context you'd otherwise have to rediscover

- The write grant may still be 403 — the relay: Fable produces files
  as artifacts, Nick pastes to CC verbatim. Docs-only protocol when it
  clears.
- Multiple CC sessions run concurrently, on different models, and
  relays cross — ALWAYS `list_commits` before responding to any
  report; every stale report today was caught that way. Ask "how many
  sessions are live and does each hold its own worktree" whenever the
  ground feels soft.
- The DeepSeek key was rotated after being printed into a transcript
  (verify presence, never value — standing practice). `TUTOR_BASE_URL`
  and `TUTOR_MODEL` deliberately unset on Railway (code defaults are
  the single source of truth).
- The known flakes: fx5.mjs, th2.mjs, j4.mjs — all suite-context/
  contention class; re-run in isolation before calling anything real.
- Nick's decision style: decisive recommendations, never option
  menus; judgment calls named and vetoable; corrections on the
  record; plain language in chat; the project's own register is the
  native one. He tentatively approves fluid things (the Tutor rules)
  to keep tempo — record tentative as tentative.
- The Tutor's constitution is the product's soul: the AI that never
  writes anyone's book. Every ticket you brief adds to that refusal,
  never around it.

Whatever you find on the disk that contradicts this document, the disk
wins. That is the whole law.

— Fable, for Fable, end of the Board Modes day, 2026-07-22
