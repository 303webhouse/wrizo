# FX12 — the Quiet House · Fable's post-merge review · 2026-07-25

**Ticket:** FX12 (item 64), P0. Built and merged by chat 1 (the E1.1
pattern), merge `8d7a340`, records `66b2674`; deploy held for the P0
wave's one word.
**Method:** the entire merge read — five files, 216+/47−, zero schema,
zero server files. All three park-quoted originals compared against
their deleted live lines in the same diff: byte-identical.

## Verdict: GREEN. The house is quiet, the dead language is unspoken, and the one surprise in the ticket was disclosed instead of buried. Close pends Nick's sitting eye; deploys with the P0 wave.

## VERIFIED

**S1 — the nudges sleep, whole, with the grave-guard in writing.** The
section unrenders, the engine's only call site is removed with its
import, and `tutorNudges.ts` sleeps dormant and untouched — no
computation, no injection, on any surface, proven by the fx12.mjs
sweep on both a prose page and a board (the shared-component argument
made structural). A14's letters-frame is correctly preserved as
principle while its implementation retires — the annotation says so at
the site, the grip-identical and no-badge checks survive untouched,
and the RETURN GATE (content law: no guilt, no counts, no repeats,
deduplicated) is recorded on the ledger item AND in-code, so the sleep
cannot quietly become the grave.

**S2 — the beats sentence dies deeper than asked.** Not just the
render line: the lens no longer *reads* beat data at all — the
`linkedBeatName` field, the StoryPlan/Framework lookup, and the
`entry.beatId` read are gone from `tutorLenses.ts`, and the
`tutorStructureNoBeat` term is removed from the lexicon union itself
(a type-level retirement, tsc-proven). A lens that cannot speak a dead
language is better than one that merely doesn't. Home and memberships
— true, home-derived — survive; the Thread arc inherits the true
linked-language, as briefed.

**S3 — the honest disclosure.** The build found the dedup ALREADY
existed — `computeFragmentItems`' own `seen` set, present since TU1 —
and said so plainly instead of shipping a no-op dressed as a fix. The
deliverable became the regression guard: the both-groups fixture (an
entry qualifying for recent-capture AND starred-shared-tag) proves
exactly-once listing. Correct conduct under the stalled-report law's
spirit: no agent claims a fix it didn't make.

**The parks — the file's own convention, lawful.** Three originals
quoted verbatim in tu1's pok section (verified against the deleted
lines in this diff), record-form probes with named live successors:
the fx12.mjs sweep for the two nudges checks, tu1's own in-place live
S3 successor for the beat check — the cd1-chain pattern in miniature.

## ADVISORIES — non-blocking

1. **V5's real anatomy, for the record:** Nick's duplicate sighting
   was almost certainly two DISTINCT entries with identical text (his
   test entries) — id-dedup was already true and is now guarded, but
   two same-text entries still render as indistinguishable rows.
   Visual disambiguation of identical first-lines (a quiet date or
   home suffix) rides the Tutor-content return ticket. The wipe will
   also delete the offending twins.
2. `tutorNudgesTitle`/`tutorNudgesEmpty` survive in the lexicon as
   dormant terms — intentional (the return ticket re-uses them); noted
   so no future lexicon audit flags them as orphans.

## Close condition

Nick's sitting eye on the quiet panel; ships with the P0 wave. Then
item 64 closes — and the Tutor's room contains only living language
and things that speak when spoken to.

— Fable
