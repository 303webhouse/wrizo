# FX15 — post-merge review (Fable) — item 68
Reviewed at merge f64230d. Census: 6 files, +309/−67, zero
schema/server/deps. VERDICT: PASS. Item 68 REVIEWED — GREEN.
The default flip is one honest line (visible requires pref==='on');
the unbidden affordance removed whole; optIn() exposed as the
on-request entry (BG1's Sprout is its caller — the deck survives,
only the unbidden OFFER retires). fx15.mjs proves deck-drawn via a
disclosed test seam (a model line cannot be a POOL member; a
synchronous draw means no send on load), A13 structurally (span,
aria-hidden, outside the contenteditable), no tab-fill, vanish on
first ink, never-persists. The S3 parks in b2.mjs/fx14.mjs are
park-on-park done right: FX14's route park kept byte-identical,
FX15's note superseding further, originals verbatim, successors
named; the retired door's reasoning recorded (dead SV6 vocabulary —
the Journal is a home, not a page kind). ERA NOTE: the merge-time
suite (44/44 + 43/44, th2-armed documented) was cleared under the
P1-era standard, since superseded by DF1.1 and re-swept clean
repeatedly (50/50 → 52/52). LINEAGE: SV18's stale-'on' escape and
SV31's placeholder removal were new sitting verdicts, both shipped
as FX16, both parks verified in that review.
