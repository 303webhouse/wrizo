# The menus wave — post-merge review (Fable) — merge 0aa986c

VERDICT: PASS. Basis, stated: the SERVER surface read whole at
the bytes (migrate.ts, sync.ts, persistence.ts) — the wave's one
schema addition is TWO ADDITIVE NULLABLE JSONB COLUMNS
(journal_entries.page_settings; users.page_defaults) on the
proven tutor-column recipe: no default, no backfill, idempotent,
null→undefined→app defaults, existing rows byte-identical —
ROLLBACK-SAFE (old code ignores new columns). Birth-from-defaults
is a COPY at creation, argued in place as R6's correct reading.
The defaults endpoint is a deliberate non-/sync singleton with
requireAuth, reasoning stated. The three new stores read
structurally: menusDrawers' geometry-only coexistence policy with
its honest first-paint posture and the reused-close-path
exclusion; pageDefaults' memory+localStorage+server posture,
offline-tolerant, sync-readable at birth. Parks: chat 1's
real-diff verification (30 verbatim supersession headers, gen-3
successors, citations) is leaned on and credited. Evidence base:
120/120 both settings at one tree and one bundle; probe 42/42;
cascade flush 0.00 px; shots byte-identical across a moving main
— and the founder's own walkthrough acceptance, which is the
house's acceptance criterion. The red parked run standing in the
offer BESIDE its green re-run is registered as the offer form's
new high-water mark: the red is the finding, the green only the
receipt. OBS: none blocking. E1's repair constraint (never hide
a failed measurement behind a full-bleed canvas) rides the errata
ticket as its first law. — Fable, 2026-08-28
