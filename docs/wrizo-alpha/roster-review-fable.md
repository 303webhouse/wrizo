# Item 84, the Draft roster — review (Fable) — merge 679015b

VERDICT: PASS. Server read whole: tutor.ts accepts ONE optional
field on the Bible's exact validation terms (absent, never
empty, capped with the delta's own backstop reasoning — a
payload separately visible is separately testable); the splice
is delimited, last-of-three, never persisted; the SYSTEM_PROMPT
paragraph is the house register whole ("name what the stretch is
doing, question it, point — never rewrite it, never offer a
replacement, not even a fragment") including the honest
scope-limit ("you have not been given the surrounding page").
The button law is enforced where the press happens — the server
never invents the key. useSurfaceSelection keeps A13
architecturally (a plain string crosses; nothing writable does)
with the asymmetric retention rule that makes the chip pressable
at all, the hook unconditional and only its EFFECT gated, and a
closed gate reporting empty rather than stale. The v4 bump
carries its full authority in place — candidate B, annotation
form, v3 verbatim beneath, one integer compare. TD4's wire
obligation (exactly {messages, selection}, delta burned first)
is discharged by item84b's 60 checks. — Fable, 2026-09-02
