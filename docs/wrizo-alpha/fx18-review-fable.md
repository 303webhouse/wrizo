# FX18 — post-merge review (Fable) — item 75
Reviewed at merge 7edd097 (build 0ceb080, amended post-iterate).
Census: 5 files, +479/−18, zero schema/server/deps. VERDICT: PASS.
Item 75 REVIEWED — GREEN.
The three-regime law lands exactly as ruled: Tutor.tsx's measure-
effect sets inline width from the TRUE geometric margin (the
+frame-gap overshoot — the 28px dip — named and excluded); regime
2's downward discontinuity at 280 is why CSS clamp could not
express it; the board rule stays pure CSS (no fallback flash); the
screenplay half-measure companion prevents an 8.5in mis-measure.
The z-lift is fully reasoned INCLUDING why the sliver twin needs
none (inset gutter vs. flex-end decoupling — the stacking cure vs.
the inset cure). PARKS: the fx10 supersession is the wave's most
consequential and is done at full standard — the superseded
assertion AND its own ruling comment quoted verbatim, SV24/SV26
named as outranking authority, successor correctly SPLIT (lenient
in-place invariant; exact three-regime law in fx18.mjs). fx18.mjs
asserts regimes BY NAME from measured margins — surface-dependent,
never assumed — with the gripDiag instrument making any failure
self-explaining. Both review checkpoints CLOSED: the disclosure
backdrop is a real modal (pre-seeding mirrors fx10/tu1 precedent —
"harness bug" was the true classification, the z-lift was correct
all along); the dropped 900px leg tested a surface that does not
exist below the 1100px frame minimum, and the degradation regime is
genuinely exercised (script@1100/1366, page@1100). The scout notes
file is a model record — its own falsified claims documented in
place ("disk wins"), the fork surfaced to the desk, the ruling
verbatim. OBS (non-blocking): a paper-scale change while the panel
is open defers re-measure to the next resize/reopen; note the scout
notes' "Implemented" paragraph predates the final iterate (the
[data-overlay] attribute was dropped for the inline width) — the
code and fx18.mjs are the law.
