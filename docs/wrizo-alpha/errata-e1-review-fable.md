# E1 + family — review (Fable) — merge 955384c
VERDICT: PASS. Read at the bytes (menusDrawers.ts, Sliver.tsx,
Cascade.tsx vs 89b8ff5). The announcement is now STRUCTURAL: both
drawers announce from an effect keyed on their one derived truth
(Sliver: `open`; Cascade: category-chosen AND not-docked) — every
path announces, none has to remember to, and the impure-updater
hazard (announcing from inside setState, which React may run twice
or discard) is removed with it. The paper selector is corrected
to THE BOX THE WRITER SEES (.board-canvas-wrap, .script-sheet),
single-sourced with the probe — restoring the two-drawer law on
framed screenplay, where it had been silently disabled. Fable's
constraint is implemented as its first law: the permissive answer
only after both boxes prove laid out; a measured band ≤ 0 is a NO.
fx7's NOVERDICT stands as independent confirmation. 61/61 both
settings; shots byte-identical — the repair moved no pixels.
— Fable, 2026-08-31
