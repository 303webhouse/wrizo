# CD4 — the Two Retirements · micro-brief · 2026-07-24

**Place at:** `docs/wrizo-alpha/cd4-two-retirements-brief.md`.
**Owner: chat 3** (named per the start-word law). **Branch:**
`cd4-two-retirements`, own worktree; guard-rail before every commit;
ledger on `main` only. **Sequencing:** runs between FX11's post-merge
review and M3. **ZERO SCHEMA, ZERO SERVER FILES.** Merge pre-authorized
as zero-schema; Fable reviews post-merge; deploy rides the batched word
with FX11 + M3.
**Authority — Nick's words of 2026-07-24, quoted:** "Done should die
everywhere. A writer is never Done — they may choose to finally share a
piece they've written, but that option lives under Publish or
Workshop." And on the old beats control: "I'm not really sure how this
fits in with the new architecture" — ruled by Fable as retirement of
the control, the system dormant beneath it, vetoable by Nick.

## S1 — Done dies everywhere

The Board's Done — the last one standing — is removed; PAGE → remains
the Board's only exit (its unpaired fallback already proven under
genuine trusted pointer in `bm1.mjs`). Grep-first for every "Done"
control across surfaces; any stragglers die in the same pass. **The
park obligation, per the codicil:** the b2/hb1 Board-Done checks and
cd1's successors falsified by this removal carry their full A4 park
cycles — verbatim originals, superseding authority (Nick's word,
quoted), live successors — in the same commit as the removal.

## S2 — the old Plan control retires

The page bar's legacy beats control (the elder of the two "Plan"
words) is removed; the arrow-dressed PLAN → door becomes the bar's
only Plan word — resolving the A3 collision by retirement, not rename.
**The system goes dormant, not dead:** `beat_id` and `story_plan_id`
stay in the schema untouched and grandfathered; no data migration, no
deletion; the StructureWizard's superseded-not-killed precedent binds.
The successor is the Thread arc (see `thread-arc-seed.md`), designed
post-vacation. Grep-first for every harness assertion touching the
control or the bar's action list — the live gen-3 bar check
(`['Pages','Plan','Plan →']`) is falsified by BOTH slices: one park
cycle, one gen-4 successor asserting `['Pages','Plan →']`.

## Harness

`cd4.mjs` or extensions in place, builder's call disclosed: the bar's
final shape at all three widths; Done absent everywhere (structural
sweep); PAGE →'s paired and unpaired paths re-proven under trusted
pointer at the new bar; the Publish surfaces untouched (Done's death
does not disturb its neighbors); legacy <1100 consistent.

## Non-goals

Anything Thread-arc (linking, popout menus, card icons — post-vacation
by committee); any touch to beats DATA; W1; the StoryPlan fold.

## Definition of done

No surface in Wrizo says Done. The bar holds one Plan, and it is a
door. A writer who wants to share walks through Publish; a writer who
wants to arrange walks through the door; nobody is ever finished.

— Fable, from Nick's words, for chat 3
