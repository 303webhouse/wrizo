# PW BRIEFS — ONE-CLAUSE AMENDMENT, for the courier
### PLAN desk · 2026-09-09 · browserless · nothing launched, nothing written to the repo

**WHAT CHANGED:** one standing clause added to **`pw1-build-brief.md`** and
**`pw2-build-brief.md`**, immediately after each brief's worktree/branch header. **Nothing
else in either file is touched** — both were re-synced from `main` at `f4094b7` first, and
verified free of the retired box-quiet hold before the insert.

---

## WHY — the gap is in my own documents

Both briefs pair **"build in your own worktree"** with **"full suite, both settings, green,
independently re-run"**, and say **nothing about the box being shared**. A builder reading
only the brief can reasonably conclude *my worktree, my run, isolated* — which is exactly
the belief Fable's re-relay exists to correct:

> **A WORKTREE ISOLATES FILES, NOT THE BOX. One machine, one browser pool; a run from any
> tree is a run on the box.**

The briefs seeded the misreading by putting those two instructions side by side with nothing
between them. The clause goes between them.

---

## THE CLAUSE, as inserted

> **⚠ A WORKTREE ISOLATES FILES, NOT THE BOX** — standing law (Fable via Nick, 2026-09-08).
> **One machine, one browser pool: a run from ANY tree is a run on the box.** Your own
> worktree buys you file isolation and nothing else. This brief's close conditions end at a
> full suite in both settings, so **before any harness, probe or suite launch, read the
> current box ordering on the ledger or ask chat 1 — never infer your turn from quiet.**
> Contention has voided runs in batches; every one of them was a lane that believed its tree
> was its own machine.
>
> *This clause carries no dates and names no window on purpose: a dated hold written into a
> durable brief goes stale and then misleads. The ordering lives on the ledger; the law
> lives here.*

---

## THE DISTINCTION THAT MATTERS, AND WHY THIS DESK IS SURE OF IT

**Last time this desk put a box fact into these briefs it wrote the WINDOW, not the LAW** —
a dated box-quiet hold, complete with *"DO NOT HAND THIS TO A BUILDER YET."* Within a day
the hold lifted, PW1 went to main for its builder, and **that clause would have blocked the
lane Fable had just cleared.** It never reached main only because chat 1 committed the
earlier version; the stale copies survived in this desk's scratchpad and in Nick's Downloads
until they were re-synced from `main` this session.

**So: a durable brief takes the standing law and points at the ledger for the order.**
A window written into a brief is a landmine with a timer. **This clause has no expiry, no
lane names, and no ordering in it** — when the order changes, the clause is still true.

---

## COURIER

Amended copies are in **`C:\Users\nickh\Downloads\`**:
- `pw1-build-brief.md`
- `pw2-build-brief.md`

**This desk wrote nothing to the repo** (`git status` clean, verified) and **launched
nothing** — it is not a lane named in the current ordering, so it is building browserless as
instructed. **Chat 1 commits, post-stamp, whenever the deploy pair is done.** There is no
urgency: PW1's builder is not blocked by the clause's absence, only under-warned by it.

**Nothing locks.**
