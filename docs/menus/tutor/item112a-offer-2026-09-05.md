# ITEM 112-A — REVISE'S DOORWAY AND FLOOR, OFFERED

**Lane:** 112-A build · **Branch:** `item112a` ·
**Worktree:** `.claude/worktrees/item112a`
**Date:** 2026-09-05 · **Standing:** **OFFERED, NOT MERGED.** The branch is pushed;
the merge to `main` is chat 1's act. **No deploy is implied or asked for** — Nick's
conditional ship word (`2820727`) fires on chat 1's own armed sequence, not here.

Branched from `origin/main` @ `89c5955`; carries `origin/main` @ `2820727` merged in.
**Brief:** `docs/menus/tutor/item112a-build-brief.md`, blob `75e81367`, **md5 `9619b2a6`**
— the AMENDED tip (both grips always visible, opening even onto an empty drawer).
**Charter:** `docs/menus/tutor/item112-revise-charter.md`, RS1–RS7, ratified 2026-09-02.
**S0:** `item112a-s0.md` · **Build record:** `item112a-build-record.md`.

**ZERO SCHEMA**, as the ship word requires — checkable rather than asserted:

```
git diff --stat 2820727..a672317 -- apps/server packages \
    apps/desktop/src/store/persistence.ts apps/desktop/src/types
    (empty)
```

Revise going `live:true` is entirely client-side. **Product delta: 7 files, +198/−45.**

---

## §1 · THE OFFER

**Offered SHA: `a672317`** (the tree both stamps name). This record + the ledger entry
land on top as the final commit.

| SHA | what it landed |
|---|---|
| `af3c54f` | S0 survey + the source change (Revise live, free-edit branch, empty Desk drawer) + the harness, pre-control |
| `6cc0afd` | harness green at 53; temporary diagnostics removed |
| `1dede79` | the item84b park + 3 successors; the S7 close-by-grip check; the park-count correction (0 → 1) |
| `4d68332` | build record (pre-merge stamp, since superseded) |
| `7f48238` | **merge** `origin/main` `1c8edd3` — the errata wave; one conflict, resolved |
| `6093e35` | build record re-stamped on the merged tree |
| `09a8011` | **merge** `origin/main` `2820727` — the ship word, item 99's reaper, the new runner |
| `a672317` | `b2.mjs` fixture race closed (outside 112-A's scope — §5) |
| *(this commit)* | this record + the ledger entry |

### BOTH SETTINGS CLEAN, ONE TREE, ONE BUNDLE, NEITHER STAMP DIRTY — 69/69 each

```
REAPER: 0 harness browser(s), 0 owner(s) —  | dead-owner targets=0
REAPER: nothing to reap.
SUITE DONE HARNESS_PARKED=unset — 69/69 of 69 returned a passing verdict
SUITE RESULT: CLEAN — tree=a672317 bundle=index-CK6B8dF1.js/557023b

REAPER: 0 harness browser(s), 0 owner(s) —  | dead-owner targets=0
REAPER: nothing to reap.
SUITE DONE HARNESS_PARKED=1 — 69/69 of 69 returned a passing verdict
SUITE RESULT: CLEAN — tree=a672317 bundle=index-CK6B8dF1.js/557023b NO-REBUILD
```

The parked pass ran `--no-rebuild` against the byte-identical bundle the default pass
tested, so **both stamps name the same software as well as the same tree.**
`item112a.mjs` **55 PASS** · `item84b.mjs` **62 PASS + 1 PARKED** · `b2.mjs` **84 PASS**.

### THE PROBE — 44/44 green, and it says something about the anchor

```
44/44 checks green
PASS  prose  1366  dock flush at paper (open)   paper.left 375.3 − dock.right 375.3 = 0.00px
PASS  prose  1680  dock flush at paper (open)   paper.left 501.5 − dock.right 501.5 = 0.00px
PASS  prose  1366  paper rect invariant under dock toggle  left 375.3→375.3, w 615.4→615.4
```

**The probe's matrix is 1366 and 1680. It never visits 1100.** That is exactly where the
ratified 29.7px dock overhang lives — so the cluster's binding input is not merely a fault
the probe missed, it is one the probe **cannot see** at its current widths. Named here as a
concrete, cheap note for 115/117/119: adding a 1100 leg to `menus-probe.mjs` would put the
frame's own minimum width under the ruled `dock flush at paper` law for the first time.

---

## §2 · WHAT SHIPS

A Revise the writer can enter from the strip, write freely in, walk at both reference
widths with both hands, and leave — carrying the mirrored-hands machinery and **nothing
else**. **The source change is small, and that is the finding:** `Sliver` and `Tutor` are
mounted by `PageEditor`'s framed branch with no reference to `mode` in either mounting
decision, so Revise inherits both hands, their paper anchoring, the coexistence law and the
announce-from-effect invariant **without a line of new geometry code**. The room already
existed; Revise had never been allowed into it.

| file | what changed |
|---|---|
| `ForwardOnlyEditor.tsx` | `EditorMode` gains `'revise'`; the internal `drafting` flag becomes `freeEdit` (Draft **or** Revise) |
| `ModeStrip.tsx` | Revise `live: true`, real active state, `onSwitch('revise')`; `reviseEnabled` opt-out; `data-mode-key` contract marker |
| `ModeSwitcher.tsx` | a Revise tab, gated behind `reviseEnabled` (default **false**) |
| `ModeStage.tsx` | a `revise` entry in the total `RAILS` record |
| `PageEditor.tsx` | persistence allowlist admits `'revise'`; **new `sliverContent` branch** → empty Desk drawer; passes `reviseEnabled` |
| `Tutor.tsx` | two comments corrected — their stated reason expired |
| `ScriptEditor.tsx` | `reviseEnabled={false}` — the Draft-law-only surface keeps its law |

**§8's exit, item by item:** enter (S1) · write freely (S2) · open **and close** both hands
by their own grips (S7) · the empty Desk drawer opens without error (S7) · both reference
widths (S3/S4/S5, every geometry leg runs at 1100 **and** 1366) · the paper's measure
untouched (S3, sheet rect **and** rendered text column, with a laid-out control) · the
editor-path answer on the record (§3 below).

---

## §3 · THE ANSWER THE PARKED LENS NEEDS — **YES**

Revise renders through `ForwardOnlyEditor`'s **free-edit branch**, so its rendered text runs
through `decorateEditorFor` on every keystroke — and that function **already carries the
decorator override** the flag decorator will use: a fifth parameter, `decorate`, defaulting
to `decorateMarkdown` and already load-bearing for `BoardCardPopup` (FX5 S6).
**The lens needs an argument, not new routing.** Re-verified after the errata wave touched
`draftDecoration.ts`: the signature is unchanged.

Two constraints ride with the seam: **character count must stay 1:1** (the caret restore
depends on it — independent corroboration that the CSS-only flag law was always the only law
this seam permits), and **never `display:none`/`visibility:hidden` a marker** (it would
vanish from the STORED text via `innerText`).

The forward-only instrument is absent **structurally, not by configuration**: on that branch
the runway's listeners never attach, the caret effect returns at its first line,
`insertMarkerRef` is nulled, no run span renders, `forwardLock` is never read.

---

## §4 · SURFACED, NOT BUILT

**1 · THE 1100 ANCHOR — ratified as a cluster binding input (`2820727`).** The ruled contract
(`paper.left − dock.right === 0 ±0.6`) holds at 1366 (0.00px) and is violated by **29.7px at
1100**, the frame's own minimum width. **Established by control, not argument:** clean
`origin/main` `89c5955`, src reverted and rebuilt in this same worktree, reads the identical
−29.7 in **both** Draft and Free Write; Revise's rects are byte-identical to Draft's at both
widths. Re-verified after the errata wave changed `Sliver.tsx` (+305) and `index.css` (+51) —
it survived unchanged. **Not repaired here**, per §5's own "an anchoring fault is a CSS fault
… stop and surface, do not reconcile in the build." The harness asserts the flush law where
the app achieves it and **pins** the 1100 deviation to main's measured baseline as a
regression guard, naming the control SHA inside the check. **→ 115/117/119.**

**2 · THE MODE-STRIP SUB-LABEL — RULED, not open.** Raised by this lane as a stop-and-surface;
**Nick has ruled it** (`2820727`): Draft's stale `revise` becomes **`mark`**, by the ratified
sentence **Draft MARKS, Revise DRESSES**. Lexicon change only, one line, **rides the next
window (112-B or the next errata), non-blocking for the floor's ship.** This branch therefore
leaves Draft's `sub` untouched on purpose — the rider owns it, not 112-A. Revise's own
sub-label is `dress`, from the charter's organizing sentence, not coined here.

**3 · BELOW 1100 — OPEN, a desk question, non-blocking.** Revise is live below the frame gate
on **Draft's exact terms** — §2 says make Revise behave exactly as Draft does, and inventing a
narrower rule would have been invention. Below 1100 Revise has no hands and no geometry floor
— but neither does Draft. **If the desk would rather Revise be a framed-only posture, that is
one prop (`reviseEnabled` on `ModeSwitcher`, already the seam) and no other line moves.** Not
decided by this lane.

---

## §5 · ONE CORRECTION OUTSIDE THIS TICKET'S SCOPE — `b2.mjs`

The full suite surfaced a red in `b2.mjs` (S2, board pinning) — **not this lane's file**
(`git diff 2820727..HEAD -- b2.mjs` was empty). Diagnosed rather than re-rolled:

Both `PinToBoardSheet` destination rows were clicked with **`?.click()` behind a bare 150ms
sleep**. The sheet renders in two stages — projects, then that project's boards — so a row
that had not rendered yet made the call a **silent no-op**: no pin, and the assertion then
reported a bare `[]` with nothing to say the row had been missing.

**Measured:** 1 red in 6 observed runs at tree `09a8011`. The **parked** run of the same tree
and bundle passed it; standalone passed 3/3. A broken pin path fails every time; an
unrendered row fails only when the box is busy.

**A green re-run was available and was not used as the clearance.** `d6` came back CLEAN
69/69 — and "passes in isolation" and "the machine was quiet" are both retired here, so the
instrument was repaired instead. **The repair is the loud failure, not the wait:** waiting
alone would have made the red rarer without making it legible. Each row is now waited for and
clicked in a form that asserts it was found. **Negative control** (row renamed to a
non-existent label): the new fixture check fails FIRST and names it —
`appeared:false, clicked:false` — instead of laundering a missing row into an empty-boxes
result downstream. **No park owed:** the ruled assertion's text and subject are unchanged;
only the fixture that sets it up. 82 → 84 checks.

---

## §6 · THE PARK — ONE, AND THIS LANE'S FIRST COUNT WAS WRONG

`item84b.mjs` S1 asserted, verbatim: *"the Revise tab is inert — clicking it does not leave
Draft, so the roster cannot render in Revise because Revise has no live surface to render
on."* 112-A falsifies its **premise** by ruling.

**The sweep missed it and claimed `PARKED COUNT: 0`.** It searched for `deferred` /
`aria-disabled` assertions naming Revise and found only Workshop and the script surface's
Free Write key. That assertion says *inert*, never *deferred* — a vocabulary-shaped grep could
not see it. **The full suite caught it** (a red at `item84b.mjs`, 1/60), which is item 84's own
arithmetic in canon: the count is a number to be audited, not a silence to be trusted. The
lesson is left standing in `item112a.mjs`'s park block rather than quietly amended.

Handled park-never-edit: the original stands **verbatim** under `SUPERSEDED` with a successor
pointer, in `item84b.mjs` — the file that owns it. Its live successor is **stronger** than what
it replaced: the conclusion used to be true *by construction* (Revise could not be entered),
and is now a live surface the roster must actively decline. Two further checks landed with it.
**Ticket park count: 1.** `item112a.mjs` parks 0 of its own. Both armed and passing.

Two **source comments** were corrected in place, not parked (`Tutor.tsx`, `ModeStrip.tsx`) —
they describe live mechanics; the park law governs assertions. Tutor's conclusion is unchanged;
only its reason moved from the type to the branch.

---

## §7 · WHAT STAYS DEFERRED

Untouched by this ticket, by §1's own list:

- **The Type section (RV1–RV4)** → **112-C.** Revise's Desk drawer opens onto empty content
  today; 112-C fills it. *(The hazard this caught: `sliverContent` was a two-branch ternary and
  Revise fell down the `else`, inheriting Draft's Structure control and format rail — which
  `applyRailFormat` then declines, so they would have rendered **live-looking and inert**, the
  locked door wearing paint G3 forbids.)*
- **The Revise Counsel roster** → **112-D.** No roster renders in Revise; asserted as an
  absence, not trusted to a branch.
- **The error lens (T1–T7)** → parked, in its ruled order. No flag of any kind renders.
  DR7-as-narrowed authorises its marks and nothing else; this ticket ships no unbidden anything.
- **RV3's sought door** → opens with the identity backlog, per the charter.
- **Free Write and Draft** → unchanged; their own harnesses green, and `item112a.mjs` S9 asserts
  Free Write still mounts the forward-only instrument and still **strikes** on backspace, which
  is what makes S2's absence in Revise a relocation rather than a deletion.

---

## §8 · THE ASK

**Merge `item112a` @ the pushed tip.** Chat 1's armed sequence per Nick's conditional ship word
(`2820727`): verify zero-schema (§0 above — expected empty, and any schema **STOPS**) → merge on
green → Fable's review at the merge → on PASS, the full deploy checklist naming **112-A and item
99**, rollback ratchets `1c8edd3` · `798fb798`.

**No deploy is asked for here, and the word never covers a red.** Both stamps above are clean at
one tree and one bundle; the one red this lane met is diagnosed, repaired, and negative-controlled
in §5, not re-rolled.

— the 112-A build lane
