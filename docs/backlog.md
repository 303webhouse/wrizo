# Backlog / Build Log

Reverse-chronological log of shipped tickets (newest first). One line per ticket; link the brief where one exists.

## W2 — The way back — merged to `main` and deployed, S25 + desktop hardware gates open
Brief: [w2-way-back-brief.md](w2-way-back-brief.md). Authorized by [page-primacy-canon.md](page-primacy-canon.md) (ratified on Nick's delegated word via Fable, `docs/w1-close-handoff.md`). Built on `w2-way-back` off post-W1 `main`.
- **The fix.** Navigation is honest, but every departure from a live writing session now offers a stateful one-tap return instead of the browser's blind, scroll/caret-ignorant back button. `store/wayBack.ts` (sessionStorage, one slot: entryId/route/scrollY/caret/mode/capturedAt) + `store/caretOffset.ts` (a generic linear-character-offset get/set over a text container, TreeWalker-based) + `components/useWayBack.ts` (one hook, capture + restore) wired into all five writing surfaces: PageEditor and QuickSprint capture ModeStage's internal `.mode-scroll` + the ForwardOnlyEditor host's caret; JournalEntry captures window-scroll + its own sheet's caret (authored pages only); BoardEditor/ScriptEditor capture route + mount identity only (their own view state already persists through their own stores). The return chip lives in DeskRail's top slot — "↩ + the page's first line," the ChromeHandle dot's ember-warm visual family — showing on any non-writing route while a captured session exists and its entry still exists. Reaching the same entry by ANY path (not just the chip) also consumes the slot, for free, since restore keys purely on entryId. The AGENTS.md PAGE IS PRIMARY rule landed verbatim with this ticket, per the canon.
- **The subtle bug, found and fixed mid-build (not a review finding — CC caught this during its own verification):** clicking anything outside the editor to navigate away blurs the editor and can move the browser's Selection *before* the unmounting component's cleanup runs, so a naive "read the DOM at unmount" capture silently recorded `scrollY: 0` / `caret: null` every time. Fixed by tracking scroll + caret CONTINUOUSLY into refs while mounted (a scroll listener; a `selectionchange` listener that freezes at the last in-editor position once focus leaves) and capturing from those refs at unmount instead of querying fresh.
- Verified: `tsc` ×2 + `build:web` + selftest clean; new `scripts/harness/w2.mjs` (21 checks) — mid-scroll/mid-caret capture+restore on both ModeStage and the window-scroll Journal surface, byte-identical text across the departure, one-shot chip consumption, route-only restore for board/script, two PAGE IS PRIMARY standing assertions (assist-rail collapse, the Add to… sheet) promoting W1's own rect-invariant check to a permanent one. `j4`/`j5`/`s1`/`w1` all re-run green.
- **Fable's W2 review — two required, three advisories** (`docs/w2-review-fable.md`). Verdict: no data-loss-class or architecture findings — "the hard part is right… strong ticket."
  1. **R1 — prove the keying, don't comment it.** The hook's own header comment asserted "callers are keyed by id" — true for PageEditor and JournalEntry, **false for QuickSprint**: it had no remount-forcing key at its route mount at all, so navigating between two different sprint routes reused the same component instance. `liveRef` (updated unconditionally every render) would already read the NEW draftId by the time the OLD one's capture cleanup ran — mislabeling the departing sprint's scroll/caret under the arriving one's id. Fixed by wrapping `QuickSprint` in an outer `key={draftId}`-forcing component, matching PageEditor/JournalEntry's existing pattern. Verified `draftId` is stable across mounts and that mode already persists per-draft independently (`wrizo-mode-${draftId}`, the same mechanism PageEditor uses) — no `applyMode` wiring needed.
  2. **R2 — cancel restore re-asserts on first real input.** The rAF + 80/200/350ms re-assert ladder (needed to win the mount-seeding race against a surface's own initial adjustments — the typewriter's hold-band scroll, specifically) fought the writer if they acted inside that window: typing resumed at ~300ms would get the caret yanked back mid-word by the 350ms write. Fixed with a self-removing canceller on keydown/pointerdown/wheel/touchstart (capture phase) that clears the remaining re-asserts the instant the writer does anything; the initial rAF apply always lands.
  - Advisories A1 (`getCaretOffset`'s per-keystroke TreeWalker walk — negligible at current scale, noted for a future denser editor structure), A2 (`setCaretOffset`'s focus-after-range order — correct in Chromium/Electron, noted for a hypothetical non-Chromium target), A3 (a reload preserving the chip — judged CORRECT, not a bug, recorded so it isn't "fixed" later) all noted in file headers, not code changes.
  - `scripts/harness/w2.mjs` grew 21 → 31 checks: the pager A→B non-leak proof (JournalEntry is already correctly keyed — a proving/regression test, not a live bug there), the full QuickSprint depart/return round trip (scroll + caret + Draft mode), and the R2 cancel-on-input proof.
- **Merged to `main` and deployed — 2026-07-13.** Merge pre-authorized (Nick, fix-forward mode: zero-schema, client-only, single-user prod). Merged clean (fast-forward, no conflicts). Full suite green on merged `main`: `tsc` (desktop + server) + `build:web` + selftest + `j4.mjs` (26/26) + `j5.mjs` (40/40) + `s1.mjs` (87/87) + `w1.mjs` (18/18) + `w2.mjs` (21/21). Pushed, `railway up` — confirmed live. Folded w2.1 (R1 + R2 + harness growth to 31), re-ran the full suite again (all green), pushed + redeployed a second time for Fable's delta spot-check. **Zero-schema** both times — liveness check only (`200` on `/`, the auth gate responding).

## W1 — Writing-surface polish — merged to `main` and deployed, S25 + desktop hardware gates open
Committee briefs: [page-primacy-committee-brief.md](page-primacy-committee-brief.md) · [progress-milestones-committee-brief.md](progress-milestones-committee-brief.md). No CC build brief — requested directly in-session (Nick: fix the journal/page-editor writing-surface chrome, glow/progress bar, typewriter, fade timing, and layout-shift bugs; write committee briefs for anything needing an architecture decision). Built on `w1-writing-surface-polish` off post-S1 `main`.
- **The fixes.** Extracted glow + progress bar + typewriter toggle into shared `WritingIncentives.tsx` (ModeStage now consumes it too — no forked implementation). The progress bar is a repeating lap toward the word/time goal (`useGoalProgress`): a subtler brown/cream track, an orange caret at the fill's leading edge, a full-orange celebration pulse + reset on lap completion — decoupled from B5's in-document pagination, which previously drove the bar's fill directly. Ported the glow/progress bar to `JournalEntry` (authored pages only), which had never had ModeStage's incentive-layer chrome at all. Extracted the typewriter scroll/jolt engine into a shared `useTypewriterFade` hook — the container-scroll variant (ModeStage) unchanged, a new window-scroll variant for `JournalEntry` (its ink layer needs the sheet to grow unbounded, so it can't be clipped into a fixed-height box); widened from Free-write-only to Free write + Draft; added a persisted (localStorage-only) setting plus a bottom-right toggle icon. `JournalEntry`: only wayfinding + the document-type tabs stay above the writing surface now; star/tags/copy/port/add-to/routing/autosave-note moved below it. Wired Workshop/Publish action tabs into `PageEditor`'s `ModeSwitcher` (existed in `QuickSprint`, never passed there). `useChromeDissolve`: an edge reach now requires a brief dwell before summoning chrome back (was instant on proximity); summon eased 0.4s → 0.7s; supports an array of `rootRef`s so a host's own top bar (a `.mode-stage` sibling) shares the dynamic `--fade-dur` timing instead of a hardcoded 220ms. **Fix:** `ModeStage`'s three-column layout was a centered flexbox, so collapsing/expanding the AI assist rail shifted the page horizontally — switched to a fixed-track CSS grid (mirrored at the ≤900px and ≥1700px breakpoints).
- Verified: `tsc` + `build:web` clean; a CDP scenario against the running app (glow/bar/typewriter presence, goal-celebration trigger, Workshop/Publish tabs, page-column stability on rail collapse); pre-existing `j4`/`j5`/`s1` regression harnesses re-run green (26/26, 40/40, 87/87) — none of those three tickets touched or rebuilt here.
- **Fable's W1 review — four required fixes, folded** (`docs/w1-review-fable.md`). Verdict: REQUIRED FIXES — 4 (three code, one process ruling), no data-loss-class findings — extraction quality, edge-dwell state machine, fixed-track grid, and pageType-delegation isolation all confirmed clean.
  1. **R1 — spurious celebration on mount.** `useGoalProgress`'s `lapsRef` initialized to `0` unconditionally, so opening any existing ≥250-word page (either surface) fired the goal-celebration pulse immediately for laps completed in a prior session. Fixed by seeding `lapsRef` from the first-render value/goal — only a lap crossed during the current mount celebrates.
  2. **R2 — `JournalEntry` ignored the persisted progress setting.** ModeStage honored `settings.progress === 'off'`; the Journal route (no settings gear) rendered the bar unconditionally. Gated the ProgressBar (not the TypewriterToggle, which stays independent) on `writingSettings.progress !== 'off'`.
  3. **R3 — window-scroll `data-scrolled` gate violated C2.** `useTypewriterFade`'s window-mode gate OR'd in raw `window.scrollY > 4`, fading a short page's first line on any incidental scroll even while the sheet's actual top was still visible. Fixed to gate window mode purely on the sheet's own `getBoundingClientRect().top` vs. the viewport.
  4. **R4 (process ruling)** — the `.vscode/settings.json` auto-approve expansion shipped inside the original push postdated the "config changes propose, never ship" rule (ratified 2026-07-11). Nick's call: ratify in place, logged 2026-07-12.
  - Advisories A1/A2/A5 checked clean (documented in `docs/w1-review-fable.md`); A3/A4 folded into the consolidated hardware session.
  - New committed harness `scripts/harness/w1.mjs` (18 checks) — includes the two regression scenarios Fable specified: reopening an existing ≥250-word page on both surfaces does not celebrate on mount, and a short-but-window-scrolled page does not flip `data-scrolled` while a genuinely-scrolled-past-the-fold page does. `j4`/`j5`/`s1` re-run green. `tsc` + `build:web` clean.
- **S25 + DESKTOP GATES — open, not yet deployed to gate.** Edge-dwell + 0.7s summon feel, typewriter window-scroll on the ink surface, progress caret + celebration read, ≥1700px rail-toggle page stability, Workshop/Publish tabs on a Page — feel work headless CDP cannot certify. Folded into the consolidated hardware session (ledger item 2) rather than a separate sitting.
- **Merged to `main` and deployed — 2026-07-12.** Nick relayed via `docs/w1-close-handoff.md`: merge W1 (word given); the two committee canons ratified under Nick's progress-over-perfection delegation to Fable. Merged clean (fast-forward, no conflicts — `main` hadn't diverged since S1). Full suite green on merged `main`: `tsc` (desktop + server) + `build:web` + selftest + `j4.mjs` (26/26) + `j5.mjs` (40/40) + `s1.mjs` (87/87) + `w1.mjs` (18/18). Pushed, `railway up` — confirmed live (`200` on `/`, `/auth/me` returning the expected unauthenticated response). **Zero-schema deploy** (W1 touches no server files) — liveness check only, per the J5 precedent.

## S1 — The element engine (the Screenplay Room) — merged to `main` and deployed, S25 + desktop hardware gates open
Brief: [s1-script-editor-brief.md](s1-script-editor-brief.md) · canon [s-arc-screenplay-plan.md](s-arc-screenplay-plan.md) (v1.1) · [structure-spine-plan.md](structure-spine-plan.md) · [fragments-under-pages-canon.md](fragments-under-pages-canon.md) (§3, ScriptDoc ratified). S-arc ticket 1/5, the arc's heavyweight. Built on `s1-script-editor` off post-J5 `main`. One new dep per the brief's amendment: `@fontsource/courier-prime` (Regular + Bold), scoped to `--font-script` — `--font-mono` untouched.
- **Slice 0 — orient (read-only).** Delegated to an Explore agent against merged `main`; all 7 points matched the brief's names exactly (zero drift) — `PageType` union + `PAGETYPE_LABEL` (`resumeVocab.ts`), `PageEditor()`'s pre-hooks delegation wrapper, `createBinderPage`/`createBoardPage`/the F4 `CreateProject` birth path, the `boxes` jsonb recipe end-to-end (`types` → `persistence` → both sync mappers → `migrate.ts`'s lowercase `alter table ... add column if not exists` convention), VW's `PROSE_SURFACES` + the `BoardTextBox` paste-guard pattern, Courier Prime's confirmed absence since brand-paint, and `StoryPlan`/`Beat`/`getStoryPlanByProjectId` untouched (P-arc's lane, not this ticket's).
- **Slice 1 — the substrate.** `pageType: 'script'`; `ScriptElType`/`ScriptEl`/`Scene`/`ScriptDoc`/`TitlePageFields` in `types/index.ts` (`dual`/`struck`/`number`/`omitted`/`beatId` reserved-not-built, present but never written by S1's UI). `journal_entries.script jsonb`, both sync mappers (`rowToJournalEntry` + `upsertJournalEntries`, param count $1-$20). New `store/scriptDoc.ts` (`flattenScenes`/`groupIntoScenes`/`createEmptyScriptDoc`/`newElement` — the editor works a FLAT element array; a scene boundary is just "the next element with `t==='scene'`"; `groupIntoScenes` synthesizes an empty leading heading rather than dropping content if the very first element is ever retyped away from `scene`, closing a data-loss edge case found during design). New `store/scriptText.ts` (`serializeScriptDoc` — the golden derived-text shadow: blank-line separated, uppercase types uppercased, a character's paren/dialogue lines stay tight beneath it). `persistence.getScriptDoc`/`saveScriptDoc` (doc + shadow in one write) /`createScriptPage`. `PAGETYPE_LABEL.script = 'SCRIPT'`.
- **Slice 2 — the birth paths.** `CreateProject`: `kind==='screenplay'` now births a script page (`createScriptPage`) instead of manuscript; every other kind unchanged. `ProjectHome`: a new "Scripts" section (rendered only when non-empty — no clutter on every other project) grouped atop Manuscript with its own "+ New script page" button; the existing "+ Add support page" picker also gains a **Script** leaf (both paths call `createScriptPage`, never the generic `createBinderPage`, so a script page is never born without its seeded `ScriptDoc`).
- **Slice 3 — ScriptEditor, the room.** `PageEditor()` gains the third `pageType` delegate (the J4 routing rule, verbatim). A house-native block editor: one styled block per element, ONLY the active element a live `contentEditable="plaintext-only"` (guarded fallback `true`) — seed-once-per-edit-session, keyed remount (the `BoardTextBox` pattern). New shared modules: `scriptMetrics.ts` (ch-unit widths/indents/uppercase-types/`PAGE_LINES`/`SPACE_BEFORE` — S2's future paginator imports the SAME module, the one-truth seam), `scriptKeys.ts` (the frozen Enter/Tab table, both AMENDABLE cells commented verbatim per the brief), `scriptAutocomplete.ts` (slugline → location → " - " → TOD chaining, character two-letter completion, `(` extension completion, transitions — all pure/testable, no DOM), `scriptSmartText.ts` (live `int./ext./est./i/e` → scene promotion; auto `(CONT'D)` on a repeat speaker with intervening action). Caret offset/placement via a plain Range/Selection pair (a live element is always a single text node under plaintext-only). Ghost placeholders per type via a `:empty::before` CSS rule (`data-ghost`, never a competing DOM child). Draft law only — no `ModeSwitcher`/`ModeStage` on this surface (commented at the `PageEditor()` delegation site).
- **Slice 4 — the room's law wiring.** Voice Wall: `.script-el-active` joins `PROSE_SURFACES` (whisper-recording) and carries the same paste/drop guard as `BoardTextBox` (own-shadow allowed, foreign blocked + whispered); "Copy script text" emits the derived serialization. Pen discipline: the I0 pattern verbatim (`touch-action:none`, `handwriting="false"`, capture-phase pointer-neutralize), scoped to the one active element. TTFK: `useSessionLog('script', …)` — required widening `SessionLog['surface']`/`SessionSurface` (`types/index.ts`) to add `'script'`, the only other type-surface this ticket touched. Resume/mirror: verified, not built — `resume.ts` already routes any typed `pageType` generically to `/page/:id`, so SCRIPT + the shadow's last line surface on the Desk for free (harness-confirmed).
- Verified in-harness (**82 checks**, `scripts/harness/s1.mjs`, committed): DoD1 (screenplay-kind create lands directly on a script page, ghost visible, seeded `v:1` doc, no title demanded); the frozen Enter/Tab table cell-by-cell, all 8 rows × both keys, plus a "no blank litter" element-count assertion per empty-Enter and Shift+Tab's independent backward cycle; split-at-caret (trimmed tail into a new element); backspace-merge (no separator, caret lands exactly at the join); ArrowUp/ArrowDown edge-walking; the full autocomplete chain INT. → a doc-derived location (KITCHEN) → " - " → DAY, arrow-navigated and Enter-accepted; character two-letter completion + `(` extension completion (Tab-accepted); auto (CONT'D) absent pre-commit, present + uppercased post-commit; uppercase-on-commit (typed lowercase, verified uppercase after Enter); foreign paste blocked (`preventDefault`) + whispered, an own-shadow-matching paste allowed, copy-out emits the exact derived serialization; the I0 pen guard attributes present; the `script` jsonb column byte-identical across a full reload; the Desk's resume mirror shows "SCRIPT" unprompted; a non-screenplay kind still births manuscript (old path untouched); both new birth-path affordances (the dedicated button and the generic picker's Script leaf) work on an arbitrary binder. Stable across 2 consecutive full runs (82/82 both). The persisted `j4.mjs` (26/26) and `j5.mjs` (40/40) re-run green — no regression. `tsc` (desktop + server) + `build:web` (Courier Prime Regular/Bold bundled, confirmed in the manifest) + selftest green.
- **One harness-authoring bug found and fixed while writing the scenario** (a testing artifact, not an app bug): after the backspace-merge check, the caret was still at the merge boundary (offset 5 of "helloworld"), not the end — the next scripted `Enter` (intended to cleanly commit and land a fresh empty element for the ArrowUp/Down test) instead triggered ANOTHER split at that offset, corrupting the doc state the rest of the scenario assumed and cascading 14 failures through the autocomplete/CONT'D sections. The app behaved correctly throughout — the split-at-caret code path fired exactly as designed on the actual (unintended) input; the fix was a `__setCaret` call to the true end before that `Enter`, not an app change.
- **Not independently re-verified this pass (disclosed):** IME/soft-keyboard composition on the `plaintext-only` contenteditable (the codebase's own mobile-typing history — J10.1 — makes this a real, not theoretical, risk area; deferred to the S25 gate); the `contentEditable` fallback path for an environment that doesn't support `plaintext-only` (Chromium/Edge, which this harness runs on, does — the `true` fallback branch is defensive/unexercised code); the exact `caretRangeFromPoint` click-to-caret-position precision (the R1 fix's harness addition proves click-to-*activate* works, but not where the caret lands within the clicked text); the `transition` element's own autocomplete branch (CUT TO:/DISSOLVE TO:/etc. — the code path exists, mirrors the proven scene/character branches, but wasn't separately exercised); the live prod push/pull round-trip for the new `script` column (the D2/J4 ritual — deferred to first deploy, matching every prior ticket this session; only the LOCAL jsonb round-trip across a reload was harness-verified).
- **S25 + DESKTOP GATES — both open, not yet deployed.** Typing rhythm, ghost legibility, autocomplete at pointer and thumb size (S25); pointer precision + keyboard-map muscle-memory feel (desktop) are feel work headless CDP cannot certify — DoD 10 explicitly makes Nick's device verdict the ticket's close condition. Report = push only — **S1 is NOT merged**; it merges only after review returns and Nick relays the word.
- **Fable's S1 review — two required fixes, folded** (`docs/s1-review-fable.md`). Verdict: REQUIRED FIXES — 2 (one code, one verification), no data-loss or sync-law findings — "for a 1,400-line heavyweight this is an unusually clean build."
  1. **R1 — the autocomplete popover was unanchored (code fix).** `AutocompletePopover` rendered as the LAST child of `.script-sheet` with `position:absolute` and no top/left, so it took its static-flow position at the bottom of the WHOLE sheet regardless of where the active element actually was — invisible below the fold on any script longer than a screen. Every harness interaction had always edited the document's tail, so all 82 checks passed while the popover was broken for the primary case: editing mid-document. Fixed by moving the popover's render INSIDE the element map, as the active element's own flow-sibling (a keyed `Fragment`, not a wrapping element) with `marginLeft: INDENT_CH[type]ch` — its no-top/left absolute position now resolves to its static position at THAT point in the document, floating below the active block without pushing later elements down. `s1.mjs` gained a mid-document regression test: click back to element 0 (many elements now follow it), retrigger the popover, and assert its `offsetTop` sits within one line-height of the active element's bottom (not the sheet's) — measured `gap:0, distFromSheetBottom:250` on the actual run, an unambiguous confirmation.
  2. **R2 — CSS-variable existence check (verification only).** All six vars the S1 styles consume (`--ink-on-paper`, `--ink-on-paper-low`, `--ink-border`, `--ink-900`, `--ink-800`, `--paper`) confirmed present in `index.css`'s `:root` block via grep — no code change needed.
  - **Advisories folded opportunistically** (Fable flagged both as "fix whenever the file is next touched" — it was): **A2** — Backspace-merge, the two arrow-walk commits, and `activateAt`'s click-to-activate commit all read `elements[i].text` (React state) where Enter's own DOM read was ground truth; all four now read the live DOM text before committing, closing the asymmetry (React's discrete-event flushing made the gap unreachable today, but it cost one line each to remove). **A3** — the post-reload `waitFor` used a JS comma operator (`"!!a, !!b"`), silently evaluating to only the second predicate; fixed to `||`. **A4** — added an exact-match (not containment) golden-string assertion on a small, isolated fixed doc (`"INT. OFFICE\n\nShe sits."`) — the derived shadow is S3's future Fountain parser contract, and containment alone can't catch whitespace drift.
  - **A1** (synthetic-heading id churn) explicitly deferred by Fable to P3's brief, not this pass — logged, not actioned.
  - **A5** (ghost visibility on a cleared element depends on `:empty` semantics) logged as an S1 gate item for the hardware session, not a code fix.
  - **One flaky-harness bug found and fixed while extending the scenario** (a testing artifact, not an app bug): two `sleep(2300)` calls (waiting out the 2000ms component debounce + 300ms persistence flush) sat with zero buffer at the exact worst case and intermittently read a stale/empty `entry.text` under load — surfaced on a stability re-run (3 fails, then passed, then 3 more fails — genuinely intermittent, not a one-off). Fixed both by polling for the specific expected content instead of a blind sleep (the same lesson J5's harness already learned); stable across 3 consecutive full runs after the fix.
  - `s1.mjs` grew 82 → **87 checks**, all green, stable across 3 runs. `j4.mjs` (26/26) + `j5.mjs` (40/40) re-run green. `tsc` (desktop + server) + `build:web` + selftest green.
  - Pushed; **still NOT merged** — merge waits on Fable's delta spot-check + Nick's word. Per Fable's protocol note: this deploy will NOT be zero-schema like J5's — the live prod push/pull round-trip for the new `script` column (the D2/J4 ritual) is required on first deploy, not optional.
- **Merged to `main` and deployed — 2026-07-11.** Nick relayed "Merge `s1-script-editor` to `main`... `railway up`... then run the live prod push/pull round-trip for the `script` column." Before merging, set aside an unrelated in-progress workstream sitting uncommitted in the working tree (a Publish-dialog stub, `WritingIncentives`/`useTypewriterFade`, chrome-dissolve tweaks — not S1's, not touched by S1) via `git stash push -u` on an explicit pathspec (not a blanket stash — the longstanding untracked `docs/state-of-wrizo-2026-07.md` and `images/` were left alone throughout). Merged clean, no conflicts. Full suite green on merged `main`: `tsc` (desktop + server) + `build:web` + selftest + `j4.mjs` (26/26) + `j5.mjs` (40/40) + `s1.mjs` (87/87). Pushed, `railway up` — confirmed live (`Starting Container` / server listening in the deploy logs, `200` on `/`).
  - **The live prod round-trip (the D2/J4 ritual, required this time — not zero-schema like J5's).** A scratchpad script (`roundtrip-script.mjs`, not committed — mirrors the pattern used for J4's `boxes` check) registered a throwaway test account against the live server, pushed a `journal_entries` row with `pageType:'script'` and a populated multi-element `ScriptDoc` (a scene heading, action, character, and dialogue line) via `POST /api/sync`, then pulled it back down through a SECOND `/api/sync` call simulating a second device (`lastSyncAt` before creation, empty push). Result: `pageType` matches, `entry.text` (the derived shadow) matches, and the full `ScriptDoc` matches byte-for-byte on a key-order-insensitive deep comparison (Postgres/JS reorder object keys on round-trip — same non-issue J4's check already established). **ROUND-TRIP: PASS.** The test entry was soft-deleted afterward to keep prod data clean (the throwaway test account itself remains — inert, no cleanup endpoint exists, no real-data risk).
  - Restored the stashed workstream (`git stash pop`) immediately after — applied cleanly, zero conflicts (its base commit was S1's own final review-fix commit, and the merge had adopted that content wholesale for the files in question, so the stash's diff still applied against the merged tree exactly as taken).

## J5 — The Spread console (lenses + Add to…) — merged to `main` and deployed, S25 + desktop hardware gates open
Brief: [j5-spread-console-brief.md](j5-spread-console-brief.md) · design canon [j-arc-design.md](j-arc-design.md). J-arc ticket 5 (extends J3's Spread). Built per `docs/open-threads.md` item 3 ("Runbook Step 4 self-executes after the J4 merge"), off post-merge `main`.
- **Slice 0 — orient + verify (read-only).** Prerequisite gate re-confirmed both hold (J3+VW on `main`; J4's `pageType 'board'`/`BoardEditor`/`portToBoard` on `main`). Grepped user-visible "notebook" — found two accessible-name violations: `aria-label="Notebook"` (`JournalEntry.tsx`'s nav) and `aria-label="Notebook spread"` (`Spread.tsx`'s grid); fixed to "Journal"/"Journal spread" in Slice 1 (internal names — `getNotebookPages`, `notebookIndexAfter`, etc. — untouched, per the canon). Tag/star authoring confirmed present in `JournalEntry.tsx` (`toggleStar`/`.entry-star`, `addTag`/`removeTag`/`.entry-tags`) — lenses consume it as-is. Plan/beat shape confirmed usable: `getStoryPlanByProjectId` → `StoryPlan{frameworkId, beatNotes}`; `getFramework(frameworkId).beats: Beat[]` (`{id, name, order, prompt, act?}`) — the plan-attach leaf was NOT dropped. Zero new columns needed — confirmed, no surprises.
- **Slice 1 — the lenses.** A quiet chip row (`SpreadLensRow`) under the Spread header: **ORDER** Your order (the existing notebook walk) · Newest (`createdAt` descending) — both VIEW ONLY, neither ever writes `orderIndex`. **CONTENT** All · Text · Ink · Text+ink, predicate ported verbatim from the Journal list's own thumbnail idiom (`hasInk`/`hasText`). **★ Starred** toggle. **TAG** chips — the Journal list's exact conditional idiom (unique-tag set from ALL loose pages, single-active-tag toggle, clear affordance), rendered only when ≥1 tag exists. Filters compose (content × star × tag) under either order. `SpreadGrid` gained a `dragEnabled` prop (`isDefaultLens = order==='your' && content==='all' && !starOnly && !tagFilter`): the delegated pointer handler's `onDown` now early-returns when disabled (mirrors the existing `selectModeRef` guard) — lift never begins outside the default lens, tap/select/focus untouched — with a one-line note ("a view, not an arrangement — drag to reorder in Your order") shown whenever the lens deviates. Selection (`Set<string>` by id) already survived lens changes for free — the count was never index-based.
- **Slice 2 — Add to… (FILE).** New shared `components/AddToSheet.tsx` — a destination-first drill-down (Root → Drawer → Binder) with a clickable crumbline, every leaf carrying its verb tag (`.add-verb-tag`, brass only on MOVES). Root: "The Shelf" (MOVES, `setPageHome(id,'shelf')`) · each Drawer ▸. Drawer: its binders ▸ · "＋ Standalone document here" (MOVES — new `persistence.fileToNewBinder(sourceIds, drawerId)`: ONE Untitled/'other' binder via F4's title-later law, ALL selected pages filed into it) — an empty drawer shows an explanatory line before the same standalone leaf. Binder: "File here as a new page" (MOVES). Wired as "Add to…" beside "Port…" in the Spread's Select-mode header (`Spread.tsx`) and the loose journal page's actions row (`JournalEntry.tsx`, single-page; a MOVE navigates away since the loose-page view no longer applies — a COPY/LINK leaves it in place). A new `components/ActionToast.tsx` (`useActionToast`, mirrors `VoiceWallWhisper`'s hold/fade timing) shows the completion toast; FILE's is exact: *"Filed N pages to \<dest\> — moved; it left the Journal."*
- **Slice 3 — Add to… (ADD/LINK).** Binder level gains, when present: **"Append to \<chapter\>"** per manuscript page (COPIES) — new `persistence.appendToChapter(sourceIds, chapterId)`, selected pages' TEXT in notebook order (Your order) — the J4 port precedent, not click/selection sequence — blank-line separated, landing at the chapter's end; the ink notice ("chapter and plan take text only — ink reaches a project only via a Board.") shows when the selection carries ink. **"Attach to the plan"** (LINKS), only if `getStoryPlanByProjectId` resolves — new `persistence.attachToPlanBeat(sourceIds, binderId, beatId)` sets `beatId` + appends to `routedProjectIds`; the Journal list's existing "routed" crumb picks it up for free. **"Add onto Board — \<title\>"** per existing board page (COPIES) — `portToBoard` refactored: the box-generation loop extracted into `buildPortedBoxes(sourceIds, includeInk, startY, startZ)`, and a new `persistence.appendToBoard(sourceIds, boardEntryId, includeInk)` appends new locked groups BELOW existing content (`startY = max(boxes.y+h) + spacing`, `startZ = max(z)+1`) — the SAME one-choice ink prompt as J4's port, shown only when the selection has ink. Every COPY toast: *"Copied — \<what\>. The originals stay in the Journal."*; every LINK toast: *"Linked — marked routed to \<beat\>; the page stays in the Journal."*
- Verified in-harness (33 checks, `scripts/harness/j5.mjs`, committed per the new harness-persistence rule): the full content/star/tag/order lens matrix on a 4-page seeded population (text-only, text-only+starred+tagged, ink-only, text+ink) renders exactly the right subset per lens, composed correctly; selection survives a lens flip that hides a selected cell AND correctly re-shows it selected; drag under a non-default lens writes **ZERO** `orderIndex` changes (asserted via localStorage before/after) with the lens note showing, drag re-enabled + note gone back in the default lens; FILE-to-Shelf + FILE-standalone-into-an-empty-drawer (birthing exactly ONE Untitled/'other' binder) both move correctly and leave the Journal, toasts exact; chapter-append lands byte-exact (`"Chapter start.\n\nDelta has both."`) with the source page byte-untouched and the ink notice shown; plan-link sets `beatId`+`routedProjectIds` (nothing moves) and the "routed" crumb appears; board-append lands a new locked group's box BELOW a seeded existing box (`newY ≥ seedBottom`) carrying the source's ink + provenance. `tsc` (desktop + server) + `build:web` + selftest green; the persisted J4 scenario re-run green (no regression from the Spread.tsx changes).
- **Two real harness bugs found and fixed while writing the scenario** (both testing artifacts, not app bugs): (1) a direct `localStorage.setItem` fixture-seed doesn't update the app's already-hydrated in-memory cache, AND is silently re-clobbered by a still-mounted component's unmount-flush if the seed happens while that component is on-screen — fixed by navigating to a static page (no pending flush) before every direct seed, then reloading. (2) A blind `sleep(2300)` after typing occasionally raced the 300ms debounced autosave under this scenario's sustained CDP load (5 pages + many sheet round-trips) — `makePage` now polls the SPECIFIC entry (by id, read from the URL) rather than guessing a delay.
- **Not independently re-verified this pass (disclosed):** keyboard-only operation of the lens chips/sheet (DoD 9) — built with real `<button>` elements throughout (native Tab/Enter/Space semantics), not separately CDP-driven; Esc-closes-the-sheet IS verified structurally (a `keydown` listener calling `onClose`, mirroring `PortToBoardSheet`'s absence of one — logged as a J5-specific addition, not backfilled onto J4's sheet).
- **S25 + DESKTOP GATES — both open, not yet deployed.** Chips/sheet at thumb size, drill-down feel, toast legibility (S25); pointer precision on chips/handles/sheet (desktop) are feel work headless CDP cannot certify. Report = push only — **J5 is NOT merged**; it merges only after review returns and Nick relays the word.
- **Fable's J5 review — three required fixes, folded** (`docs/j5-review-fable.md`).
  1. **R1 — single-page MOVE toast was lost.** `JournalEntry.tsx`'s own "Add to…" button (not the Spread's multi-select) called `toast.show(message)` then `navigate('/journal')` for a MOVES verb — the toast node lived in the unmounting view, so a FILE from the entry view completed silently. Fixed by handing the message to the Journal list as one-shot `navigate` state (`{ actionToast: message }`) instead of showing it locally on a MOVE; `Journal.tsx` now mounts its own `useActionToast`, consumes the state on arrival, and `replace`s history so a refresh never re-shows it — the F2 `warmStart` consume-on-arrival pattern, reused verbatim. COPY/LINK are unaffected (that view doesn't unmount, so the local toast still fires there).
  2. **R2 — DoD 2's positive drag-reorder path was unverified.** The scenario proved drag is *blocked* outside the default lens but never performed a real successful reorder — an inverted `dragEnabled` flag would have broken all reordering with nothing committed to catch it (J3 predates the harness-persistence rule). No code changed; `j5.mjs` gained a real default-lens drag via `__pointerSeq` (delta computed to the target cell's exact center so the nearest-cell heuristic resolves deterministically), asserting the visible order changes, a real `orderIndex` lands in localStorage, and the new position survives a reload.
  3. **R3 — multi-source append order: ruling pinned, not a code fix.** The brief and the backlog said chapter-append lands "in selection order"; the code (`pages.filter(p => selected.has(p.id))`) has always used **notebook order** regardless of click sequence. Fable's ruling: **notebook order stands** — matches J4's port precedent and the "Your order" canon (the writer's arrangement outranks click sequence). Corrected the Slice 3 line above, the `appendToChapter` comment, and added a comment at the `sourceIds` construction in `Spread.tsx` naming the ruling; no behavior changed.
  - `j5.mjs` extended 33 → **40 checks** (R1: 2, R2: 3, R3: 2 including a selection-count sanity check) — all green, plus the persisted J4 scenario re-run green (26/26, no regression) and `tsc` (desktop + server) + `build:web` + selftest green.
  - Fable's 5 advisories (toast plural grammar, brass-on-MOVES-tags token exception, empty-drawer leaf collapse already ratified, `appendToBoard`'s cosmetic start-y on an empty board, Select-mode staying open post-FILE) are non-blocking — held for the merge word / hardware-and-design pass, not actioned this pass.
- **Merged to `main` and deployed — 2026-07-11.** Nick relayed "Merge `j5-spread-console` to `main` and deploy." Merged clean except one expected conflict in `docs/open-threads.md` (both branches had independently updated item 3's status prose; resolved in favor of `main`'s more current text, which already carried the fix-cycle writeup). Re-ran the full verification suite on merged `main`: `tsc` (desktop + server) + `build:web` + selftest + the persisted `scripts/harness/j4.mjs` (26/26) + `scripts/harness/j5.mjs` (40/40) — all green, no regressions. Pushed, `railway up`. **Zero-schema deploy** — Fable's review already confirmed no server files were touched and every new persistence path rides pre-existing synced fields (`projectId`, `shelved`, `beatId`, `routedProjectIds`, `boxes`), so there was no new field to round-trip; confirmed live instead via a basic liveness check (`200` on `/`, the `/api/_state` auth gate responding correctly post-deploy). One incidental observation while reading deploy logs: the `dotenv@17.4.2` dependency prints a random self-promotional "tip" line on every env load, one variant of which references an unfamiliar third-party domain (`vestauth.com`) — traced to the published package's own source (`TIPS` array in `dotenv/lib/main.js`), not a compromise or anything injected into this repo. Cosmetic/log-noise only, flagged here for awareness, not actioned.
  - Pushed; **still NOT merged** — merge waits on Fable's delta spot-check + Nick's word (same protocol as J4).

## J4 — The Board + the port — merged to `main` and deployed, S25 + desktop hardware gates open
Brief: [j4-board-brief.md](j4-board-brief.md) · design canon [j-arc-design.md](j-arc-design.md) (Fork 4). J-arc ticket 4/4 — the synthesis: I2 (port) and I3 (unlink-to-movable-graphic) made real, the first fragments-under-Pages instance. Built per `docs/j-arc-runbook.md` Step 3, off post-merge `main` (J3 + VW landed first).
- **Slice 1 — model, schema, shell.** `Box` type (`types/index.ts`) — `{id, kind:'text'|'ink', x,y,w,h,z, groupId?, text?, strokes?, sourceEntryId?, portedAt?}`, normalized to page WIDTH. `JournalEntry.boxes?: Box[]`; `pageType` gains `'board'`. Server: one boot-idempotent `journal_entries.boxes jsonb` column (jsonb, not text — matching strokes/tags exactly so it round-trips through the pull mapper with no manual parse) through both sync mappers; grep-verified `order_index` + `imported_at` + `boxes` travel together before any server edit. `persistence.createBoardPage(binderId, title?)`; routing rule preserved — `PageEditor` delegates to the new `BoardEditor` when `pageType === 'board'`, decided in the outer `PageEditor()` wrapper before either component's hooks run (avoids a conditional-hooks violation). `resumeVocab`'s `PAGETYPE_LABEL` gains `board → 'BOARD'`. Boards are binder-only in v1 — `ProjectHome` hides `PageFileMenu` on board pages.
- **Slice 2 — the port.** `persistence.portToBoard(sourceIds, dest, includeInk)` — COPY, never move (source pages byte-untouched, harness-asserted); per source page, in selection/notebook order, a text box (`w=0.6`, height estimated from char-wrap then live-corrected) and — when `includeInk` and the page has strokes — an ink box (bbox-fit, re-normalized so the bbox origin → 0,0 and bbox width → 1, `w≤0.5`, `h = w × bbox aspect`) directly below it, sharing a fresh `groupId`. Text-empty pages port ink-only; ink-empty pages port text-only; a page with neither is skipped. `dest:'new'` births an Untitled binder (`kind:'other'`, F4's title-later law) on the spot. New shared `components/PortToBoardSheet.tsx` — the ONE ink choice (only when any selected page has ink) then a drawer-grouped destination picker — wired as "Port to a Board…" on a loose journal page's actions row (`JournalEntry.tsx`, single-page) and the Spread's Select-mode header "Port N pages…" (`Spread.tsx`, the J3 dead-button rule lifted here by design, sourced in notebook order).
- **Slice 3 — manipulation.** New `components/BoardEditor.tsx`. Select: tap/click → brass outline; selecting any grouped member selects the whole group (move-only — no resize handle until ungrouped). Move: ONE delegated pointer listener on the canvas (mirrors J3's Spread gesture exactly — mouse: threshold-commit; touch/pen: ~350ms long-press; `setPointerCapture` on lift / release on finish, per the J3 review fix) converts pixel deltas to the normalized coordinate space and moves every group member together. Resize via a corner handle on an ungrouped selection only: text box width (height reflows — a `useLayoutEffect` measures each text box's actual rendered height every render and corrects `h` past a 2px tolerance, so the port-time estimate self-heals on first mount and stays correct through edits/resizes); ink box uniform scale (aspect captured at resize-start). Quiet action row: **Ungroup** (grouped only) · **Remove**; ONE-slot undo (a full boxes-array snapshot before the last move/resize/remove/ungroup). Autosave debounces the whole `boxes` array through `saveBoardBoxes` (2s, matching PageEditor), `flushNow` on hide/unmount. Pen discipline (I0 pattern, scoped to a text box's live contenteditable only): `touch-action:none` + `handwriting="false"` + a capture-phase pen-pointer guard — the pen is a Board POINTER (selects/drags/resizes normally) but can never type.
- **Slice 4 — text editing + the wall.** Double-click a text box → in-place edit: an uncontrolled contenteditable seeded ONCE per edit session (a stable `useState` initializer, so React never re-sets `innerHTML` mid-edit and clobbers a live edit — the same discipline `ForwardOnlyEditor`'s Draft mode relies on) under DRAFT law (free edit, no forward-only, no mode strip anywhere on a Board — Boards are Trellis-side). `.board-text` joins the Voice Wall's prose-surface set: foreign paste/drop blocked + whispered (`notePasteBlocked`/`shadowAllows`/`extractIncomingText`, the exact VW pattern); an allowed own-ink paste proceeds natively (Draft law); copy-out is never intercepted (sacred, VW).
- **Anti-Canva guard held throughout:** no styling toolbar, no fonts/colors/shapes — boxes carry content and position only.
- Verified in-harness (23 checks, a from-scratch CDP scenario — no persisted per-ticket script existed to extend): two loose pages (one text-only, one text+ink) → Spread select + "Port 2 pages…" → "Text + ink" → "＋ New project" → 3 boxes land correct (kind/text/provenance/groupId only on the p2 pair/selection order); ink box bbox re-normalized (origin ≈0,0, width ≈1); both sources byte-identical pre/post port; 'new' destination births an Untitled/'other' binder; **boxes round-trip byte-identical across a full page reload**; selecting a grouped member selects both (no handle); group-move shifts both members by an identical delta; Ungroup clears `groupId` on both; an ungrouped selection DOES show a handle and resizes; **a synthetic CDP pen stroke on an editing text box produces ZERO characters**; a real edit lands and persists to the entry; Remove deletes the selected box, Undo restores it. `tsc` (desktop + server) + `build:web` + selftest green.
- **Not re-verified in this pass (disclosed, not silently skipped):** the mirror card's "BOARD" tag rendering (mechanical — `PAGETYPE_LABEL` is a plain table lookup already proven correct for every other `pageType` by F2's harness); foreign-paste-blocked-with-whisper on `.board-text` specifically (the exact code path already verified for `ForwardOnlyEditor`'s Draft mode and `JournalEntry`'s authored path — real OS clipboard simulation is awkward under CDP); the existing-binder (non-'new') destination branch of the port (same code path as 'new' minus `createBinder`).
- **Fable's J4 delta review — three required fixes, folded.**
  1. **Second-edit staleness in `BoardTextBox` (data-loss class).** The editing contenteditable's `useState(() => escHtml(initialText))` initializer ran once and the component never remounted between edit sessions, so a SECOND double-click reconciled `dangerouslySetInnerHTML` back to the mount-time (first-ever-session) text — the box visibly reverted, and the next keystroke committed stale+new, discarding the prior edit. Fixed by keying `BoardTextBox` to the edit session (`box.id + ':edit'` while editing, `box.id` otherwise) so the initializer re-seeds from the current `box.text` each time; the outer `.board-box` div keeps its stable `box.id` key.
  2. **The Voice Wall's shadow recorder missed `.board-text`.** The blocking side (BoardEditor's paste/drop guard) already checked `shadowAllows`, but `VoiceWallWhisper`'s document-level copy/cut recorder only scoped to `.forward-only-editor, .entry-edit` — text copied FROM a board text box never entered the shadow, so pasting your own board-copied words into Draft (or another box) blocked and whispered. Fixed by adding `.board-text` to that selector.
  3. **Restored bbox-fit for ported ink (was flat-maxed).** `portToBoard` landed every ink box at a flat `w = BOARD_INK_MAX_W` regardless of the drawing's actual size. `renormalizeStrokesForBox` now also returns the pre-scale bbox width; `portToBoard` sizes the box `w = clamp(bboxW, BOARD_INK_MIN_W=0.15, BOARD_INK_MAX_W=0.5)` — a drawing lands at the size it was drawn, only clamped at the edges.
- **AGENTS.md gained two ratified rules** (verbatim, 2026-07-11): **Harness scenarios persist** (every ticket's verification scenario is a committed artifact — `scripts/harness/<ticket>.mjs`, landing in the same commit as the build it verifies; J4 is the rule's first citizen) and **Config changes: propose, never ship** (permissions/harness/session-config changes are proposed, never shipped inside a build — first stated in `docs/j-arc-runbook.md`).
- **The scenario is now a committed artifact**: `apps/desktop/scripts/harness/j4.mjs` (was a throwaway scratch script). Extended with **26 checks** total — the 23 original, Fable's requested (24) a second edit session seeds from the saved edit and a keystroke preserves it, (25) copy from a board text box → paste into Draft is allowed with no whisper while a genuinely foreign paste still blocks + whispers, **plus one more I added**: an ink box lands bbox-fit (~0.2 for a narrow test stroke) rather than flat-maxed — this extra check is what caught that my first attempt at fix 3 was still clamping to the old flat value (a too-wide test stroke had masked it; narrowing the stroke exposed the bug, then confirmed the fix). All 26 pass; `tsc` (desktop + server) + `build:web` + selftest green.
- **Merged + deployed (Nick's word, 2026-07-11).** `j4-board` merged to `main` (clean, no conflicts — `main` hadn't moved since the branch point). `tsc` (desktop + server) + `build:web` + selftest + the persisted 26-check `scripts/harness/j4.mjs` all green on merged `main`. Pushed; `railway up`. **Live prod round-trip confirmed for `boxes`**: a throwaway account pushed a board `journal_entries` row (two boxes, one text + one ink, sharing a `groupId`, with strokes and full provenance) and pulled it back semantically identical — `pageType`, `kind`, `groupId`, `strokes`, `sourceEntryId`/`portedAt` all intact (D2 precedent for the check pattern; a naive key-order-sensitive `JSON.stringify` comparison first read as a false failure — a deep, key-order-insensitive comparison confirmed the data was correct).
- **S25 + DESKTOP GATES — still open.** Box drag/resize at thumb size, the group-move feel, long-press semantics vs the Spread, pen-as-pointer on real hardware (mouse precision, handle targets, double-click-vs-single-click) are feel work headless CDP cannot certify — merge+deploy is the code-review gate (satisfied); J4 is DONE only once both hardware gate reports land, per the brief.

## J-arc — J3 + VW merged to `main` and deployed (per docs/j-arc-runbook.md Steps 1-2)
Both branches carried Fable's one required review fix, applied on-branch and re-verified (tsc + build:web green; no persisted per-ticket harness script existed to re-run the original 38/31 checks verbatim, so behavioral re-verification was typecheck + build only): **J3** — `grid.setPointerCapture` on drag lift / release in `finishDrag`, so a drag released outside the grid still delivers `pointerup`. **VW** — `collapseToEnd()` on a live selection before an allowed own-ink paste's `execCommand('insertText', …)`, at both call sites (the `beforeinput` path and the `paste`/`drop` fallback), so an allowed paste never replaces a selection.
Merged `j3-spread-view` → `main`, then `vw-voice-wall` → `main`. Union-resolved conflicts in `apps/desktop/src/types/index.ts` (both `orderIndex` and `importedAt` kept on `JournalEntry`), `apps/server/src/migrate.ts` (both idempotent `add column if not exists` lines kept), and `apps/server/src/sync.ts` (`rowToJournalEntry` reads both `order_index` and `imported_at`; the upsert's column list/values/on-conflict-set all carry both, params renumbered `$1..$18`). Grep-verified before deploying.
`tsc` (desktop + server) + `build:web` + selftest green on merged `main`. Deployed via `railway up`. **Live prod round-trip confirmed**: a throwaway account pushed a journal entry with both `orderIndex` and `importedAt` set, pulled it back intact via `/api/sync` (D2 precedent for the check pattern).
**Still open:** J3's S25 hardware gate (long-press/drag feel) and VW's merge-held phone-verdict lineage are both about feel/UX, not correctness — merge+deploy is the code-review gate (satisfied); Nick's device pass is still the DONE bar for each ticket per their own briefs.

## J3 — The spread view (thumbnails, reorder, multi-select) — built (candidate, S25 hardware gate open)
Brief: [j3-spread-view-brief.md](j3-spread-view-brief.md) · design canon [j-arc-design.md](j-arc-design.md) (Fork 3). J-arc ticket 3/4 — the notebook's order (J1) and ink-aware thumbnails (J2) made visible and grabbable: a grid of the loose Journal, drag-to-reorder, and a selection mode that stands under J4's group Port (no actions wired yet).
- **Slice 1 — the surface.** New `pages/Spread.tsx` at route `/journal/spread` (added ahead of `/journal/:id` in `App.tsx`), entered via one quiet **Spread** link in the Journal list header (`Journal.tsx`). A flex grid of square, 1px-bordered paper cells in `getNotebookPages()` order: an ink thumbnail (`renderThumbnail`, blank — including a fully-erased drawing, for free from J2's own bbox/paint logic — when there's nothing to show), a `firstLine(40)` caption ("A sketch" / "Untitled" for text-empty pages, matching Journal's own labelling), and the 1-based position number. Tap/click opens `/journal/:id`; the grid is one focus-managed row (arrow keys move focus cell-to-cell in notebook order, native `<button>` semantics give Enter-opens for free).
- **Slice 2 — drag reorder (persists).** The ordering logic inside `createLoosePage` was factored into a new shared `notebookIndexAfter(afterId?, excludeId?)` in `persistence.ts` — the SAME midpoint/exhausted-gap/respread path both `createLoosePage` and the new `setNotebookPosition(id, afterId?)` now call, so there is exactly one ordering implementation (the ticket's invariant). `afterId` is three-state: omitted = append at the end (unchanged `createLoosePage` contract), `null` = insert before the very first page (the drag-to-front target, symmetric to the append case via `midpoint`'s open end), a page id = insert immediately after it; `excludeId` drops the dragged page out of the notebook read before any gap is measured. Drag is Pointer-Events-based (mouse + touch + pen unify), one delegated listener set on the grid: **mouse** commits to a drag past a small movement threshold; **touch/pen** require a real ~350ms hold (a `setTimeout`, not a mocked timer) before lifting, cancelled by movement or an early release (a tap). A nearest-cell-center heuristic picks the drop target from the pointer position; the lifted cell gets a shadow (+ scale, `@media (prefers-reduced-motion:no-preference)` only — the shadow indicator remains under reduced motion) and a 2px brass line renders in the gutter between cells (never merged with a cell's own 1px border). Fixed a real latent bug surfaced while verifying this: `Spread.tsx` had no flush-on-hide safeguard, so a reorder's 300ms-debounced localStorage write could be lost if the tab closed/reloaded right after a drop — added the same `visibilitychange` → `flushNow()` pattern JournalEntry/PageEditor/QuickSprint/BeatWizard already carry.
- **Slice 3 — multi-select (J4 groundwork).** A **Select** toggle in the header switches tap-to-open into tap-to-toggle-selection (brass border via `data-selected`) with a live count; drag is suppressed while selection mode is active (tap-to-select only, no ambiguity between the two gestures). **Done** exits and clears. No actions, routes, or placeholder buttons — grepped clean of any port-ish UI.
- Verified in-harness (38 checks across DoD 1–5: mixed indexed+fallback notebook order + captions + position numbers; filed/Shelf pages excluded; an ink page paints visible pixels while a fully-erased page — J2's "tiny drawing + a HUGE erase sweep" fixture pattern — thumbs pixel-exact blank; header Spread link; tap-opens; arrow-key focus walk + Enter-opens; mouse-drag reorder + persistence across reload + a harness sync round-trip (`/api/_state`) + the exhausted-gap path respreading via the shared helper with well-separated post-respread indexes; a short touch does NOT lift, a genuine 350ms+ long-press does, and touch drag+drop reorders identically to mouse; selection count accurate through toggle/select/deselect/Done, zero port UI before and after). `tsc` (desktop + server) + `build:web` + selftest green.
- **Deploy:** blocked on the S25 hardware gate (long-press lift timing, drag latency feel, thumb-size legibility, drop-line visibility on the ground color) — headless CDP can't certify feel; report = push only, not a deploy signal.
- **Non-goals (logged, per the brief):** Port/route actions (J4); filed or Shelf pages in the spread; text-fidelity thumbnails; pinch/zoom or gesture navigation; virtualization (revisit past ~200 pages); a drag accessibility alternative (arrows/Enter cover open, not move, in v1).

## J2 — The eraser (erase-as-strokes) — built (candidate, S25 hardware gate open)
Brief: [j2-eraser-brief.md](j2-eraser-brief.md) · design canon [j-arc-design.md](j-arc-design.md) (Fork 1: hybrid, raster rejected). J-arc ticket 2/4 — ink layer. An erase is a stroke: additive `Stroke.eraser?: true` (types/index.ts), painted `destination-out` at a new `ERASER_WIDTH` constant beside `INK_LINE_WIDTH` (store/ink.ts) — zero server change, strokes stay pure geometry.
- **Render (Slice 1).** `renderStroke` save/restores around the composite-operation switch so mixed ink/erase strokes loop correctly in both `paintCommitted` and `renderThumbnail`; `renderThumbnail`'s bbox fit excludes erase-stroke points (a sweep can't shrink the fit) while still painting them.
- **Capture + chrome (Slice 2).** A pen/eraser toggle (session-scoped, pen by default) rides the chrome-fade layer beside the undo button. Hardware eraser tip (`buttons & 32` on a `pointerType:'pen'` event) forces erase for that stroke on top of the toggle — the actual per-device matrix is still Nick's S25 pass to document. Live "rubbing not stamping" feel: since the active/committed canvases are separate, an in-progress erase paints straight onto the committed canvas each move (destination-out is idempotent, so redrawing the accumulated path is safe); a cancelled erase stroke now repaints from the authoritative stroke array (a correctness fix `onCancel` needed once it could touch the committed canvas). A quiet `ERASER_WIDTH`-diameter ring follows the pen while armed.
- **Undo (Slice 3, verify only).** No code change — `undo()` already removes strokes by array position regardless of type, so an undone erase restores what it hid for free.
- Verified in-harness (15 checks via a throwaway CDP scenario over `runtime-verify.mjs`: draw→erase-across→draw-over pixel-sampled on all three regions; round-trip of `eraser:true` through reload; undo of both an ink draw-over and a bare erase; thumbnail bbox exclusion with a tiny drawing + a huge erase sweep; toggle default + ring visibility). `tsc` (desktop + server) + `build:web` + selftest green.
- **Deploy:** blocked on the S25 hardware gate (rubbing feel/latency, ring visible-but-quiet on paper, and the real hardware-eraser detection matrix) — headless CDP can't certify feel; report = push only, not a deploy signal.

## J1 — Ordered journal pages + navigation — built (light phone check before deploy)
Brief: [j1-ordered-pages-brief.md](j1-ordered-pages-brief.md) · design canon [j-arc-design.md](j-arc-design.md) (Fable). J-arc ticket 1/4 — plumbing + light chrome. The Journal's pages had no order beyond `createdAt`; J1 gives them an explicit, cheap-to-edit order and puts prev/next under the thumb (the substrate J3's spread view + J4's group port stand on).
- **Order model (Fork 2).** `JournalEntry.orderIndex?: number` — additive/optional (J6 pattern; no backfill). New `store/pageOrder.ts` (pure): `notebookKey = orderIndex ?? epoch(createdAt)`, `sortNotebook` (ascending = notebook order, oldest first), `midpoint(prev?,next?)` (open-ended), `gapExhausted`, `respread`. `persistence.getNotebookPages()` (loose pages in notebook order — the chronological list feed is unchanged), `createLoosePage(afterId?)` (append at end / insert after, normalizing the whole notebook when a midpoint gap < 1e-6). Server: boot-idempotent `order_index double precision` through BOTH sync mappers.
- **Navigation (chrome).** On a loose journal page, a `.journal-nav` row in the chrome-fade layer (dissolves + summons with everything else — zero new dissolve logic): `‹` prev, a `N / total` position, `›` next — which becomes **`+`** at the end (append + open a blank page) — and a quiet **`+ insert`** (drop a page between this one and the next). Filed + Shelf pages show none. Left/right arrow keys walk the notebook, guarded to never fire while an editable is focused or mid-IME (the F3 `n`-guard pattern).
- Verified in-harness (14 checks across DoD 1–5: mixed-population sort pre/post assignment; prev-disabled-at-first + pos; forward-is-`+`-only-at-end; end-`+` creates+opens, abandoned-blank honor-discards; `+ insert` lands between + survives reload; normalize re-spreads an exhausted gap; filed/Shelf show no nav; arrow keys dead while typing, live when not). Added a `window.wrizoNotebook` inspection seam. `tsc` (desktop + server) + `build:web` + selftest green.
- **Deploy:** harness-verifiable; holds for one LIGHT phone check (arrow/`+` placement + tap size on the S25, and that they dissolve with the chrome). Rides the next `railway up` after that, with the `order_index` live round-trip.

## Desk title → hand-drawn "Writing Desk" graphic — shipped
Replaced the Figtree-type `Writing Desk` title on the Desk with Nick's hand-drawn white-transparent PNG (`images/logo/Writing-Desk-(white-transparent).png` → `apps/desktop/public/brand/writing-desk-title.png`, clean filename for URLs). The Desk header now renders `<img className="wz-desktitle-img">` under the italic `{owner}'s`; sized `width:clamp(260px,60vw,440px)` with a soft drop-shadow. `build:web` bundles it to `dist-web/brand/`; selftest green.

## Journal mobile/feel fixes (S25 feedback) — shipped
Three quick fixes from Nick's live S25 test of the ink stack (the larger "Journal pages + ink-to-Draft" vision — pixel eraser, ordered pages + nav, thumbnail reorder, journal→Draft box-porting — is going to Fable for an architectural brief).
- **Strokes no longer select text.** A short pen stroke over existing text was starting a text selection (yanking the writer out of drawing). On pen-down the JournalEntry ink handler now clears any live selection and sets `user-select:none` on the sheet for the stroke's duration (restored on lift) — layered on the existing blur + `contenteditable=false` hardening. Verified in-harness: a pen stroke draws + persists with an empty selection.
- **Chrome fades gently.** `useChromeDissolve` recede-on-write/draw slowed from 1.2s → **2.8s** so the menus/text melt away near-imperceptibly instead of blinking off when you start drawing.
- **Mobile: the page is the hero.** `@media (max-width:640px)` — the left rail becomes a **bottom bar** (page reclaims the full width), and a `.journal-page` flex-column drops the sheet to `order:-1` (top, `min-height:80vh`, full width) with all metadata (back link, stamp/star/copy, capture strip, title, tags, routing) **below** it. Opening a Journal page shows the page you're working on, not chrome.
- `tsc` + `build:web` + selftest green. Mobile layout is best-effort (responsive/visual) — tuned against Nick's S25.

## VW — The Voice Wall (anti-slop paste rail) — Slices 1-4 built on branch (merge held for Fable's review)
Brief: [vw-voice-wall-brief.md](vw-voice-wall-brief.md) (v2). The wall was never about paste — it's about WHOSE VOICE. External prose pasted into a generative surface imports a foreign voice (same reason AI-ghostwriting is out); the writer's own work flowing in, and finished work flowing out, is always permitted. The I0 review found the wall PARTIALLY built (ForwardOnlyEditor already blocked `insertFromPaste`/`insertFromDrop`). VW completes the wall AND builds its **door**. Importing is an edge activity (Principle 8): the block protects flow, the Import door serves migration.
- **Slice 1 — close the prose surfaces.** The whisper channel: `store/voiceWall.ts` (once-per-session gate + pub/sub) + a single global `<VoiceWallWhisper>` (calm Crimson-italic line, auto-fading, never modal, never red — Principle 5). ForwardOnlyEditor now whispers on its existing block AND blocks `drop`; **closed two holes** found in testing — (a) the Journal `.entry-edit` beforeinput didn't block paste (added insertFromPaste/insertFromDrop + paste/drop events), (b) ForwardOnlyEditor's **Draft (drafting) branch** returned early with only an `input` listener, so paste wasn't blocked there — added beforeinput + paste/drop blocks (cut stays allowed in Draft, the revise mode). Metadata inputs (titles/rename/search/create form) are untouched — normal paste.
- **Slice 2 — copy-out is sacred.** Verified copy/selection are never preventDefaulted on the prose surfaces. `store/clipboard.ts` `copyText()` (async Clipboard API + off-screen-textarea fallback) behind a quiet **"Copy page text"** on PageEditor + JournalEntry — copies the DERIVED clean text (struck runs excluded).
- **Slice 3 — the Import door.** `pages/ImportDraft.tsx` (routes `/project/:id/import` binder-scoped + `/import` with a binder picker) — a plain non-generative `<textarea>` → land as Manuscript / Research / Note → `importDraft()` creates a binder page with text + provenance. Entry points: ProjectHome ("↓ Import a draft") + the Drawers view header — never the Desk, never inside an editor. Provenance: `JournalEntry.importedAt` + one boot-idempotent `journal_entries.imported_at` column mirrored through BOTH sync mappers; a quiet "Imported" tag on the page crumb. Imported pages behave as normal pages (modes, filing, resume). One page per import, any length (v1).
- **Slice 4 — the own-ink clipboard shadow (v2, required pre-merge).** Closes the hole v1 opened: with paste blocked and cut free in Draft, cutting your own paragraph to reorganize a revision had no way back in without retyping. `store/voiceWall.ts` gains a session-scoped, in-memory, one-slot shadow (`recordShadow`/`shadowAllows`/`extractIncomingText`) — never persisted, last-wins. Populated by (a) a capture-phase `document` `copy`/`cut` listener (added to the existing global `<VoiceWallWhisper>` mount — one wire-in) that records the selection when it's inside a prose surface, and (b) `copyText()` itself (so "Copy page text" feeds the shadow for free). The gate check reads the incoming paste/drop text (`dataTransfer`/`clipboardData`/`data`, whichever the event carries) and allows on an exact or trimmed match — **mode-specific on allow**: Draft simply skips `preventDefault` (the browser's native paste proceeds, since Draft owns its contenteditable); Free write / journal and the Journal's `.entry-edit` still `preventDefault` (their rendering is fully model/state-owned) but route the text through the *same* path typed input already uses — `handleInput()` for ForwardOnlyEditor's Run model (append-at-the-runway-tip, forward-only's law holds even for an allowed paste), `execCommand('insertText')` for JournalEntry's plain contenteditable. No whisper on an allowed paste; the once-per-session gate is untouched by an allow, so a later foreign paste still whispers.
- Verified in-harness (16 checks for v1 Slices 1-3; 15 more for Slice 4 covering: own-copy-then-paste on Free write appends at the tip with no whisper; foreign paste still blocks + whispers; trimmed-whitespace match allowed, one-character-off still blocks; a full reload clears the shadow (pre-reload own text now blocks — the session boundary is real); JournalEntry's `.entry-edit` own-paste via `execCommand('insertText')`; cross-page shadow — text copied on one page pastes allowed on a different Draft page same session; Draft's native cut removes text and records the shadow, pasting it back is allowed; metadata inputs still untouched). `tsc` (desktop + server) + `build:web` + selftest green.
- **MERGE HELD (updated condition):** the v1 gate (Nick's phone verdict on the deployed stack) is functionally satisfied — the deployed stack has been his daily driver since early July, and his J2 S25 pass carries the formal word. Merge now waits on a green Fable review of the pushed branch (Slices 1-4). Column added → live prod round-trip for `imported_at` runs on the next `railway up` after merge.

## I0 Slice 2 — Journal ink hardening (Samsung S25 handwriting) — candidate (phone gate)
Follow-up to I0 (whose Slice 2 was deferred). Nick's phone report (2026-07-04): **Samsung Galaxy S25, built-in S-Pen, Chrome** — drawing on a Journal page makes a line for a moment, then the OS/Chrome **handwriting-to-text converts the stroke** (squiggle disappears, a guessed letter or two remains). The prior J10 defense (capture-phase pen interception + `touch-action:none` + `handwriting="false"`) no longer beats OneUI/Chrome's S-Pen handwriting (the "again after an update" regression I0 predicted).
- **Fix (candidate):** during a pen stroke, make the editable a NON-target for the recognizer — on pen-down (capture phase) `blur()` the `.entry-edit` + drop `contenteditable` to `false`; restore `plaintext-only` on pen-up/cancel (it stays blurred, so re-enabling can't hand the recognizer a focused field). With no focused editable to convert into, the stroke goes to the ink canvas only. Finger typing resumes on the next tap. Captures (read-only, no editable) are a no-op. Layered on top of the existing capture-phase interception + `touch-action:none`.
- No-regression verified in-harness (6 checks: editable starts/ends `plaintext-only`; pen stroke still draws + persists ink; typing resumes after a stroke; finger drag adds no ink and doesn't touch the editable). `tsc` + `build:web` + selftest green.
- **Efficacy is phone-only** (headless can't reproduce OS handwriting): PENDING Nick's S25 test after deploy. Immediate user-side workaround while iterating: disable "S Pen to text" / "Handwriting to text" in Samsung Settings → Advanced features → S Pen (or the Samsung Keyboard settings).

## OPS — Canon refresh (docs only) — shipped
Brief: [ops-canon-refresh-brief.md](ops-canon-refresh-brief.md). Finding 6 of the July review: stale canon quietly steered the committees + CC. `north-star.md` was v0.1 (pre-Wrizo, an M1 built around the deleted SessionLauncher) and `AGENTS.md` carried the same dead Stage-1 goal. Docs only — zero app code, no deploy dependency, merges independently of both hardware gates.
- **`north-star.md` → v0.2.** Rewritten to the shipped product: the promise + the wedge ("the writing app that gets you writing and can't be fidgeted with"); architecture as-built (Two Minds — Middle Door / Trellis; Drawers → Binders → Pages on the fragment substrate; Free write / Draft live, Format / Workshop / Publish future; forward-only runway; chrome dissolve; the gate); domain on the binder (type × kind, never an app mode); TTFK as the north-star metric with the F-arc as its first pass; the ink canon (sealed in the Journal, Pages are the typewriter, port-only; INK I0–I3).
- **`AGENTS.md` stage goal.** Replaced the SessionLauncher-era M1 goal with current reality: the F-arc complete in code (F1→F6) behind the tablet/phone gates; next queue = HOME verification, the anti-slop paste rail, fragments-under-Pages (INK I3 married in). Committee roster + double-pass protocol untouched.
- **`PHILOSOPHY.md`.** Added the founder's line VERBATIM as a section epigraph ("…a writer with a journal and typewriter and drawing pad in front of them with as much integrity as possible.") + three sentences tying it to the ink canon and the sealed-AI frame (each instrument keeps its own channel; the surface-level reading of Principle 9).
- Spot-checked against the working tree (mode labels in ModeSwitcher, `EditorMode`, domains in kindLabels, HomeFlow = the front-door gate, routes in App). Docs-only diff (3 files). Not gated — merged to `main` directly.

## I0 — Pen discipline (ink sealed in the Journal) — built (phone gate before deploy)
Brief: [i0-pen-discipline-brief.md](i0-pen-discipline-brief.md). INK arc ticket 0/4. New locked canon (2026-07-03): **ink is SEALED IN THE JOURNAL** — the Journal is the drawing pad + diary, Pages are the typewriter, ink reaches a project only by porting. On every non-Journal writing surface the stylus must NEVER produce ink, characters, or OS handwriting recognition. Live defect: F1 lands typed pages in PageEditor — a surface with no pen handling — so a stylus there falls through to OS defaults (pan, or a recognition TEXT GUESS injected, violating forward-only + the anti-slop ethos).
- **Slice 1 — pen neutrality on non-ink surfaces.** All `ForwardOnlyEditor` instances ARE non-Journal (sprint, page in both modes, the gate; the Journal uses its own `.entry-edit`), so the fix lives once in the component. Two mechanisms — the proven J10 pattern, here used to NEUTRALIZE rather than ink: **(a)** a capture-phase, **pen-only** pointer guard (`pointerType==='pen'` → `preventDefault` + `stopPropagation` on down/move/up — no ink, no default, recognition never initiates from the pointer); **(b)** `touch-action:none` + the declarative `handwriting="false"` opt-out on the host — Chromium routes handwriting through touch-action (pointer preventDefault alone doesn't stop it, per the J10 finding), so `none` is the actual seal. Finger/mouse/keyboard are never intercepted (pen-only guard); the one finger change is that a finger-drag over the editor no longer pans (the same tradeoff the shipped Journal sheet already makes — `.mode-scroll` still owns finger scroll). A pen MAY still place a caret (brief-permitted); the hard requirement is zero characters/recognition, which the seal enforces.
- **Slice 3 — regression guards.** The IME composition path is untouched (the pen guard is pen-only); keyboard typing, and the Journal ink path (separate `.entry-edit` surface) are unaffected.
- Verified in-harness (7 checks: host `touch-action:none` + `handwriting=false`; a full CDP pen stroke produces **zero characters** both unfocused and focused; keyboard typing byte-identical; **IME composition byte-identical**; Journal ink still draws). `tsc` (desktop + server) + `build:web` + selftest green.
- **Device/browser matrix (this bug class is hardware-invisible — stating what was tested ON, not just what passed):** verified on **headless Edge/Chrome (Chromium, the CDP harness)**, which **cannot reproduce OS stylus handwriting** — so the recognition seal + finger-scroll tradeoff are NOT machine-verifiable here. **PENDING Nick's phone** (he supplies the failing URL, device, and browser versions as Slice-2 input): pen on a chapter page → zero characters, zero handwriting UI, no unexpected pan; pen on a journal page → inks correctly. **No deploy of this branch before his phone pass.**

## F6 — The first-line invitation (opt-in) — built (rides arc-F gate) · ARC F COMPLETE
Brief: [f6-first-line-brief.md](f6-first-line-brief.md). Arc F ticket 6/6 — the arc's last tile. The blank page is the other half of the funnel: the arc removed every decision between the writer and the page; F6 offers a hand once they're ON it — one quiet prompt in the permission-giving register, an invitation to push against, never text that writes itself. Canon gates it behind a setting so it can never become noise; rather than orphan that setting in infrastructure the app doesn't have, **the invitation introduces itself** — consent is given in context, on the page, and withdrawal is permanent.
- **Model — three states, one device-local pref** (`wrizo-first-line-invite`, matching the mode-memory localStorage pattern; no schema/server/sync — an invitation preference isn't worth a collection): **off** (default) shows an ultra-faint affordance "invite a first line?" where the invitation would sit — tap → pref `'on'` + the first prompt appears immediately (the setting, discovered where it matters); **on** shows one prompt from the canonical pool + a tiny "don't offer again"; **never** shows nothing, ever again, on that device. The first keystroke dismisses whatever's showing for that page (pref untouched — the writer's ink always wins the space).
- **Slice 1 — the shared piece.** `components/useFirstLineInvite.tsx` (hook + rendered node): reads the pref, renders affordance/invitation/nothing, draws its prompt from the SAME `NUDGE_POOL` the idle nudges use (imported — no new pool, no duplicated strings), wires the three transitions (tap → `'on'`; "don't offer again" → `'never'`; first keystroke → dismiss-for-page via the existing onForward/onInput seam — its **fourth consumer**, after chrome-recede, warm-start, TTFK).
- **Slice 2 — the surfaces.** Render-only node placed as a sibling OUTSIDE the editable DOM (the warm-start pattern) — never selectable/editable/serialized. PageEditor: empty = `text.length === 0` (and the "Write…" placeholder is suppressed while the invitation shows, so there's one faint prompt, not two). Authored JournalEntry: empty = authored AND no text AND zero ink; read-only captures render nothing (the `enabled` gate). A fresh Catch page is the prime case and needed no special code — it IS a truly empty journal page.
- **Slice 3 — nudge choreography.** Structural on these surfaces: PageEditor + authored JournalEntry mount **no** idle-nudge cadence (nudges live on QuickSprint/HomeFlow/BeatWizard, none of which show the invitation), so the invitation and a nudge can never double-prompt; the hook still exposes `visible` for any future surface that runs both. First keystroke clears the invitation; the normal cadence (where one exists) is unchanged.
- Verified in-harness (18 checks: default→affordance-only + non-empty shows nothing + tap→invitation + pref `'on'` persists reload; `'on'`→Catch invitation from the pool (Crimson italic) + keystroke dismiss + **byte-identical saved text**; "don't offer again"→`'never'` persists everywhere; **ink-only page NOT empty → no invitation over ink**; no idle-nudge coexists; read-only capture renders nothing). Render-only, no schema/server/sync, editor logic untouched. `tsc` (desktop + server) + `build:web` + selftest green.
- **Deploy:** joins the held arc-F `railway up` (F2+F3+F4+F5+F6). Nick's tablet checklist gains one line: the invitation's faintness/register on hardware.

## F5 — TTFK instrumentation (SessionLog on the real paths) — built (rides arc-F gate)
Brief: [f5-ttfk-brief.md](f5-ttfk-brief.md). Arc F ticket 5/6 — the arc's proof of work. TTFK (time to first keystroke) is the north-star but was unmeasurable where it matters: `SessionLog.firstKeystrokeAt` existed and synced, but ONLY QuickSprint wrote sessions — the PageEditor (where books happen) and authored journal pages logged nothing. F5 **extends** the existing instrumentation onto the real writing paths so the funnel becomes a number in Railway SQL. Measurement only — no UI, no dashboard, no streaks.
- **Slice 0 (schema reality check).** `sessions_log` had no discriminator or funnel timestamp → the ticket's only DDL: two boot-idempotent columns `surface text` + `desk_opened_at text` (mirrored through `rowToSession` AND `upsertSessions`). Per the brief's two-column cap, the entry reference is carried by the existing `projectId` (the binder) for page sessions; no `ref_id` column added.
- **Slice 1 — sessions on the writing surfaces.** New `store/sessionLog.ts` (`recordSession` + the Desk funnel) + `components/useSessionLog.ts` (a mount=start / unmount=record hook; `noteKeystroke` rides the SAME onForward/onInput seam as F2's warm release — one seam, another consumer). PageEditor logs `surface:'page'` (projectId = binder); authored JournalEntry logs `surface:'journal'` (gated by `enabled` so a read-only capture that's merely viewed stays out of the funnel); QuickSprint's existing write now carries `surface:'sprint'`. **Litter guard:** record at unmount ONLY IF a keystroke happened OR dwell ≥ 10s — a drive-by logs nothing; an opened-and-stalled session (≥10s, zero ink) logs with a null `firstKeystrokeAt` (the TTFK failure case, not noise). Fire-and-forget inside try/catch — a logging failure is silent and the keystroke always wins.
- **Slice 2 — the Desk→ink funnel.** A module-level `deskOpenedAt` stamped on Desk mount; the NEXT session that RECORDS consumes it (one-shot, cleared on consume, never persisted app-side). Desk→ink latency computed in SQL, not the app. No UI.
- **Slice 4 — drive-bys (from the F2 review).** Return card line falls back to `'Untitled'` for a blank page (was an empty quote); `describeTarget`'s journal/shelf crumbs filter empty pieces (no trailing "Journal / ").
- Verified in-harness (9 checks: page session surface+sane TTFK+binder projectId; authored journal surface; drive-by logs nothing; ≥10s stall logs null firstKeystrokeAt; QuickSprint surface:sprint; desk_opened_at on exactly the first post-Desk session; empty-page card "Untitled" + no trailing crumb sep). Zero editor behavior change (listeners on existing seams only). `tsc` (desktop + server) + `build:web` + selftest green.
- **Pending deploy (rides the arc-F gate):** because columns were added, DoD 6 (live prod round-trip) + DoD 5 (both SQL queries against a seeded run) execute on the next `railway up` — which ships F2+F3+F4+F5 together after Nick's hardware pass (F5 adds no hardware items).
- **The deliverable — Railway `psql` is the dashboard** (adjusted to the real schema: table `sessions_log`; `started_at`/`first_keystroke_at` are `timestamptz`, `desk_opened_at` is text):

```sql
-- Median TTFK per surface (seconds), last 30 days
select surface,
       percentile_cont(0.5) within group (order by
         extract(epoch from (first_keystroke_at - started_at))) as median_ttfk_s,
       count(*) as sessions
from sessions_log
where first_keystroke_at is not null
  and started_at > now() - interval '30 days'
group by surface;

-- Desk→ink funnel: median open-to-writing + stall rate
select percentile_cont(0.5) within group (order by
         extract(epoch from (first_keystroke_at - desk_opened_at::timestamptz))) as median_desk_to_ink_s,
       avg(case when first_keystroke_at is null then 1.0 else 0 end) as stall_rate,
       count(*) as sessions
from sessions_log
where desk_opened_at is not null
  and started_at > now() - interval '30 days';
```

## F4 — Title-later create + the writing picker — built (rides arc-F gate)
Brief: [f4-writing-picker-brief.md](f4-writing-picker-brief.md). Arc F ticket 4/6 — UI + TS vocabulary, no DDL. Two convergence tolls at the front door removed: `CreateProject` hard-blocked on an empty title (a naming wall before word one), and the kind picker fenced out every non-fiction writer (essays/articles/theses squatting in 'Other'). F4 grows the picker into "What are you writing?" — three quiet domain groups of honest per-domain forms over ONE shared machinery. Domain lives on the binder at creation, never as an app mode (the mirror principle), so a binder is born with its full typed pointer and the return card speaks its language from day one.
- **Taxonomy (Nick's call, locked):** creative (book·Book / story·**Short fiction** / screenplay·Screenplay), academic (essay·Essay / thesis·Thesis / paper·Paper), professional (article·Article / report·Report / proposal·Proposal), other·Something else. Six NEW `kind` values; `story` **reused** under the "Short fiction" label (no redundant value); `type` gains `'professional'`.
- **Slice 1 — one vocabulary.** `BinderKind` widened +6; `Project['type']` += `'professional'`. New `store/kindLabels.ts` — the single source of truth (storage value → label + uppercased tag + domain + picker desc), consumed by BOTH the picker and `resumeVocab` (its local `KIND_LABEL` deleted; `story` now tags `SHORT FICTION`). Picker & card **cannot drift**. Server `type` verified `text not null` with no CHECK — `'professional'` round-trips with zero DDL, no sync change.
- **Slice 2 — the picker.** Rebuilt `CreateProject` per the feel source: three domain groups (hairline-ruled headers) of 3 form cards (name + one-line desc), "Something else" full-width below. Selecting sets BOTH `kind` + `type`. Self-contained F-arc visual world (`.create-picker` local tokens); orange only on selection + Start writing. Static group order (the first-run sort hint is deferred to the HOME-verification pass, logged).
- **Slice 3 — title-later.** Empty-title block removed; the title input is optional below the picker ("Untitled — you can name it after you've written"), empty → `createBinder`'s existing 'Untitled'. "Start writing" enables on form selection, not title presence.
- **Slice 4 — landing.** Every real form births binder + first manuscript page → `/page/:id`, Free write, **caret waiting** (PageEditor now autofocuses an empty page). Something else → the project overview.
- **Slice 5 — inline rename.** ProjectHome's title is click-to-edit (Enter/blur → `saveProject`, Escape cancels); the crumb + mirror card reflect it next render. The eyebrow is now domain-aware (`domainLabel`, incl. Professional).
- Verified in-harness (20 checks: picker structure + keyboard-reachable + Start-disabled-until-picked; academic/essay + professional/article + creative/book births + Something-else→overview; empty-title → "Untitled" page, focused, Free write; card vocabulary essay→ESSAY, story→SHORT FICTION, legacy book/screenplay unchanged; rename commits + reflects in crumb + card; legacy binder renders on card/DrawersTree/ProjectHome). `createBinder` gained an optional `type` param (default creative; sole caller updated). `tsc` (desktop + server) + `build:web` + selftest green.
- **Deploy:** rides the combined arc-F gate — one `railway up` ships F2 + F3 + F4 after Nick's hardware pass (picker legibility joins the checklist).

## F3 — Catch: the zero-decision capture — built (rides F2's tablet gate)
Brief: [f3-catch-brief.md](f3-catch-brief.md). Arc F ticket 3/6 — small UI + routing, no schema. Capture must cost nothing, but the Desk's "New page" routed through the legacy scratch-draft `/sprint` path while real pages are JournalEntries — two capture models, the fast one buried. F3 makes capture ONE always-available action that opens a fresh journal page instantly (no title, no kind, no filing); filing stays a later low-stakes gesture via the Shelf.
- **One model:** shared `components/useCatch.ts` → `createJournalPage()` (already existed) → `/journal/:id` (the authored J10 editor, which self-focuses + places the caret). No warmStart — a blank page has nothing to glow. Honor-discard (J1a) guarantees a blank, untouched Catch leaves no litter.
- **Slice 1 — Desk action.** "New page" (scratch link) → **"＋ Catch a thought"** (`.wz-catch`, bordered quiet **tan** — the orange budget stays on the return card's primary) + an "N" kbd hint. "Begin project" unchanged.
- **Slice 2 — rail action.** A **＋ Catch** button at the top of the DeskRail — one tap from Journal / Shelf / Drawers / ProjectHome and the writing surfaces (where it inherits the rail's chrome-dissolve for free). Same shared helper.
- **Slice 3 — `n` shortcut.** Gmail-style bare `n`/`N` (no modifiers) fires Catch via one window listener in the always-mounted DeskRail; ignored entirely when the event target is an input/textarea/contenteditable or mid-IME-composition — a key that types never captures. (Ctrl/Cmd+N is browser-reserved in the web build; Electron menu accelerators are a separate later concern.)
- **Slice 4 — retire scratch from the Desk only.** Removed the Desk's `clearDraft('scratch')` + `/sprint` use (grep-clean); `/sprint` + the `'scratch'` draft stay untouched for project sprints.
- Verified in-harness (14 checks: Desk Catch → blank focused page; blank abandon → honor-discarded, stream gains nothing; rail Catch from all four surfaces + rail carries chrome-fade; `n` fires on Desk, does NOT while typing (types instead), Ctrl+n ignored; kept Catch persists + appears in the stream + files to the Shelf with one-home holding). `tsc` (desktop + server) + `build:web` + selftest green.
- **Deploy:** rides F2's tablet gate — one `railway up` ships F2 + F3 together after Nick's hardware pass. Adds two items to that pass: the Desk Catch button and the rail action at thumb size.

## F2 — The mirror card (warm start) — built (tablet gate before deploy)
Brief: [f2-mirror-card-brief.md](f2-mirror-card-brief.md). Arc F ticket 2/6 — FEEL WORK. F1 made resume typed but the Desk rendered none of it (label-only button while `lastLine`/`daysAgo`/`kind`/`pageType`/`home` were computed and dropped). F2 turns the Desk into a **mirror** — the writer's own last sentence becomes the primary action's justification — and the landing carries a warm start that releases into flow. Renders ONLY from the typed pointer; nothing writes to entry text (clean-save invariant harness-asserted).
- **Slice 1 — `describeTarget()` (pure).** New `store/resumeVocab.ts`: `ResumeTarget → { tag, crumb, note? }`. `tag` = FORM · SURFACE from the pointer alone — kind (book→BOOK…) else drawer-name-uppercased else PROJECT; pageType (manuscript→MANUSCRIPT, character→CHARACTER PAGE…); home overrides (journal→"JOURNAL · UNFILED", shelf→"SHELF · UNFILED"); legacy project → kind label alone. `crumb` = [drawer?, project?, page firstLine(40)] (loose/shelf → [Home, firstLine]). Exposed on `window.wrizoVocab` for the harness.
- **Slice 2 — the return card (Desk).** When a target exists the primary block becomes `.wz-return`: eyebrow "PICK UP WHERE YOU LEFT OFF" + `tag` (accent, small-caps); crumb (project/page-label bolded); the `lastLine` in quotes (`--font-prose` italic, clamped 2 lines; falls back to the page title / "Untitled" on a blank page); when-row (`relativeDays` + "Keep writing"). Support pages (non-manuscript) get a quiet **structural link** back to the binder's newest manuscript page (a fact of the binder, omitted if none); loose/shelf get the "file it later from the Shelf, or never" note. No target → the plain "Start something" primary, unchanged. Orange lands only on the action + tag; square corners, 1px border.
- **Slice 3 — warm-start landing.** The card navigates with one-shot `state.warmStart`. New `components/useWarmStart.ts`: on a warm landing PageEditor + the authored JournalEntry place the caret at end and paint a **render-only** overlay (`.wz-warm`, measured via a Range over the last paragraph — never touches editable DOM) — `rgba(255,152,0,.11)` + inset 3px left accent — releasing on the first forward keystroke OR after 6s (~1.3s transition; reduced-motion → static tint, still clears on keystroke). The history state is consumed (replace) so back/refresh never re-glows. Drive-by papercut: a shelved typed page's "Done" now exits to `/shelf` (was `/journal`).
- **Slice 4 — Change-4 reconciliation.** Deep resume for page targets keeps orientation: the PageEditor crumb (Drawer / Binder / Page) renders on landing, JournalEntry keeps its journal back link, ProjectHome's overview-arrival marker is untouched (legacy targets).
- Verified in-harness (31 checks: all six card shapes — binder manuscript / support+structural-link / loose / shelf / legacy sprint / no-target; `describeTarget` tag+crumb+route+home per shape; warm landing glows + **byte-identical saved text** + first-keystroke release + 6s auto-release + reduced-motion static-tint + one-shot no-re-glow on refresh; shelved-page Done → `/shelf`). Durable harness add: `app.emulateMedia()` (CSS media emulation, sibling to `emulateDpr`). `tsc` (desktop + server) + `build:web` + selftest green.
- **TABLET GATE (deploy held):** feel work — warm-emphasis subtlety, card typography, and tap targets are eyeballed on hardware BEFORE `railway up`. Not yet deployed; awaiting Nick's hardware pass.

## F1 — Resume repair (the typed pointer) — shipped
Brief: [f1-resume-repair-brief.md](f1-resume-repair-brief.md). Arc F ticket 1/6 — pure plumbing. The resume system predated Pages: it read only `sprintText`/story plans, PageEditor never bumped the parent's activity, and the Desk raced a stale timestamp so a chapter session resolved to `/journal/:id` (the ink view), not the mode-aware editor. F1 makes resume a **typed pointer**: the target is the most recently edited writing surface, whatever it is, carrying `home`/`kind`/`pageType` so any surface renders from the writer's own trail.
- **Slice 1 — activity stamping.** `Project.lastActivePageId?`; `lastActivityType` union += `'page'`. ONE seam: `saveJournalEntry` stamps the parent project (`lastActivityAt`/`lastActivityType:'page'`/`lastActivePageId`) whenever `entry.projectId` is set — catches PageEditor autosave, legacy filed-page edits, and `setPageHome` filing; skips silently if the project is missing/soft-deleted. Server: boot-idempotent `alter table projects add column if not exists last_active_page_id text`; carried through `rowToProject` + `upsertProjects`.
- **Slice 2 — `getResumeTarget()` v2** returns `{ route, home:'binder'|'journal'|'shelf', kind?, pageType?, project?, entry?, label, lastLine, daysAgo }`. One recency race across every surface (binder pages by `entry.updatedAt`, loose journal, shelf, legacy sprint projects by `lastActivityAt||updatedAt`); newest wins; soft-deleted excluded. Route by pageType (typed → `/page/:id`, untyped → `/journal/:id`, matching the redirect so no bounce). **Stale-pointer guard:** a project's `lastActivePageId` must resolve to a live entry still parented to it, else the newest remaining binder page, else the overview. `lastLine` computed for every target (F2's substrate). Desk's inline `keepWritingRoute` race DELETED — the primary action is `getResumeTarget()?.route` else the create flow.
- **Slice 3 — route hygiene.** `JournalEntry` redirects a typed page (`pageType != null`) to `/page/:id` on mount (legacy untyped filed pages stay — ink preservation is load-bearing). Shelf's open path made pageType-aware so no typed page even links to the ink view.
- Verified in-harness (14 checks across DoD 1-7: chapter session stamps the project, resume → `/page/:chapterId`, target carries `home:binder`/`kind:book`/`pageType:manuscript`/`lastLine`, loose→journal, shelf→home shelf, legacy filed→`/journal` (ink guard), stale pointer → newest binder page, deep-link typed→`/page` redirect). `tsc` (desktop + server) + `build:web` + selftest green. Live round-trip after deploy.
- **DEFERRED (arc F, logged):** the return card UI/vocabulary (F2), Catch (F3), the writing picker + title-later (F4), SessionLog/TTFK (F5), first-line invitation (F6).

## FIX — Journal chrome recede + mobile fullscreen
- **Journal chrome now recedes on write/draw.** The dissolve engine lived only in the sprint/ModeStage surface, so the JournalEntry view's menus + text never faded while writing or drawing. Added `useChromeDissolve` to JournalEntry (editorSelector `.entry-full` so taps/strokes on the sheet don't summon chrome back; `rootRef` the page so `--fade-dur` stays scoped) driven by `noteWrite` on both text input AND pen `onDown`; marked the back-link / stamp+star / capture strip / title / routed note / tags / routing slot `.chrome-fade`; added the `ChromeHandle`. It drives WritingSession, so the DeskRail + global header recede in step. The lit sheet + ink never fade. Verified in-harness (8 checks: visible at rest, recede on write, rail recedes too, back-link fades, sheet never fades, Esc resurfaces, recede on draw, fullscreen button present).
- **Mobile fullscreen.** `FullscreenToggle` no longer hides itself where the Fullscreen API is unsupported (iOS Safari): it now always shows and, on iOS, falls back to an **in-app immersive mode** (`html.app-immersive` hides the rail + watermark + rail gutter and fills the dynamic viewport) so "Full screen" still maximizes the page on mobile. Desktop + Android keep the real Fullscreen API. (True OS fullscreen from the web isn't possible on iOS Safari — Add-to-Home-Screen standalone is the OS path; noted.)

## BUG — Journal ink capture (pen → text) — fixed
Brief: fix-journal-ink. Stylus drawing in the Journal converted to typed text with no ink retained.
- **Bisect (confirmed first, not blind):** the routing hypothesis was DISPROVEN — Journal entries open at `/journal/:id` (the ink view), never the text-only PageEditor; B4's only journal change was the additive greyed-tab strip (above the sheet, no stacking impact); strokes still persist. A synthetic CDP pen already produced a stroke (not text), so routing + canvas stacking were fine.
- **Root cause:** the sheet's pen listeners ran in the **bubble** phase, but on an authored page the editable text node is the pen's event TARGET — so OS stylus handwriting (which converts the pen to text) won at the target before the bubble-phase ink handler could `preventDefault`. (Synthetic pens don't trigger OS handwriting, which is why it "passed" headlessly.)
- **Fix:** move the pen interception to the **capture phase** (`{ capture: true }`) + `stopPropagation()` for pen in `onDown`, so the sheet grabs a pen FIRST and keeps it off the editable (no caret, no handwriting) — the stroke goes to the ink canvas. Non-pen (finger/mouse) still falls through, so caret/typing are unaffected. No `strokes` storage touched.
- Verified in-harness (6 checks: ink canvas mounts, pen → stroke not text, canvas painted, stroke persists across reload, existing ink entry still renders). Selftest green. **Real-stylus confirmation still wise** — the headless harness can't simulate OS handwriting, so the capture-phase hardening is verified structurally + against synthetic pens, not against a physical stylus.

## B5 — Pagination + large-screen foundations — shipped
Brief: b5-pagination. Two large-screen problems: the writing area was one long scroll (no page boundary), and the 32" sat mostly empty with tiny type/icons. (The ambitious spatial layout — dual-pane plot-beside-page — is deferred to a mockup + dogfooding round; this is the always-correct foundation it sits on.)
- **A — in-document pagination + page-turn reward.** A page is the **sheet HEIGHT** (not a line count, so it survives the type scale): a pagination effect in ModeStage watches the editor's content height vs the sheet height (`.mode-scroll` clientHeight); on crossing a boundary it flips — a subtle paper-feed animation (`.mode-page.flipping`, honors reduced-motion) + a soft synthesized page-turn rustle (WebAudio, gated by the sound toggle) + the progress bar resets with a reward flash. The bar now shows **page fill** (height fraction of the current sheet) and the meta shows `p.N`; `.mode-page[data-page]` exposes the current sheet. One "page" concept — no new term.
- **Large-screen foundations.** A `@media (min-width:1700px)` token block (placed LAST so it overrides the base widths) scales the small type + icons up — the 7.5px sublabels → 9px, the 18–20px glyphs/icon buttons up, the rail wider — so the 32" is legible (no dot icons). The writing column baseline widened to a centred `min(700px,64vw)` (→ `min(760px,46vw)` wide) with the surround as intentional ambient dark. The home (`.wz-desk`) already uses clamp() type, so it inherits the scale (B4).
- **One pages door.** "Pages" appeared in three places (SECTIONS panel stub, Pages⟷Plan toggle, ProjectHome section). Reconciled to one: dropped the duplicate **"Pages" stub from the Draft SECTIONS rail** — the canonical pages surface is ProjectHome, reached via the Pages⟷Plan toggle.
- Verified in-harness (8 checks: starts at page 0, overflow flips the sheet (`data-page≥1`) + page number + bar present, no SECTIONS "Pages" stub, sublabel 7.5px at base → 9px wide, rail scales up wide). `tsc` + `build:web` + selftest green.
- **DEFERRED (the mockup round):** the ambitious large-screen spatial layout (dual-pane plot-beside-page, side notecard board) — after living with a real book, Nick mocks 2–3 arrangements to react to, then briefs the chosen one. The tokens + centred baseline + consolidation here are its foundation.

## B4 — Home + journal navigation — shipped
Brief: b4-home-journal. The home answers one question (what now?), stops duplicating the left rail, and fixes the Journal behaviour + the Shelf label. (Large-screen type scale is B5's job — B4 ships at current scale and inherits it.)
- **#8 — home decluttered, centered, one action.** A single primary: **Keep writing** if there's recent work, else **Start something** (→ the B1 create flow). Below it, the quiet drawers + On-the-Shelf glance (DrawersTree) + the create entry (New page / Begin project) + "+ New Drawer". **Removed** the rail-duplicating "Open a Drawer" / "Open the journal" links. `.wz-desk` now centered (was left-clustered).
- **One logo element, opacity by surface.** New `App.BrandMark` (bottom-right, `z-index:110` to sit above the Desk's full-screen world): **full opacity on the home**, a faint watermark (`.1`) everywhere else, one `<img>` switched by route. The Desk's own top-left logo removed.
- **#9 — "On the Shelf".** DrawersTree's virtual "Unsorted" group renamed **On the Shelf** and moved **below** the drawer dropdowns (loose pages sit below filed work).
- **#11 — Journal = Free-Write capture.** A loose Journal entry's page interface now shows the mode strip with **Free write active and Draft / Format / Workshop / Publish GREYED**; clicking a greyed tab prompts *move this to a Drawer or the Shelf to develop it past capture* (with a "Send to the Shelf" action), and an **auto-save note** reassures that capture is safe without filing. Only for loose entries (`projectId == null`); filed pages open in the live page editor.
- Verified in-harness (11 checks: empty home → Start something, work → Keep writing, no rail-dup links, logo full on home / faded away, On-the-Shelf renamed + below drawers, no "Unsorted", Journal Free-Write active + 4 greyed + autosave note + file-first prompt). Selftest updated (Desk wait is label-agnostic now) + green. `tsc` + `build:web` green.

## B3 — AI assist frame — shipped
Brief: b3-ai-frame. The right-side ASSIST frame gets behaviour: it introduces itself once, invites the user to connect their own AI under the anti-slop boundary (clears blocks, never writes), and is the single channel any future AI response speaks through. Real provider wiring deferred — B3 is the frame + invitation + the channel seam, no model calls.
- **`store/aiAssist.ts` (new) — the channel.** A module pub/sub: `showAssistResponse(text)` / `clearAssistResponse()` / `useAssistResponse()`. Also exposed on `window.wrizoAssist` as the single public seam a future AI integration calls. Unit-testable independent of React.
- **C6 — first-entry pop-out + connect invitation.** The frame pops out the FIRST time the user switches to a non-Free-Write mode (flag `wrizo-assist-introduced`, once, never re-nags). It shows the invitation + boundary as invitation not warning ("Connect your AI to help clear blocks in your own writing. It will never write for you.") + a **Connect AI** CTA → a stub dialog.
- **C7 — frame controls + assist channel.** A collapse arrow on the panel collapses it to a thin border carrying a persistent tan assist icon that re-opens it (state persisted, `wrizo-assist-collapsed`). A response sent through the channel pops the frame out and displays it in `.assist-response`.
- **AI boundary preserved:** the frame stays SEALED in Free Write (no AI in generation); open/collapsible in Draft/Format. A response never surfaces while sealed.
- Verified in-harness (11 checks: sealed in Free Write, first pop-out expanded + invitation + flag, connect dialog, close-arrow collapses, icon re-opens, no re-nag after intro, channel pops out + shows text, seal holds on a response). `tsc` + `build:web` + selftest green.
- **DEFERRED:** the real provider connection (OAuth/keys/model calls); any actual AI feature (block-clearing) — B3 is the channel, not the contents.

## Top-strip polish + mode-aware typewriter — shipped
Patch (top-strip-polish) + a clarification on B2's typewriter default.
- **Typewriter is now mode-aware** (supersedes B2 C4's global default-off): ON in Free Write / Journal (generation), OFF in Draft/Format (revision). `writingSettings.typewriter` default back to `true` (the Free-Write preference); ModeStage gates the mechanic with `typewriterOn = mode === 'journal' && settings.typewriter`, so it never engages outside Free Write regardless of the toggle. This is the right resolution of C4's "scroll fights revision" — revision is Draft, where it's now off.
- **Strip separators** — a vertical seam (`#3a2613`) before Workshop and before Publish splits the postures+actions row into three groups: `Free write · Draft · Format | Workshop | Publish` (writing | workshop | delivery). Visual only — Workshop/Publish still dispatch as actions (`ModeSwitcher` renders a `.mode-tab-sep` + restores the action tab's left border).
- **Sound + gear → matched tan icon buttons by the editor's top-right.** The speaker emoji left the top strip; the sound toggle is now a one-color tan **mic** SVG (strikethrough when off) co-located with the **gear** in ModeStage's `.mode-gear-wrap` cluster (`soundOn`/`onToggleSound` props from QuickSprint; absent on PageEditor). The gear is a legible one-color tan SVG (was a tiny `⚙` that read as a dot on large monitors); both 34px, `var(--text-mid)`, matched.
- Verified in-harness (12 checks: two separators in the right places, sound+gear SVGs in the cluster + gone from the strip, icons match the resting label tan + matched size, mic strike toggles, typewriter on in Free Write / off in Draft). `tsc` + `build:web` + selftest green.

## B2 — Free Write & typewriter fixes — shipped
Brief: [b2-freewrite-typewriter-brief.md](b2-freewrite-typewriter-brief.md). Five dogfooding fixes to the Free Write / typewriter view in ModeStage (no change to the forward-only rules or the dissolve engine).
- **C1 — hold the line lower.** Active-line band `0.62 → 0.73`, so ~2 more lines of context stay visible before a line scrolls off / fades. Anchor only — fade curve untouched.
- **C2 — fresh-page first line full opacity (bug).** The top fade was a fixed mask, so a fresh/short page's first line sat in the faded zone. Now the mask applies only once content has actually scrolled past (`data-scrolled`, set from `scrollTop` in the band loop + a scroll listener); a fresh page never fades line 1.
- **C3 — typewriter jolt.** A line advance now gets a small upward overshoot-and-settle (~130ms, ~7px) rather than a smooth glide — a hint of mechanical paper-feed; honors reduced-motion (instant).
- **C4 — typewriter fully off + default off.** `writingSettings.typewriter` defaults **false** (the line-hold scroll fights revision → opt-in). Off → no hold / fade / jolt, plain scrolling (the band effect early-returns; the mask requires `data-typewriter='true'`).
- **C5 — leaving Free Write drops struck text.** Already correct via the clean re-seed: the editor reports `derivedText` (struck runs excluded), so switching Free write → Draft re-seeds the clean text and the struck spans vanish from view; struck content is never written to storage (the page persists clean text only). Confirmed in-harness.
- Verified in-harness (7 checks: default off, fresh page not scrolled + no mask, scrolls after fill, line held low, struck shown in Free write, Draft drops struck). `tsc` + `build:web` + selftest green.

## B1 — Start a book: content types + create pipeline — shipped
Brief: [b1-start-a-book-brief.md](b1-start-a-book-brief.md). The create-and-organize spine — a project can now actually be *started and written* as a Book. New Books/Stories are modeled as a project of chapter **Pages** from the first keystroke, sidestepping the body-vs-page debt (legacy single-body projects untouched).
- **Slice 1 — taxonomy (data + sync).** `Project.kind?: 'book'|'story'|'screenplay'|'other'` (the Binder.kind facet, distinct from `type` which stays default creative); `JournalEntry.pageType?: 'manuscript'|'character'|'worldbuilding'|'research'|'note'`. Server: boot-idempotent `alter table projects add column if not exists kind` + `journal_entries add column if not exists page_type`, carried through `rowToProject`/`upsertProjects` + `rowToJournalEntry`/`upsertJournalEntries` (no backfill — null = Other/untyped). `persistence`: `createBinder(title, kind, drawerId?)` + `createBinderPage(binderId, pageType)`.
- **Slice 2 — create pipeline (skeleton).** `CreateProject` reworked: a **kind picker** (Book/Story/Screenplay/Other) + title, drawer-scoped via `?drawer=` . Book/Story create a first `manuscript` page and open it in the editor; Screenplay/Other open the project home. `DrawersTree` gains an in-drawer **"+ Create New" → New Page / New Project** (a project created there inherits the `drawerId`). Loose "New page" at home unchanged.
- **Slice 3 — ProjectHome + writing the chapters.** ProjectHome surfaces `getBinderPages` grouped into **Manuscript** (chapters, "+ New chapter") vs **Support pages** (Character/Worldbuilding/Research/Note, "+ Add support page"); the Plan stays the StoryPlan/board. **New `PageEditor` (`/page/:id`)** — the load-bearing piece — opens a Page in the **mode-aware editor** (Free write/Draft/Format) operating on the PAGE's text, never the project's `sprintText`. The mode-aware editor turned out generic (only QuickSprint's wiring was sprint-specific), so this was a clean lean mount; mode switching re-seeds the clean derived text. A Pages↔Plan toggle on the page reaches the board. Typed pages open in `/page/:id`; legacy untyped filed pages keep the authored `/journal/:id` editor (preserves ink).
- Verified in-harness (9 checks: Book → first chapter opens, kind+pageType persist, chapter text saves, chapter under Manuscript, +New chapter → two chapters, Character support page, in-drawer create scoped to drawer). `tsc` (desktop + server) + `build:web` + selftest green. Live round-trip after deploy.
- **DEFERRED (logged):** structure auto-scaffold per kind; Format conventions per kind (Format greyed); support-page templates; academic type; body-vs-page unification for legacy projects; a polished create dialog (B1 is the skeleton).

## Writing-screen redesign — Version B + dissolve-to-bare — shipped
Brief: [writing-screen-redesign-brief.md](writing-screen-redesign-brief.md). Makes the screen express the settled model — desk-area locations in a left rail, page postures on the file, Plan as a sibling view, Workshop/Publish as actions — and resolves the chrome into a clean two-layer recede (navigation layer dissolves; incentive layer persists).
- **Slice 1 — postures renamed.** `ModeSwitcher`: **Free write** (forward-only, generate) · **Draft** (free edit, revise) · **Format** (greyed). Workshop removed from the switcher → a file **action** (greyed) alongside **Publish** (opens a stub dialog; real export deferred). AI seal re-maps with the rename (sealed in Free write, open in Draft) — labels only, no AI wired.
- **Slice 2 — `DeskRail` (new).** A slim fixed left rail of desk-area LOCATIONS — Journal · Shelf · Drawers · Library (Library greyed/stub) — replacing D1/D2's top-bar nav (removed from `GlobalHeader`). It's a `WritingSession` reader carrying `.chrome-fade`, so it recedes with the rest of the navigation layer. Routed content wrapped in `.app-main` (left padding) to clear it; on the Desk's full-screen world the rail sits behind, as the old header nav did.
- **Slice 3 — recede on the new chrome + timer rework.** The nav row, mode tabs, Pages/Plan toggle, actions, breadcrumb, and rail all carry the dissolve; the glow, progress bar, optional timer, caret, and page never do. **The big countdown sprint-timer box is gone** — the timer is now **opt-in (off by default)** and, when on, a slim `⏱ m:ss` session clock merged into the progress row (incentive layer, persists while writing). Finish is a quiet manual action (no byTimer path; "Keep going +5" → "Keep writing" dismiss). Settings gear updated: **Recede depth** (was Fade depth) · **Timer on/off** (new) · Progress metric · Typewriter (Top-bar dissolve/dim removed with the bar). `writingSettings`: `topBar` dropped, `timer` added.
- **Slice 4 — file's place.** A **Pages ↔ Plan** toggle on a binder file (Plan → the existing StructureBoard route); a quiet **Drawer / Binder / Page** breadcrumb in the navigation layer.
- Reused `useChromeDissolve` UNCHANGED (same 1.2s/3-min/2-min/0.4s timings + 56px-edge/Esc/tap-off summon) — only WHICH elements carry the dissolve class changed. Verified in-harness (16 checks: rail 4 items + Library greyed, tabs renamed, Workshop/Publish + stub dialog, keystroke recedes rail+nav while glow+progress stay, Esc resurfaces, timer off-by-default → slim readout, project breadcrumb, Pages/Plan toggle, Plan → board). `tsc` + `build:web` + selftest green.
- **DEFERRED (logged):** Plan side-by-side / movable notecards (this is the toggle + existing board only); Publish's real export; Format mode; full mode-aware editing of arbitrary Pages; AI wiring; drag-and-drop.

## Journal re-sync — one-time backfill of pre-D2 entries — shipped
Brief: [journal-resync-patch.md](journal-resync-patch.md). D2 made journal entries sync, but pre-existing entries were stranded: the in-memory dirty registry starts empty on load, and pre-D2 the client pushed entries and marked them clean (`stampMap` includes `journalEntries`) even though the old server silently dropped them — so every existing entry sits in localStorage flagged "synced" while the server never stored it.
- **`persistence.markAllJournalEntriesDirty()`** — re-flag every `cache.journalEntries[].id` dirty (incl. soft-deleted, so tombstones travel); touches neither the cache nor `updatedAt`.
- **`sync.ts` trigger** — after a `/sync` response, if `'journalEntries' in resp.pull` (the new server always sends the key; the old one never did) AND `writer-studio-journal-resync-v1` is unset → backfill once, set the flag, and fire an immediate follow-up sync to push the backlog promptly. Idempotent per device; LWW + stable ids make re-pushing a row the server already has a no-op.
- Harness double upgraded to mirror the post-D2 pull shape (always carries a `journalEntries` key) + capture pushes via `/api/_state`. Verified in-harness (4 checks: flag set, the stranded entry re-pushes, flag persists, a second run is prevented). `tsc` + `build:web` + selftest green. The pre-existing-entry case the throwaway round-trip can't reach by construction.

## Pages & the Shelf — D2 — shipped
Brief: [pages-shelf-d2-brief.md](pages-shelf-d2-brief.md). Makes pages first-class: durable (synced), filable into Binders, or held on a new Shelf. A page's home is exactly ONE of three — in a Binder (`projectId` set), on the Shelf (`projectId` null AND `shelved`), or in the Journal (`projectId` null AND `shelved` falsy) — one boolean, no migration.
- **Slice 0 — journal entries finally sync server-side** (the client half was always wired). Boot-idempotent DDL in `migrate.ts`: `create table if not exists journal_entries` carrying every JournalEntry field (text, project_id, session_id, starred, source + jsonb `tags`/`routed_project_ids`/`strokes`) plus the two NEW D2 columns `shelved boolean not null default false` and `beat_id text`, + `(user_id, updated_at)` index. `sync.ts`: `rowToJournalEntry` + `upsertJournalEntries` (last-write-wins, user-scoped, soft-delete; jsonb fields `JSON.stringify`'d like `beat_notes`); wired into `/sync` push + pull. `api.ts` unchanged (pull type already had `journalEntries`).
- **Slice 1 — page model.** `types`: `JournalEntry` gains `shelved?` and `beatId?` (the Page↔Beat seam, Foundation 3 — no UI yet). `persistence.ts`: `getJournalPages()` (loose + not shelved), `getShelfPages()` (loose + shelved), `getBinderPages(binderId)`, `setPageHome(pageId, binderId|'shelf'|'journal')` (the one mover — enforces exactly-one-home: filing a binder clears `shelved`, shelving clears `projectId`), `createShelfPage()`. `getJournalEntries` kept for the Desk's resume logic.
- **Slice 2 — surfaces.** New **Shelf** (`/shelf` + top-bar "Shelf" nav): sortable list of shelf pages, open in the existing page editor, **file to…** a Binder / send to Journal, new shelf page, **multi-select bulk file** (seeds the future AI "tidy the shelf"). **ProjectHome gains a "Pages" section** — `getBinderPages` with per-page move-out (binder's main draft + StructureBoard untouched). **The Journal** is now the loose-page stream (`getJournalPages`) — project-context captures surface under their Binder's Pages instead of double-showing. Shared `PageFileMenu` (reuses the Drawers `.dz-menu`).
- Verified in-harness (11 checks: Shelf renders, journal excludes shelved, file-to-binder persists `projectId` + clears `shelved` + leaves shelf, appears in Binder Pages, move-to-Journal, **`beatId` survives a home move**). `tsc` (desktop + server) + `build:web` + selftest green. **Live server round-trip** run against prod after deploy (register throwaway account → push a journal page incl. `projectId`/`shelved`/`beatId` → pull back intact; the SQL mirrors the proven drawers/storyPlans upsert).
- **DEFERRED (D2 non-goals, logged):** body-vs-page unification (the Binder's main draft staying a separate fragment-substrate surface is known debt); full mode-aware editing on an arbitrary Page (modes-UI brief); AI auto-organize the Shelf (designed as a clean drop-in — bulk-select is ready); Journal-as-Binder (Foundation 6); drag-and-drop filing.

## Drawers — D1: Drawers over Projects — shipped
Brief: [drawers-d1-brief.md](drawers-d1-brief.md). The top of the Drawers IA — a browsable `Drawer → Binder(Project)` tree that replaces the Desk's flat recent list. Deliberately minimal so the Binder/Page/Shelf taxonomy work shapes later slices without re-migrating.
- **New `Drawer` collection, mirrored verbatim on the existing pattern.** `types`: `Drawer { id, name, order, createdAt, updatedAt, deletedAt? }` + optional `Project.drawerId`. `persistence.ts`: `drawers` registered everywhere `drafts`/`journalEntries` are (KEYS / Cache / hydrate / dirty / DirtyRecords / getDirtyRecords / markClean / flushTimers / RemoteRecords / applyCollection; `resetLocalData` is automatic). CRUD: `getDrawers`/`getDrawer`/`saveDrawer`/`createDrawer`(order = max+1)/`renameDrawer`/`softDeleteDrawer`/`setProjectDrawer`. Client `sync.ts` stampMap gains `'drawers'`; `api.ts` needs no edit (its `pull`/`push` ARE the persistence `RemoteRecords`/`DirtyRecords`, already extended).
- **Server (boot-idempotent DDL, no migration file).** `migrate.ts` adds `create table if not exists drawers (...)`, `alter table projects add column if not exists drawer_id text`, and the `drawers_user_updated` index — same path as `users.name`, so it applies on the live Railway DB on the next `railway up`. `sync.ts`: `rowToDrawer` + `upsertDrawers` (copied from `upsertStoryPlans` — last-write-wins, user-scoped, `where excluded.updated_at > drawers.updated_at`); `drawer_id` added to `rowToProject` + `upsertProjects` (insert col + param + on-conflict update); `drawers` included in `/sync` push + pull.
- **NO backfill.** Projects with no/deleted `drawerId` render under a **virtual "Unsorted"** group (not a real row) — seeds the future Shelf cleanly without naming a drawer.
- **`DrawersTree.tsx` (one component, two mounts).** Collapsible drawers (sorted by `order`) + a top Unsorted group; items sorted most-recently-active then title, paged 10 at a time ("+ N more"). "+ New Drawer" with inline rename; per-drawer ⋯ menu (rename / delete = soft-delete → its projects fall back to Unsorted); per-row "move to…" menu (drawers + Unsorted). Binder row → existing **ProjectHome** (`/project/:id`); StructureBoard one click further. Mounted on the **Desk** (REPLACES the recent list, atomically) and a new **`/drawers`** route (top-bar "Open a Drawer" nav + Desk link).
- **Preserved:** Keep-writing resume, New page, Begin project, the Journal entry point, `/journal` + loose pages — all unchanged.
- Verified in-harness (11 checks: tree replaces recent, +New Drawer creates+persists, rename persists, seeded project under Unsorted, move-into-drawer persists `drawerId`, delete → soft-delete + fallback to Unsorted, row → ProjectHome). `tsc` (desktop + server) + `build:web` green; selftest green. Server round-trip rides the proven storyPlans pattern + applies on deploy.
- **DEFERRED (D1 non-goals, logged):** **D2** — the Page level inside a Binder (multiple pages per Binder). **Shelf / loose-journal organization** — hard prerequisite first: `journalEntries` do NOT sync server-side today (`apps/server/src/sync.ts` has no `journal_entries` table; the client push/pull is a no-op for them — they live only in this browser). Also deferred: renaming `Project → "Binder"` in code; drag-and-drop reorder/move; per-type default drawers.

## Mode-aware editor — Phases 2-3 + typewriter fade shipped
Brief: [mode-aware-editor-brief.md](mode-aware-editor-brief.md); built to the prototype `apps/desktop/scratch/wrizo-modes-hybrid.html` (feel source). New `components/ModeStage.tsx` wraps the editor in the writing-studio chrome; `useChromeDissolve.ts` is the push-back-to-writing engine; `store/writingSettings.ts` is the persisted gear.
- **Push-back-to-writing engine (`useChromeDissolve`).** Dissolve on keystroke (rails/format-pen bar → faint floor `--fade-min`, top bar → 0); **fixed timing 3-min wait → ~2-min slow fade-in** (the prototype's Preview/Real toggle was a demo — NOT shipped); explicit summon (viewport edge / `Esc` / tap-off-text) returns chrome fast (~0.4s) and cancels the slow timer; **casual pointer movement does NOT summon**. Drives `WritingSession` so the global header recedes in step; replaces `useChromeFade` in QuickSprint (the hook stays for other surfaces).
- **Glow + progress.** Ambient ember behind the page, opacity/scale tied to progress, **eased** (`pow(p,.55)`) so early words warm the room; blooms as the rails dissolve (they occlude it at rest). Progress bar under the page **stays visible while writing** (feedback, not chrome): metric Words / Time (first-write→now) / Off, soft goals (`WORD_GOAL=250`, `TIME_GOAL=25m`).
- **Rails + AI frame (frames only).** Left: Journal = capture (Spark deck / Fragments / Send→Drawer), Drafting = tools (Structure / Pages / Notes / Find) — labels + hoverable stubs. Right AI: **SEALED** shutter in Journal ("journal is yours alone"), **OPEN** empty assist surface elsewhere; **drawer-open animation** on Journal→other only.
- **Format / pen bar.** Drafting: working **Bold / Italic / Underline / bulleted-list / indent / outdent** (`document.execCommand` on the free-edit surface); heading / quote / ⋯more are stubs. Journal: **3 inks** (black-brown / red / blue) set the editor text + caret colour (new `penColor` prop on `ForwardOnlyEditor`); nib selector stub.
- **Settings (real, persisted behind the gear).** Progress (Words/Time/Off), Fade depth (Partial 8% / Full 0), Top bar (Dissolve / Dim = stays clickable), Typewriter (On/Off). `store/writingSettings.ts` (module store + `useWritingSettings` hook).
- **Typewriter fade (default on, toggle-able).** The lit page is a fixed-height scroll viewport; a top mask-gradient fades history, and a MutationObserver keeps the caret/active line at ~62% of the viewport so earlier lines ride up and dissolve as the writer moves down. Works in both modes (caret-rect band-scroll; falls back to last-line). Off → no mask, normal scroll.
- **Layout:** adapted into QuickSprint's flow (timer/finish/beat intact), 3-column centred stage; **rails hide < 900px** (phone/tablet-portrait get the page — the writing surface is what matters on small screens). Verified in-harness (18 checks: rails/sealed-AI/pen inks/glow/dissolve-on-write/Esc-resurface/progress/Drafting-open+bold/settings+persist/typewriter toggle/**IME no-regression**); selftest green.
- ⚠️ **HARDWARE GATE — next.** Validate on a real tablet (touch + stylus) and phone: dissolve feel + 3m/2m return + edge/Esc summon, typewriter scroll, pen inks with stylus, format bar, IME still clean. Deployed for Nick to test.
- **DEFERRED (logged):** caret-preservation on mode switch (lands at end today — do AFTER this build, per Nick); deep rail internals (outline/notes/working Find); format sub-menus + nib/ink styles; AI features (frame only); stylus overlay per mode; cross-mode promotion gestures; Drawers (3-level IA); "What do you write?" prompt; per-element slow-return for the top bar (currently fast `.chrome-fade`, rails slow).

## Mode-aware editor — Phase 1 shipped (hardware gate next)
Brief: [mode-aware-editor-brief.md](mode-aware-editor-brief.md); prototype `apps/desktop/scratch/wrizo-modes-hybrid.html` (on disk, feel source for the chrome).
- **Mode-parameterized editor (no fork):** `ForwardOnlyEditor` gains a `mode` prop. **Journal** = today's forward-only runway + IME (unchanged). **Drafting** = free editing (runway OFF): a plain contenteditable seeded with the clean text, `onChange` reports live `textContent`; the per-render caret-anchor + forward-only handlers are skipped, the browser owns the caret.
- **Switching = lens change on the same doc:** re-seed the new mode with the current CLEAN text (struck excluded) and remount via key — prose carries across, caret lands at end, no navigation. Per-document last-used mode persisted (`wrizo-mode-<draftId>`); default Journal for scratch scraps, Drafting for project pages.
- **ModeSwitcher** (`components/ModeSwitcher.tsx`): tabs Journal · Drafting · Formatting · Workshop with stage sub-labels, active filled brass; Formatting/Workshop **greyed** (visible, disabled → brief "coming soon"). Integrated into the sprint surface; kept reachable (the dissolve/top-bar engine that fades it is Phase 2).
- **Clean-save invariant holds** in both modes (Journal = derived; Drafting = textContent). Verified in-harness: switching, Journal strike, Drafting delete, clean-text bridge, greyed tabs; selftest green; IME (Journal) unchanged.
- ⚠️ **HARDWARE GATE — next.** Per the brief, validate on a real tablet (touch + stylus) and phone — mode-switching, Journal runway, Drafting free-edit, IME-still-works — BEFORE Phases 2-3. Deployed so Nick can test.
- **DEFERRED (Phases 2-3, after the gate):** dissolve/glow/progress engine + settings; left rails (capture/tools frames); right AI frame (sealed/open/drawer-open); formatting bar (B/I/U/list working) + pen bar (3 inks + nib stub). Also: stylus overlay per mode, cross-mode promotion gestures, Drawers, "What do you write?" prompt.

## Cleanup batch 2: Changes 3/4/5 — shipped
- **Change 5 — revised forward-only runway.** Every backspace STRIKES (never deletes), escalating with consecutive presses: char → char → rest-of-word → previous-word → rest-of-sentence → locked (nudge after a couple of no-ops); typing resets to step 1. New strike engine in `forwardOnly.ts` (`strikeStep`/`strikeTail` + word/sentence helpers); the active word is flushed on the first press so all strikes hit committed runs. `derivedText` already filters by `run.struck`, so the clean-save guarantee holds at **every** granularity (char/word/sentence) — verified: struck content stays visible, derived excludes it exactly.
- **Change 3 — name at signup + Desk letterhead.** Added a `name` column to `users` (idempotent `add column if not exists` on boot — picks up on the live DB without a migration runner); `apiRegister(email, password, name)`; the account step gains a "your name" field; `currentUser` module holds the authed user (set in App on /auth/me + sign-in, cleared on logout). Desk headline → top-left letterhead: **[Name]'s** in Crimson Pro over **Writing Desk** in Figtree (swappable slot); subheading → "Scribble, draft, plot, revise, or share (coming soon)".
- **Change 4 — resume routing de-confused.** "Keep writing" no longer drops into the exact last step: a project opens its **overview** (`/project/{id}`, not `/sprint` or `/beat`); a standalone journal page opens that page; whichever was touched most recently wins. ProjectHome's primary resume action gets a "↩ you last wrote here" marker when there's existing writing.

## Cleanup: Bug 1 (mobile IME) + Bug 2 (New page) — shipped
- **Bug 1 (mobile text vanishing) — fixed.** `ForwardOnlyEditor` had no IME/composition handling: `beforeinput` preventDefault clobbered soft-keyboard/swipe/autocorrect composition, so mobile-typed text vanished. Fix: (1) hand off to the IME during composition — skip preventDefault on `insertCompositionText`/`isComposing`, commit the finalized text on `compositionend`; (2) render the editable via `innerHTML` (dangerouslySetInnerHTML) so each render fully replaces the DOM — the post-composition render wipes the browser's draft nodes (no duplication), and React skips the update when the html string is unchanged (an unrelated re-render mid-composition can't clobber the composing text); (3) don't re-anchor the caret mid-composition. Verified in-harness with **real CDP composition events** (`Input.imeSetComposition` + commit): composed text lands, mixed keyboard+IME appends, runway still strikes. *Real-device confirmation still wise, but the CDP path mirrors mobile IME.*
- **Bug 2 (New page opened previous writing) — fixed.** `/sprint` with no project reuses the shared `'scratch'` draft, so "New page" reseeded the last text. Desk's "New page" now `clearDraft('scratch')` before navigating → blank every time ("Keep writing" still resumes it). Proper per-document persistence (unique page ids, no data loss) is the deferred big-architecture work.
- Harness gained an `ime()` helper (CDP composition) for future mobile-input testing.

## DEFERRED from the cleanup brief (next batch)
- **Change 5 — revised forward-only runway** (every backspace strikes; char→char→word→word→sentence escalation; widen `derivedText` to exclude struck content at every granularity). Intricate editor rework — separate focused slice. (Note: `derivedText` already filters by `run.struck`, so it's granularity-agnostic *as long as* strike ops isolate struck content into struck runs — the work is mostly the escalation logic + char/sentence strike ops in `forwardOnly.ts`.)
- **Change 3 — name at signup** (needs a `name` column + migration on the users table, `apiRegister` + account form) **+ Desk headline** `[Name]'s Writing Desk` (Crimson name over Figtree "Writing Desk") + subheading "Scribble, draft, plot, revise, or share (coming soon)".
- **Change 4 — resume routing**: Keep writing → project *overview* (not the last step) with a "you last wrote here" marker; track last-active doc + project/step (intersects the persistence-durability gap).

## Gate idle-nudges — extracted to a shared hook + v6 pool
- **`store/idleNudges.ts` (new):** the re-tuned nudge cadence (3→2→1 min, ephemeral #1/#2, held #3, keystroke-reset, no-repeat) lifted out of QuickSprint into a reusable `useIdleNudges({ active, activityKey })` hook. Behaviour only; each surface dresses its own slip (Crimson italic) + reduced-motion.
- **Pool swapped to v6's canonical 25** (now the source of truth, superseding the interim SME set) — 4 implicit registers; sensory fragments unpunctuated. **US spelling locked** ("color", "traveled") — v6 had British ("colour"/"travelled"); flag for one-word confirm if UK is wanted (trivial flip).
- **QuickSprint** refactored onto the hook (no regression — manual "Take a nudge" pull intact). **HomeFlow gate** now mounts it (`.wz-nudge`, bottom-center, v6 style) — a stuck newcomer gets prompts toward the 50-word goal. Both verified in-harness with shortened timers; selftest green.

## Desk (authed home) + invite drop — closes the HOME arc seam
- **Invite dropped (decided):** removed the invite field from the account step, the `inviteCode` param from `apiRegister`, the server-side invite check + `INVITE_CODE` env requirement (`apps/server/src/auth.ts`, `env.ts`). Wrizo is open — the writing-gate is the membership filter. Account collects email + password for now (passwordless/email-first stays backlog).
- **Desk (`pages/Desk.tsx`, new) replaces `SessionLauncher`** as the authed home (`/`), in the v6 "launch" aesthetic (scoped `.wz-*`): bighead "You're ready to keep writing." (swappable Figtree slot), filled-orange **Keep writing** (resumes `getResumeTarget()` → else the latest journal entry → else a fresh `/sprint`), quiet **New page** / **Begin project**, grayed **Customize** + tooltip. Account-create AND returning sign-in both land here.
- **No returning-user regression:** the Desk preserves what SessionLauncher gave — a restyled recent-work list (projects + journal pages, newest first), the journal stays reachable, resume resumes. Harness-verified: account(no invite)→Desk, gate entry in recent, Keep writing → the entry, journal link present; selftest green (freshSprint now navigates the Desk).
- Deleted orphaned `LoginScreen.tsx` + `SessionLauncher.tsx` (`Wordmark.tsx` now unused — left in place). Harness `freshSprint`/selftest updated off the old "Start writing" button.

## HOME port — anon front door (v6) — branch `home-port`, NOT deployed
First slice of the HOME port ([home-port-brief.md](home-port-brief.md)), built from `wrizo-home-v6.html` (the feel source of truth). Held for the tablet pass per the brief.
- **`HomeFlow.tsx` (new):** landing → forced first-write gate → reward → account / sign-in, as a stage machine. The gate **mounts the existing `ForwardOnlyEditor`** (zero editor logic) — runway, strikethrough, struck-words-drop-from-derived, paste block all inherited. WORD_GOAL=50 (lowerable). Both progress signals (fill-track + ambient ember-grow 0.4→1.0). Goal → bloom + word-echo of the writer's own clean words; reduced-motion suppresses the bloom, keeps the echo.
- **Pre-auth content survival (non-negotiable):** on "Create my desk" the gate's clean derived prose is saved as the first journal entry (`saveJournalEntry`), then auth — harness-verified end to end (landing→gate→reward→account→register→authed app, entry present).
- **Visuals:** ported v6 verbatim — espresso ground `#110600`/lift `#150a04`, light ink `#f0e7d8`, square corners, solid borders (translucent-orange only on the secondary btn), Nick's hand-drawn logo (`public/brand/wrizo-logo.png`, extracted from v6). All scoped under `.wz-home`.
- **Wiring:** App's anon branch now renders `HomeFlow` (was `LoginScreen`, now orphaned). Harness gained a `WS_ANON=1` gate to drive the anon front door.
- **DEFERRED / flagged (NOT in this slice):**
  - **Gate idle-nudges** — the re-tuned cadence lives in QuickSprint; mounting it in the gate needs that system extracted into a shared hook. Follow-up.
  - **Desk / launchpad** (v6 "launch" screen as the authed home, replacing SessionLauncher; Keep writing / New page / Begin project / grayed Customize + tooltip). After account-create the user currently lands on the existing SessionLauncher.
  - **Auth model mismatch** — v6's account is email-only, but the shipped `apiRegister` needs **email + password + invite code**. The account step collects all three (styled to v6). A true email-only/passwordless, no-invite public front door needs a backend ticket — decision for Nick.
  - Bigheads are Figtree `.bighead` placeholders (hand-drawn art swaps later); v6's 25-prompt pool not yet adopted (the nudge system uses the canonical SME pool).

## Tablet tripwire — "Take a nudge" button (watch, decide on hardware)
The manual nudge pull is kept but de-emphasized (translucent `.btn-ghost`, surfaces on hover/focus — a tool for someone who needs it, not a treat that beckons). Manual pull and the auto-cadence are guarded so they can't fire two nudges at once. **Tablet watch:** do you reach for it to get *unstuck*, or to *avoid writing*? If it's avoidance, kill the button. That's the tripwire.

## Nudge pool reconciled to canonical (repo-claim 3 closed)
Replaced CC's improvised 25 with the canonical SME pool — verbatim, 4 balanced registers (sensory images / concrete moves / permission-giving / pre-1930 public-domain literary allusions). Cleared the `FIXME(home-port)`. Built + confirmed in bundle; old strings gone. Cadence/render unchanged (Crimson Pro italic). Not deployed (held with the pre-home tweaks for the tablet pass).

## Pre-HOME tweaks (v6-independent; branch `pre-home-tweaks`)
Two standalone tasks from the HOME port brief ([home-port-brief.md](home-port-brief.md)), done while the HOME shell stays gated on `wrizo-home-v6.html`. NOT deployed — held for the tablet pass per the brief.
- **Nudge re-tune (global mechanic):** replaced the single 60s budget-3 model with an escalating idle cadence — first nudge at 3 min, second +2 min, third +1 min then HOLDS. First two are ephemeral (dissolve after 10s); the third persists. Any keystroke resets to the 3-min countdown. Removed the old A6 budget/lockout. Nudge now renders in Crimson Pro italic (the writing voice) with an opacity-gated fade; reduced-motion → instant via the global reset. Verified the full state machine in-harness with shortened constants (ephemeral #1/#2, held #3, reset, manual). The gate inherits this automatically when it mounts `WritingSession`.
  - ⚠️ **Pool is NOT canonical** — still CC's 25 from the last ticket, flagged `FIXME(home-port)` in `QuickSprint.tsx`. Needs the canonical curated 25 (4 registers) from the prototype/transcript; the cadence is pool-agnostic so it's a one-array swap.
- **Structured-notes font cleanup:** `.form-input` / `.form-select` had no `font-family` → UA default. Set them (and made `.form-textarea` explicit) to `var(--font-ui)` = Figtree, so project-builder structure inputs (Setup field, beat notes) speak in the UI voice, never the prose serif.

## Brand paint — Ember → Wrizo (name, fonts, accent, ground)
**Branch:** `brand-paint` off `m1-creative-flow`. Brief: [brand-ticket-brief.md](brand-ticket-brief.md). Paint only — no structure.
- **Name:** Wordmark now renders the text **Wrizo** in Figtree, glyph removed (no `ember-hero.png`); tab `<title>` → Wrizo; PWA manifest `name`/`short_name` → Wrizo; testament line "tended Wrizo". Package names (`@writer-studio/*`) untouched, per scope.
- **Fonts:** Figtree (titles/UI via `--font-display`/`--font-ui`), Crimson Pro (body + writing surface via `--font-prose`, inherited by `.paper-page`/`ForwardOnlyEditor` — no logic touched). Installed `@fontsource-variable/crimson-pro`; wired `@fontsource-variable/figtree`. Removed Mulish/Newsreader/Courier Prime imports; `--font-mono` falls back to system mono.
- **Accent:** `--brass` → **#ff9800** (locked/invariant; token name kept to avoid a global rename). First application of the real orange (the `.btn-brass` action).
- **Ground:** `--ink-950` and `html`/`theme-color`/manifest bg → **#110600** deep espresso.
- **Verified:** tsc + build:web pass; build-log self-check inverts the font list (emits `figtree-*` + `crimson-pro-*`, no `mulish-*`/`newsreader-*`/`courier-prime-*`); harness selftest green; brand runtime check confirms title/wordmark = Wrizo, no "Ember" on screen, `--brass`=#ff9800, `--ink-950`=#110600, UI=Figtree, writing surface=Crimson Pro.
- **Note:** unused asset `public/brand/ember-hero.png` left in place (file removal is out of paint scope; remove at the hand-drawn wordmark pass).

## Deferred (logged, not scheduled)
- **Import-external-draft case** — the legitimate "draft started elsewhere, want to import it" exception to the paste block. Deferred per HOME brief.
- **Recurring reward mechanisms (Desk/journal)** — word-count / page / draft-completion rewards, with **customizable triggers** and **toggle-able** audio/visual. Separate from the one-time gate reward.
- **Milestone-gated user-handwriting customization** — users replace the logo + section headings with their own handwriting as a progression reward. The swappable heading/logo slot is being built in the HOME port; the gating is deferred.
- **Dark/light writing-mode toggle (Desk)** — post-gate; keep structure tokens theme-ready so it's cheap.
- **Paste block internal/external split + friction-gated disable** — the current block stops ALL paste; the brief wants internal copy/paste allowed, external blocked, and disabling behind friction + an AI-slop warning. Sub-build, non-blocking for HOME.
